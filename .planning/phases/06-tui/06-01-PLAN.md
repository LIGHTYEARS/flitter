---
phase: 06-tui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/tui/src/scroll/scroll-controller.ts
  - packages/tui/src/scroll/scroll-controller.test.ts
  - packages/tui/src/scroll/scroll-physics.ts
  - packages/tui/src/scroll/scrollable.ts
  - packages/tui/src/scroll/scrollable.test.ts
  - packages/tui/src/scroll/scroll-key-handler.ts
  - packages/tui/src/scroll/index.ts
autonomous: true
requirements:
  - TUI-09

must_haves:
  truths:
    - "ScrollController 可以通过 jumpTo/animateTo 改变滚动偏移"
    - "ScrollController 的 offset 被 clamp 到 [0, maxScrollExtent] 范围"
    - "followMode 开启时新内容追加自动滚到底部"
    - "Scrollable 容器根据子内容高度和约束计算 maxScrollExtent"
    - "键盘/鼠标滚动事件正确映射到 ScrollController 方法"
  artifacts:
    - path: "packages/tui/src/scroll/scroll-controller.ts"
      provides: "ScrollController 类 — 滚动偏移管理 + 动画 + followMode"
      exports: ["ScrollController"]
    - path: "packages/tui/src/scroll/scroll-physics.ts"
      provides: "ScrollPhysics 接口 + ClampingScrollPhysics 实现"
      exports: ["ScrollPhysics", "ClampingScrollPhysics"]
    - path: "packages/tui/src/scroll/scrollable.ts"
      provides: "Scrollable StatefulWidget — 滚动容器"
      exports: ["Scrollable"]
    - path: "packages/tui/src/scroll/scroll-key-handler.ts"
      provides: "ScrollKeyHandler — vim/标准键盘 + 鼠标滚轮处理"
      exports: ["ScrollKeyHandler"]
    - path: "packages/tui/src/scroll/index.ts"
      provides: "scroll 模块统一导出"
  key_links:
    - from: "packages/tui/src/scroll/scrollable.ts"
      to: "packages/tui/src/scroll/scroll-controller.ts"
      via: "Scrollable 内部持有 ScrollController 并在 layout 后调用 updateMaxScrollExtent"
      pattern: "updateMaxScrollExtent"
    - from: "packages/tui/src/scroll/scroll-key-handler.ts"
      to: "packages/tui/src/scroll/scroll-controller.ts"
      via: "键盘事件映射到 scrollUp/scrollDown/scrollPageUp 等方法"
      pattern: "scrollUp|scrollDown|scrollPageUp|scrollPageDown"
---

<objective>
实现 ScrollController + Scrollable 容器 + ScrollKeyHandler，为 ListView 和所有可滚动内容提供滚动基础设施。

Purpose: TUI 框架中所有超出视口的内容（对话消息、文件列表、命令输出）都需要滚动支持。ScrollController 是滚动系统的核心状态管理器。
Output: scroll/ 子目录包含完整的滚动控制、物理特性、容器 Widget 和键盘处理器。
</objective>

<execution_context>
@/Users/bytedance/.oh-my-coco/studio/flitter/.trae/get-shit-done/workflows/execute-plan.md
@/Users/bytedance/.oh-my-coco/studio/flitter/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-tui/06-RESEARCH.md

<interfaces>
<!-- Phase 4/5 已有接口，executor 直接使用 -->

From packages/tui/src/tree/constraints.ts:
```typescript
export interface Size { readonly width: number; readonly height: number; }
export class BoxConstraints {
  readonly minWidth: number;
  readonly maxWidth: number;
  readonly minHeight: number;
  readonly maxHeight: number;
  tighten(args?: { width?: number; height?: number }): BoxConstraints;
  loosen(): BoxConstraints;
  constrain(size: Size): Size;
}
```

From packages/tui/src/tree/stateful-widget.ts:
```typescript
export abstract class State<T extends StatefulWidget = StatefulWidget> {
  get widget(): T;
  get element(): StatefulElement;
  get mounted(): boolean;
  setState(fn?: () => void): void;
  abstract build(): Widget;
  initState(): void;
  dispose(): void;
  didUpdateWidget(oldWidget: T): void;
}
export abstract class StatefulWidget extends Widget { ... }
```

From packages/tui/src/tree/frame-scheduler.ts:
```typescript
export type FramePhase = "build" | "layout" | "paint" | "render";
export class FrameScheduler {
  requestFrame(): void;
  // ~16ms 帧循环：build → layout → paint → render
}
```

From packages/tui/src/vt/types.ts:
```typescript
export interface KeyEvent { key: string; ctrl: boolean; alt: boolean; shift: boolean; }
export interface MouseEvent { button: number; x: number; y: number; action: 'press' | 'release' | 'move'; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScrollController 滚动状态管理器</name>
  <files>packages/tui/src/scroll/scroll-controller.ts, packages/tui/src/scroll/scroll-controller.test.ts, packages/tui/src/scroll/scroll-physics.ts</files>
  <read_first>
    - amp-cli-reversed/framework/widget-property-system.js (行 393-585, ScrollController Q3 原始实现)
    - packages/tui/src/tree/frame-scheduler.ts (FrameScheduler API, 用于动画帧请求)
  </read_first>
  <behavior>
    - ScrollController 初始 offset=0, maxScrollExtent=0, followMode=true, disposed=false
    - jumpTo(50) 设置 offset=50 并通知所有 listeners
    - jumpTo(-10) clamp 到 0; jumpTo(999) 当 maxScrollExtent=100 时 clamp 到 100
    - animateTo(target, duration) 在多帧内平滑过渡 offset 到 target
    - scrollUp(3) 等价于 jumpTo(offset - 3); scrollDown(3) 等价于 jumpTo(offset + 3)
    - scrollToTop() 等价于 jumpTo(0); scrollToBottom() 等价于 jumpTo(maxScrollExtent)
    - scrollPageUp(viewportSize) 等价于 scrollUp(Math.floor(viewportSize / 2))
    - scrollPageDown(viewportSize) 等价于 scrollDown(Math.floor(viewportSize / 2))
    - updateMaxScrollExtent(200) 设置 maxScrollExtent=200
    - followMode=true 时，updateMaxScrollExtent 自动调用 scrollToBottom()
    - enableFollowMode()/disableFollowMode()/toggleFollowMode() 正确切换
    - addListener/removeListener 正确管理回调列表
    - dispose() 清理 listeners 和动画 timer, 设置 disposed=true
    - dispose 后调用任何方法不 throw 但不执行
    - ClampingScrollPhysics.clampOffset(offset, 0, max) 返回 clamp 值
  </behavior>
  <action>
创建 `packages/tui/src/scroll/scroll-physics.ts`:
- 导出 `ScrollPhysics` 接口: `clampOffset(offset: number, minExtent: number, maxExtent: number): number`
- 导出 `ClampingScrollPhysics` 类实现 `ScrollPhysics`，使用 `Math.max(min, Math.min(max, offset))`

创建 `packages/tui/src/scroll/scroll-controller.ts`:
- 导出 `ScrollController` 类，字段:
  - `private _offset = 0`
  - `private _maxScrollExtent = 0`
  - `private _followMode = true`
  - `private _disposed = false`
  - `private _listeners: Set<() => void> = new Set()`
  - `private _animationTimer: ReturnType<typeof setInterval> | null = null`
  - `private _animationTarget: number | null = null`
  - `private _animationStartOffset: number = 0`
  - `private _animationStartTime: number = 0`
  - `private _animationDuration: number = 0`
  - `private _physics: ScrollPhysics = new ClampingScrollPhysics()`
- getter: `offset`, `maxScrollExtent`, `followMode`, `disposed`
- `addListener(fn)` / `removeListener(fn)` — Set add/delete
- `_notifyListeners()` — iterate Set, 每个回调 try/catch
- `jumpTo(offset)`: clamp via physics, 设置 _offset, 取消动画, notifyListeners
- `animateTo(target, duration = 200)`: 记录 start offset/time/duration, 开始 16ms interval, 每 tick 用 easeOutCubic 计算当前 offset
- 动画曲线 `_easeOutCubic(t)`: `1 - Math.pow(1 - t, 3)`
- `scrollUp(n = 3)` / `scrollDown(n = 3)` / `scrollToTop()` / `scrollToBottom()`
- `scrollPageUp(viewportSize)` / `scrollPageDown(viewportSize)` — 半屏翻页
- `updateMaxScrollExtent(extent)`: 设置值, followMode=true 时调用 scrollToBottom()
- `enableFollowMode()` / `disableFollowMode()` / `toggleFollowMode()`
- 在 jumpTo 中: 如果用户主动往上滚 (newOffset < maxScrollExtent), 自动 disableFollowMode
- `dispose()`: clearInterval, 清空 listeners, 设置 _disposed=true

测试文件 `scroll-controller.test.ts`:
- 测试 offset clamping (jumpTo 负值/超大值)
- 测试 listener add/remove/notify
- 测试 followMode 自动滚底
- 测试 dispose 后行为
- 测试 scrollUp/Down/PageUp/PageDown
- 测试 animateTo 最终 offset 到达 target
  </action>
  <verify>
    <automated>cd /Users/bytedance/.oh-my-coco/studio/flitter && npx pnpm@10 exec tsx node_modules/.bin/vitest run packages/tui/src/scroll/scroll-controller.test.ts 2>/dev/null || npx tsx --test packages/tui/src/scroll/scroll-controller.test.ts 2>/dev/null || bun test packages/tui/src/scroll/scroll-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scroll-controller.ts 包含 `export class ScrollController`
    - scroll-controller.ts 包含 `jumpTo(`, `animateTo(`, `scrollToTop(`, `scrollToBottom(`
    - scroll-controller.ts 包含 `_followMode`
    - scroll-controller.ts 包含 `_easeOutCubic(`
    - scroll-physics.ts 包含 `export interface ScrollPhysics`
    - scroll-physics.ts 包含 `export class ClampingScrollPhysics`
    - scroll-controller.test.ts 包含至少 15 个 test/it 块
    - 所有测试通过 (exit code 0)
  </acceptance_criteria>
  <done>ScrollController 具备 offset 管理、clamping、动画、followMode、dispose 全部能力，15+ 测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scrollable 容器 Widget + ScrollKeyHandler</name>
  <files>packages/tui/src/scroll/scrollable.ts, packages/tui/src/scroll/scrollable.test.ts, packages/tui/src/scroll/scroll-key-handler.ts, packages/tui/src/scroll/index.ts</files>
  <read_first>
    - packages/tui/src/scroll/scroll-controller.ts (Task 1 产出)
    - amp-cli-reversed/framework/widget-property-system.js (行 300-392, ScrollKeyHandler P1T)
    - packages/tui/src/tree/stateful-widget.ts (StatefulWidget/State 基类)
    - packages/tui/src/tree/render-box.ts (RenderBox 基类)
    - packages/tui/src/tree/constraints.ts (BoxConstraints, Size)
    - packages/tui/src/vt/types.ts (KeyEvent, MouseEvent 类型)
  </read_first>
  <behavior>
    - ScrollKeyHandler.handleKeyEvent(ArrowUp, controller) 调用 controller.scrollUp(scrollStep)
    - ScrollKeyHandler.handleKeyEvent(ArrowDown, controller) 调用 controller.scrollDown(scrollStep)
    - ScrollKeyHandler.handleKeyEvent(PageUp, controller) 调用 controller.scrollPageUp(viewportSize)
    - ScrollKeyHandler.handleKeyEvent(Ctrl+u, controller) 调用 controller.scrollPageUp(viewportSize)
    - ScrollKeyHandler.handleKeyEvent(Home, controller) 调用 controller.scrollToTop()
    - ScrollKeyHandler.handleKeyEvent(End, controller) 调用 controller.scrollToBottom()
    - ScrollKeyHandler.handleMouseScroll(button=64, controller) 调用 controller.scrollUp(scrollStep)
    - ScrollKeyHandler.handleMouseScroll(button=65, controller) 调用 controller.scrollDown(scrollStep)
    - Scrollable widget 创建 RenderScrollable, layout 时计算 maxScrollExtent = max(0, childHeight - viewportHeight)
    - Scrollable paint 时根据 controller.offset 裁剪子 Widget 输出
  </behavior>
  <action>
创建 `packages/tui/src/scroll/scroll-key-handler.ts`:
- 导出 `ScrollKeyHandler` 类:
  - `constructor(options?: { scrollStep?: number; pageScrollStep?: number })`
  - `private _scrollStep: number` — 默认 3
  - `private _pageScrollStep: number` — 默认 10
  - `handleKeyEvent(event: KeyEvent, controller: ScrollController, viewportSize: number): boolean`
    - ArrowUp / k → controller.scrollUp(scrollStep), 返回 true
    - ArrowDown / j → controller.scrollDown(scrollStep), 返回 true
    - PageUp → controller.scrollPageUp(viewportSize), 返回 true
    - PageDown → controller.scrollPageDown(viewportSize), 返回 true
    - Ctrl+u → controller.scrollPageUp(viewportSize), 返回 true
    - Ctrl+d → controller.scrollPageDown(viewportSize), 返回 true
    - Home / g → controller.scrollToTop(), 返回 true
    - End / G (shift+g) → controller.scrollToBottom(), 返回 true
    - 其他 → 返回 false
  - `handleMouseScroll(button: number, controller: ScrollController): boolean`
    - button 64 (wheel up) → controller.scrollUp(scrollStep), 返回 true
    - button 65 (wheel down) → controller.scrollDown(scrollStep), 返回 true
    - 其他 → 返回 false

创建 `packages/tui/src/scroll/scrollable.ts`:
- 导出 `Scrollable` (StatefulWidget 子类):
  - props: `controller?: ScrollController`, `child: Widget`
  - State 内部: 如果外部未提供 controller，自动创建
  - `initState()`: controller.addListener → setState
  - `dispose()`: controller.removeListener, 如果自管理则 controller.dispose()
  - `build()`: 返回一个包装 Widget，在 layout 阶段计算 maxScrollExtent = max(0, childSize.height - constraints.maxHeight)
  - paint 阶段: 应用 offset 偏移 (子 Widget 的 y 坐标 -= controller.offset)
- 说明: 简化实现 — Scrollable 是一个 StatefulWidget，其 RenderObject 扩展自 RenderBox, 在 performLayout 中先 layout child 获取 childSize, 然后设置 maxScrollExtent; 在 paint 中应用 offset clip

创建 `packages/tui/src/scroll/index.ts`:
```typescript
export * from "./scroll-controller.js";
export * from "./scroll-physics.js";
export * from "./scrollable.js";
export * from "./scroll-key-handler.js";
```

更新 `packages/tui/src/index.ts` 添加:
```typescript
export * from "./scroll/index.js";
```

测试:
- ScrollKeyHandler 各键位映射验证
- Scrollable 创建/销毁生命周期
- Scrollable 自管理 controller dispose
  </action>
  <verify>
    <automated>cd /Users/bytedance/.oh-my-coco/studio/flitter && npx pnpm@10 exec tsx node_modules/.bin/vitest run packages/tui/src/scroll/scrollable.test.ts 2>/dev/null || npx tsx --test packages/tui/src/scroll/scrollable.test.ts 2>/dev/null || bun test packages/tui/src/scroll/scrollable.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - scroll-key-handler.ts 包含 `export class ScrollKeyHandler`
    - scroll-key-handler.ts 包含 `handleKeyEvent(` 和 `handleMouseScroll(`
    - scrollable.ts 包含 `export class Scrollable`
    - index.ts 包含 `export * from "./scroll-controller`
    - packages/tui/src/index.ts 包含 `export * from "./scroll/index`
    - scrollable.test.ts 包含至少 10 个 test/it 块
    - 所有测试通过 (exit code 0)
  </acceptance_criteria>
  <done>Scrollable Widget + ScrollKeyHandler 实现完整，vim/标准键盘/鼠标滚轮映射覆盖，10+ 测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → ScrollController | 键盘/鼠标事件通过 ScrollKeyHandler 转换为滚动操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | D (DoS) | ScrollController.animateTo | accept | 动画 interval 有 duration 上限，timer 在 dispose 时清理，不会泄漏资源 |
| T-06-02 | T (Tampering) | ScrollController._offset | mitigate | offset 通过 ClampingScrollPhysics.clampOffset 强制限制在 [0, maxScrollExtent]，防止越界 |
</threat_model>

<verification>
1. `bun test packages/tui/src/scroll/` 全部通过
2. `grep -r "export class ScrollController" packages/tui/src/scroll/` 找到定义
3. `grep -r "export class Scrollable" packages/tui/src/scroll/` 找到定义
4. `grep -r "export class ScrollKeyHandler" packages/tui/src/scroll/` 找到定义
</verification>

<success_criteria>
ScrollController 具备完整的 offset 管理、动画过渡、followMode 和 dispose 能力；Scrollable Widget 在 layout 后自动计算 maxScrollExtent；ScrollKeyHandler 正确映射 vim 风格 + 标准键盘 + 鼠标滚轮事件。25+ 测试覆盖核心场景。
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui/06-01-SUMMARY.md`
</output>
