---
phase: 06-tui
plan: 06
type: tdd
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - packages/tui/src/overlay/overlay-entry.ts
  - packages/tui/src/overlay/overlay.ts
  - packages/tui/src/overlay/layer-link.ts
  - packages/tui/src/overlay/autocomplete-controller.ts
  - packages/tui/src/overlay/autocomplete-controller.test.ts
  - packages/tui/src/overlay/command-palette.ts
  - packages/tui/src/overlay/index.ts
autonomous: true
requirements:
  - TUI-13

must_haves:
  truths:
    - "OverlayEntry 通过 builder 函数延迟构建 Widget"
    - "多个 OverlayEntry 按插入顺序堆叠 (后插入在上层)"
    - "OverlayEntry.remove() 正确从 Overlay 移除并触发重绘"
    - "LayerLink 将 Overlay 弹出层锚定到目标 Widget 位置"
    - "AutocompleteController 根据触发器和文本产出选项列表"
    - "AutocompleteController 支持上下选择和确认"
  artifacts:
    - path: "packages/tui/src/overlay/overlay-entry.ts"
      provides: "OverlayEntry — 延迟构建的弹出层条目"
      exports: ["OverlayEntry"]
    - path: "packages/tui/src/overlay/overlay.ts"
      provides: "Overlay StatefulWidget — 管理 OverlayEntry 列表"
      exports: ["Overlay", "OverlayState"]
    - path: "packages/tui/src/overlay/layer-link.ts"
      provides: "LayerLink — 锚点定位"
      exports: ["LayerLink"]
    - path: "packages/tui/src/overlay/autocomplete-controller.ts"
      provides: "AutocompleteController — 自动补全状态管理"
      exports: ["AutocompleteController", "AutocompleteState"]
    - path: "packages/tui/src/overlay/command-palette.ts"
      provides: "CommandPalette Widget — 命令面板"
      exports: ["CommandPalette"]
    - path: "packages/tui/src/overlay/index.ts"
      provides: "overlay 模块统一导出"
  key_links:
    - from: "packages/tui/src/overlay/autocomplete-controller.ts"
      to: "packages/tui/src/editing/text-editing-controller.ts"
      via: "AutocompleteController 监听 TextEditingController 文本变更来触发补全"
      pattern: "TextEditingController"
    - from: "packages/tui/src/overlay/overlay.ts"
      to: "packages/tui/src/overlay/overlay-entry.ts"
      via: "Overlay 管理 OverlayEntry 列表并在 build 时构建所有 entry"
      pattern: "OverlayEntry"
---

<objective>
实现 Overlay/Popup 系统，包括 OverlayEntry、LayerLink 定位、AutocompleteController 和 CommandPalette。

Purpose: 命令面板、自动补全、工具提示等弹出式交互都基于 Overlay 系统。这是 AI CLI 交互的关键 UI 基础设施。
Output: overlay/ 子目录包含完整的弹出层管理系统。
</objective>

<execution_context>
@/Users/bytedance/.oh-my-coco/studio/flitter/.trae/get-shit-done/workflows/execute-plan.md
@/Users/bytedance/.oh-my-coco/studio/flitter/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-tui/06-RESEARCH.md
@.planning/phases/06-tui/06-03-SUMMARY.md
@.planning/phases/06-tui/06-04-SUMMARY.md

<interfaces>
From packages/tui/src/editing/text-editing-controller.ts (Plan 03+04 产出):
```typescript
export class TextEditingController {
  get text(): string;
  get cursorPosition(): number;
  addListener(fn: () => void): void;
  removeListener(fn: () => void): void;
}
```

From packages/tui/src/tree/stateful-widget.ts:
```typescript
export abstract class State<T extends StatefulWidget = StatefulWidget> {
  setState(fn?: () => void): void;
  abstract build(): Widget;
  initState(): void;
  dispose(): void;
}
```

From packages/tui/src/widgets/stack.ts:
```typescript
export class Stack extends Widget { ... }
export class Positioned extends Widget { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OverlayEntry + Overlay Widget + LayerLink</name>
  <files>packages/tui/src/overlay/overlay-entry.ts, packages/tui/src/overlay/overlay.ts, packages/tui/src/overlay/layer-link.ts, packages/tui/src/overlay/index.ts</files>
  <read_first>
    - amp-cli-reversed/framework/micromark-parser.js (行 11427-11485, OverlayEntry lZT 和 LayerLink mZT)
    - packages/tui/src/tree/stateful-widget.ts (StatefulWidget/State)
    - packages/tui/src/widgets/stack.ts (Stack/Positioned)
    - packages/tui/src/tree/element.ts (Element 生命周期)
  </read_first>
  <behavior>
    - OverlayEntry(builder) 创建条目, 初始 _needsBuild=true
    - OverlayEntry.remove() 从 Overlay 移除
    - OverlayEntry.markNeedsBuild() 标记需要重建
    - Overlay 作为 StatefulWidget, 管理 OverlayEntry[] 列表
    - OverlayState.insert(entry) 添加条目到列表尾部 (最上层)
    - OverlayState.insert(entry, below=existingEntry) 添加到指定条目下方
    - 多条目按插入顺序渲染 (使用 Stack)
    - LayerLink.getTargetTransform() 返回 { position, size } 或 null
    - LayerLink 注册 follower, target 位置变化时通知 followers
  </behavior>
  <action>
创建 `packages/tui/src/overlay/overlay-entry.ts`:
```typescript
export class OverlayEntry {
  readonly builder: (context: any) => Widget;
  readonly maintainState: boolean;
  private _overlayState: OverlayState | null = null;
  private _needsBuild: boolean = true;

  constructor(options: { builder: (context: any) => Widget; maintainState?: boolean });

  /** 绑定到 Overlay (内部调用) */
  _bind(overlayState: OverlayState): void;

  /** 从 Overlay 移除 */
  remove(): void;

  /** 标记需要重建 */
  markNeedsBuild(): void;
}
```

创建 `packages/tui/src/overlay/layer-link.ts`:
```typescript
export interface TargetTransform {
  position: { x: number; y: number };
  size: { width: number; height: number };
}

export class LayerLink {
  private _target: { getGlobalPosition(): { x: number; y: number }; getSize(): { width: number; height: number } } | null = null;
  private _followers: Set<() => void> = new Set();

  /** 设置锚点目标 */
  setTarget(target: { getGlobalPosition(): { x: number; y: number }; getSize(): { width: number; height: number } }): void;
  clearTarget(): void;

  /** 获取目标的位置和尺寸 */
  getTargetTransform(): TargetTransform | null;

  /** 注册/取消追随者 */
  addFollower(fn: () => void): void;
  removeFollower(fn: () => void): void;
}
```

创建 `packages/tui/src/overlay/overlay.ts`:
```typescript
export class OverlayState {
  private _entries: OverlayEntry[] = [];
  private _setState: (fn?: () => void) => void;

  constructor(setState: (fn?: () => void) => void);

  insert(entry: OverlayEntry, options?: { below?: OverlayEntry; above?: OverlayEntry }): void;
  remove(entry: OverlayEntry): void;

  /** 构建所有 entry 的 Widget (Stack 层叠) */
  buildEntries(): Widget[];
}

export class Overlay extends StatefulWidget {
  constructor(props: { child: Widget; initialEntries?: OverlayEntry[] });
}
// Overlay State build() 返回 Stack([child, ...entries.map(e => e.builder(context))])
```

创建 `packages/tui/src/overlay/index.ts`:
```typescript
export * from "./overlay-entry.js";
export * from "./overlay.js";
export * from "./layer-link.js";
export * from "./autocomplete-controller.js";
export * from "./command-palette.js";
```

更新 `packages/tui/src/index.ts` 添加:
```typescript
export * from "./overlay/index.js";
```

注意: 此 task 无独立测试文件, 核心测试在 Task 2 的 AutocompleteController 测试中覆盖 Overlay 集成。
  </action>
  <verify>
    <automated>cd /Users/bytedance/.oh-my-coco/studio/flitter && grep -c "export class" packages/tui/src/overlay/overlay-entry.ts packages/tui/src/overlay/overlay.ts packages/tui/src/overlay/layer-link.ts</automated>
  </verify>
  <acceptance_criteria>
    - overlay-entry.ts 包含 `export class OverlayEntry`
    - overlay-entry.ts 包含 `remove()` 和 `markNeedsBuild()` 方法
    - overlay.ts 包含 `export class OverlayState` 和 `export class Overlay`
    - overlay.ts 包含 `insert(` 和 `remove(` 方法
    - layer-link.ts 包含 `export class LayerLink`
    - layer-link.ts 包含 `export interface TargetTransform`
    - layer-link.ts 包含 `getTargetTransform()` 方法
    - index.ts 包含 `export * from "./overlay-entry`
    - packages/tui/src/index.ts 包含 `export * from "./overlay/index`
  </acceptance_criteria>
  <done>OverlayEntry/Overlay/LayerLink 核心弹出层基础设施实现完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AutocompleteController + CommandPalette Widget</name>
  <files>packages/tui/src/overlay/autocomplete-controller.ts, packages/tui/src/overlay/autocomplete-controller.test.ts, packages/tui/src/overlay/command-palette.ts</files>
  <read_first>
    - packages/tui/src/overlay/overlay-entry.ts (Task 1 产出)
    - amp-cli-reversed/framework/micromark-parser.js (行 11486-11600, AutocompleteController uZT)
    - amp-cli-reversed/framework/tui-thread-widgets.js (行 2748, CommandPalette 模式)
    - packages/tui/src/editing/text-editing-controller.ts (TextEditingController API)
  </read_first>
  <behavior>
    - 初始 isActive=false, options=[], selectedIndex=0
    - initialize({ textController, triggers: [{ char:'/', minLength:1 }], optionsBuilder })
    - 输入 "/" 触发 → isActive=true, query=""
    - 继续输入 "/he" → optionsBuilder("he") 返回 options, selectedIndex=0
    - selectNext() → selectedIndex++; selectPrevious() → selectedIndex--
    - acceptSelected() → 调用 onSelected(options[selectedIndex]), isActive=false
    - dismiss() → isActive=false, 清空 options
    - Escape 键 → dismiss()
    - debounce: 快速输入不会为每个字符都调用 optionsBuilder
    - generationId: 异步 optionsBuilder 结果过期时丢弃
  </behavior>
  <action>
创建 `packages/tui/src/overlay/autocomplete-controller.ts`:

```typescript
export interface AutocompleteTrigger {
  char: string;           // 触发字符 (如 "/", "@")
  minLength?: number;     // 最小查询长度才开始搜索, 默认 0
}

export interface AutocompleteOption {
  label: string;          // 显示文本
  value: string;          // 选中后的值
  description?: string;   // 描述文字
}

export interface AutocompleteState {
  trigger: { query: string; start: number } | null;
  options: AutocompleteOption[];
  selectedIndex: number;
  isActive: boolean;
  generationId: number;
}

export class AutocompleteController {
  private _state: AutocompleteState;
  private _textController: TextEditingController | null = null;
  private _triggers: AutocompleteTrigger[] = [];
  private _optionsBuilder: ((query: string) => AutocompleteOption[] | Promise<AutocompleteOption[]>) | null = null;
  private _onSelected: ((option: AutocompleteOption) => void) | null = null;
  private _listeners: Set<(state: AutocompleteState) => void> = new Set();
  private _debounceTimer: ReturnType<typeof setTimeout> | null = null;
  private _debounceMs: number = 100;
  private _generationId: number = 0;
  private _disposed: boolean = false;

  get state(): Readonly<AutocompleteState>;
  get isActive(): boolean;

  initialize(options: {
    textController: TextEditingController;
    triggers: AutocompleteTrigger[];
    optionsBuilder: (query: string) => AutocompleteOption[] | Promise<AutocompleteOption[]>;
    onSelected: (option: AutocompleteOption) => void;
    debounceMs?: number;
  }): void;

  /** 由 TextEditingController listener 调用 */
  _onTextChanged(): void;
  // 1. 检查光标前是否有触发字符
  // 2. 提取 query = 触发字符到光标之间的文本
  // 3. debounce 后调用 optionsBuilder(query)
  // 4. 更新 state, 通知 listeners

  selectNext(): void;      // selectedIndex = (selectedIndex + 1) % options.length
  selectPrevious(): void;  // selectedIndex = (selectedIndex - 1 + length) % length
  acceptSelected(): void;  // 调用 onSelected, dismiss
  dismiss(): void;         // isActive=false, 清空

  addListener(fn: (state: AutocompleteState) => void): void;
  removeListener(fn: (state: AutocompleteState) => void): void;

  dispose(): void;
}
```

创建 `packages/tui/src/overlay/command-palette.ts`:

```typescript
export interface CommandPaletteCommand {
  id: string;
  label: string;
  description?: string;
  shortcut?: string;
  action: () => void;
}

export class CommandPalette extends StatefulWidget {
  constructor(props: {
    commands: CommandPaletteCommand[];
    onDismiss: () => void;
  });
}
// State 实现:
// - 内部创建 TextEditingController 用于搜索输入
// - 内部创建 AutocompleteController, triggers: [{ char: '', minLength: 0 }] (总是激活)
// - optionsBuilder: 按 label 模糊匹配 commands
// - 渲染: Column([ TextField(searchInput), ListView(matchedCommands) ])
```

测试 15+ 用例:
- 初始状态 isActive=false
- 触发字符 "/" → isActive=true
- 查询更新 → optionsBuilder 被调用
- selectNext / selectPrevious 循环选择
- acceptSelected → onSelected 回调
- dismiss → isActive=false
- debounce: 快速输入只调用一次 optionsBuilder
- generationId: 旧异步结果不覆盖新结果
- dispose 清理
  </action>
  <verify>
    <automated>cd /Users/bytedance/.oh-my-coco/studio/flitter && npx pnpm@10 exec tsx node_modules/.bin/vitest run packages/tui/src/overlay/autocomplete-controller.test.ts 2>/dev/null || npx tsx --test packages/tui/src/overlay/autocomplete-controller.test.ts 2>/dev/null || bun test packages/tui/src/overlay/autocomplete-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - autocomplete-controller.ts 包含 `export class AutocompleteController`
    - autocomplete-controller.ts 包含 `export interface AutocompleteState`
    - autocomplete-controller.ts 包含 `export interface AutocompleteTrigger`
    - autocomplete-controller.ts 包含 `selectNext(` 和 `selectPrevious(` 和 `acceptSelected(`
    - autocomplete-controller.ts 包含 `_debounceTimer` 和 `_generationId`
    - command-palette.ts 包含 `export class CommandPalette`
    - command-palette.ts 包含 `export interface CommandPaletteCommand`
    - autocomplete-controller.test.ts 包含至少 15 个 test/it 块
    - 所有测试通过 (exit code 0)
  </acceptance_criteria>
  <done>AutocompleteController 实现触发器检测、debounce、generationId 竞态保护、上下选择；CommandPalette Widget 封装完成。15+ 测试通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optionsBuilder 回调 → AutocompleteState | 用户提供的回调可能返回恶意或超大选项列表 |
| Overlay builder → Widget 树 | builder 函数创建的 Widget 加入渲染管线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | D (DoS) | AutocompleteController optionsBuilder | mitigate | debounce 限流 (默认 100ms); generationId 丢弃过期异步结果; options 列表不限制大小但渲染在 ListView 中虚拟化 |
| T-06-13 | T (Tampering) | OverlayEntry.builder | accept | builder 函数由应用内部提供，非外部输入，风险低 |
</threat_model>

<verification>
1. `bun test packages/tui/src/overlay/` 通过
2. `grep "export class AutocompleteController" packages/tui/src/overlay/autocomplete-controller.ts`
3. `grep "export class OverlayEntry" packages/tui/src/overlay/overlay-entry.ts`
4. `grep "export class CommandPalette" packages/tui/src/overlay/command-palette.ts`
</verification>

<success_criteria>
OverlayEntry/Overlay/LayerLink 提供弹出层基础设施；AutocompleteController 实现触发器检测、debounce 和竞态保护；CommandPalette 封装搜索+列表交互。15+ 测试覆盖核心流程。
</success_criteria>

<output>
After completion, create `.planning/phases/06-tui/06-06-SUMMARY.md`
</output>
