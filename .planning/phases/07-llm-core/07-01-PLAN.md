---
phase: 07-llm-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/llm/src/types.ts
  - packages/llm/src/provider.ts
  - packages/llm/src/transformers/message-transformer.ts
  - packages/llm/src/transformers/tool-transformer.ts
  - packages/llm/src/types.test.ts
autonomous: true
requirements: [LLM-05]
user_setup: []

must_haves:
  truths:
    - "LLMProvider 接口定义 stream() 方法返回 AsyncGenerator<StreamDelta>"
    - "StreamParams 包含 model/messages/systemPrompt/tools/config/signal/reasoningEffort"
    - "StreamDelta 复用 @flitter/schemas 的 AssistantContentBlock + MessageState + Usage"
    - "MessageTransformer 接口定义 toProviderMessages() 和 fromProviderDelta() 双向转换"
    - "ToolTransformer 接口定义 toProviderTools() 单向转换"
    - "ToolDefinition 包含 name/description/inputSchema (JSON Schema 格式)"
    - "ModelInfo 注册表包含 contextWindow/maxOutputTokens/supportsThinking/supportsTools 元数据"
  artifacts:
    - "packages/llm/src/types.ts"
    - "packages/llm/src/provider.ts"
    - "packages/llm/src/transformers/message-transformer.ts"
    - "packages/llm/src/transformers/tool-transformer.ts"
    - "packages/llm/src/types.test.ts"
  key_links:
    - "LLMProvider 是所有具体 Provider (Anthropic/OpenAI/Gemini/xAI) 的抽象接口"
    - "StreamParams/StreamDelta 是跨 Provider 的统一输入/输出格式"
    - "MessageTransformer/ToolTransformer 由每个 Provider 各自实现"
    - "ModelInfo 为 Provider 选择合适的参数 (token 限制, 功能开关)"
---

<objective>
定义 LLM Provider 核心抽象层: Provider 接口、流式输入输出类型、消息/工具转换器接口、模型注册表。

Purpose: 为所有 Provider 实现提供统一的类型基础和接口契约
Output: `packages/llm/src/` 下 4 个源文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-llm-core/CONTEXT.md
@packages/llm/package.json
@packages/schemas/src/messages.ts
@packages/schemas/src/config.ts
</context>

<interfaces>
<!-- 从逆向代码 llm-sdk-providers.js 提取，保持 Amp 抽象一致 -->

## LLMProvider 接口

```typescript
/**
 * LLM Provider 统一接口
 *
 * 所有 Provider (Anthropic/OpenAI/Gemini/xAI) 必须实现此接口。
 * stream() 返回 AsyncGenerator，逐块 yield StreamDelta。
 */
interface LLMProvider {
  /** Provider 标识名 ("anthropic" | "openai" | "gemini" | "xai") */
  readonly name: ProviderName;

  /**
   * 发起流式对话请求
   *
   * @param params - 统一参数
   * @yields StreamDelta - 增量内容块 + 状态 + usage
   * @throws ProviderError - 不可重试的 API 错误
   */
  stream(params: StreamParams): AsyncGenerator<StreamDelta>;
}

type ProviderName = "anthropic" | "openai" | "gemini" | "xai";
```

## StreamParams (统一输入)

```typescript
interface StreamParams {
  /** 模型标识 (如 "claude-sonnet-4-20250514", "gpt-4o") */
  model: string;
  /** 对话消息历史 (Flitter 统一格式) */
  messages: Message[];
  /** 系统提示词 (支持多段，带 cache_control) */
  systemPrompt: TextBlock[];
  /** 可用工具列表 */
  tools: ToolDefinition[];
  /** 运行时配置 (含 Settings + SecretStore) */
  config: Config;
  /** 取消信号 */
  signal: AbortSignal;
  /** 推理深度控制 */
  reasoningEffort?: ReasoningEffort;
}

type ReasoningEffort = "none" | "minimal" | "low" | "medium" | "high" | "xhigh";
```

## StreamDelta (统一输出)

```typescript
interface StreamDelta {
  /** 增量内容块 (累积模式 — 每次 yield 包含完整的当前 content 数组) */
  content: AssistantContentBlock[];
  /** 流状态 */
  state: MessageState;
  /** token 用量 (仅在 complete 时填充) */
  usage?: Usage;
}
```

## ToolDefinition

```typescript
interface ToolDefinition {
  /** 工具名 */
  name: string;
  /** 工具描述 */
  description: string;
  /** 输入参数 JSON Schema */
  inputSchema: Record<string, unknown>;
}
```

## ModelInfo 注册表

```typescript
interface ModelInfo {
  /** 模型 ID */
  id: string;
  /** 所属 Provider */
  provider: ProviderName;
  /** 上下文窗口大小 (tokens) */
  contextWindow: number;
  /** 最大输出 tokens */
  maxOutputTokens: number;
  /** 是否支持 thinking */
  supportsThinking: boolean;
  /** 是否支持工具调用 */
  supportsTools: boolean;
  /** 是否支持图片输入 */
  supportsImages: boolean;
  /** 是否支持缓存控制 */
  supportsCacheControl: boolean;
}

/** 已知模型注册表 */
const MODEL_REGISTRY: Record<string, ModelInfo>;
```

## MessageTransformer 接口

```typescript
interface MessageTransformer<TNativeMessage, TNativeDelta> {
  /** Flitter 消息 → Provider 原生消息格式 */
  toProviderMessages(messages: Message[], systemPrompt: TextBlock[]): TNativeMessage[];
  /** Provider 流事件 → Flitter StreamDelta */
  fromProviderDelta(chunk: TNativeDelta, state: TransformState): StreamDelta;
}
```

## ToolTransformer 接口

```typescript
interface ToolTransformer<TNativeTool> {
  /** Flitter ToolDefinition → Provider 原生工具格式 */
  toProviderTools(tools: ToolDefinition[]): TNativeTool[];
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 定义核心类型</name>
  <files>packages/llm/src/types.ts</files>
  <read_first>
    - packages/schemas/src/messages.ts
    - packages/schemas/src/config.ts
    - packages/llm/package.json
  </read_first>
  <action>
创建 `packages/llm/src/types.ts`:

1. 导入 `@flitter/schemas` 中的 Message, AssistantContentBlock, MessageState, Usage, Config, TextBlock 等类型
2. 定义 `ProviderName` 字面量联合类型
3. 定义 `ReasoningEffort` 字面量联合类型
4. 定义 `StreamParams` 接口
5. 定义 `StreamDelta` 接口
6. 定义 `ToolDefinition` 接口
7. 定义 `ModelInfo` 接口
8. 定义 `ProviderError` 自定义错误类，包含:
   - `status: number` (HTTP 状态码)
   - `provider: ProviderName`
   - `retryable: boolean`
   - `retryAfterMs?: number`
9. 定义 `TransformState` — 转换器内部状态 (追踪 block index、时间戳等)
10. 实现 `MODEL_REGISTRY` 常量，包含已知模型元数据:
    - Anthropic: claude-sonnet-4-20250514, claude-opus-4-20250515, claude-haiku-3.5
    - OpenAI: gpt-4o, gpt-4o-mini, o3, o3-mini, o4-mini, codex-mini
    - Gemini: gemini-2.5-pro, gemini-2.5-flash, gemini-2.0-flash
    - xAI: grok-3, grok-3-mini
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 所有类型正确引用 @flitter/schemas
    - MODEL_REGISTRY 包含 4 个 Provider 的主要模型
    - ProviderError 扩展自 Error 基类
    - tsc 通过
  </acceptance_criteria>
  <done>核心类型定义完成</done>
</task>

<task type="auto">
  <name>Task 2: 定义 LLMProvider 接口</name>
  <files>packages/llm/src/provider.ts</files>
  <read_first>
    - packages/llm/src/types.ts
  </read_first>
  <action>
创建 `packages/llm/src/provider.ts`:

1. 导入 StreamParams, StreamDelta, ProviderName 类型
2. 定义 `LLMProvider` 接口:
   - `readonly name: ProviderName`
   - `stream(params: StreamParams): AsyncGenerator<StreamDelta>`
3. 定义 `MessageTransformer<TNativeMessage, TNativeDelta>` 泛型接口:
   - `toProviderMessages(messages: Message[], systemPrompt: TextBlock[]): TNativeMessage[]`
   - `fromProviderDelta(chunk: TNativeDelta, state: TransformState): StreamDelta`
4. 定义 `ToolTransformer<TNativeTool>` 泛型接口:
   - `toProviderTools(tools: ToolDefinition[]): TNativeTool[]`
5. 定义 `createProvider(name: ProviderName, config: Config): LLMProvider` 工厂函数签名
6. 定义 `resolveModel(model: string): ModelInfo | undefined` 模型查找函数
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - LLMProvider 接口使用 AsyncGenerator 返回类型
    - Transformer 接口使用泛型参数隔离 Provider 原生类型
    - tsc 通过
  </acceptance_criteria>
  <done>Provider 接口定义完成</done>
</task>

<task type="auto">
  <name>Task 3: 实现消息转换器基类</name>
  <files>packages/llm/src/transformers/message-transformer.ts</files>
  <read_first>
    - packages/llm/src/types.ts
    - packages/llm/src/provider.ts
    - packages/schemas/src/messages.ts
  </read_first>
  <action>
创建 `packages/llm/src/transformers/message-transformer.ts`:

1. 实现 `BaseMessageTransformer` 抽象类:
   - 提供公共的辅助方法:
     - `filterToolResults(messages)` — 过滤掉 incomplete tool_use 块
     - `extractTextContent(block)` — 从 content block 提取纯文本
     - `buildCacheControl(cacheControl?)` — 构建缓存控制头
     - `createEmptyDelta()` — 创建空 StreamDelta (streaming 状态)
     - `createCompleteDelta(content, usage, stopReason)` — 创建完成 Delta
     - `createErrorDelta(error)` — 创建错误 Delta
   - `abstract toProviderMessages(...)` — 留给具体 Transformer 实现
   - `abstract fromProviderDelta(...)` — 留给具体 Transformer 实现

2. 实现 `TransformState` 类:
   - `blocks: Map<number, BlockState>` — 按 index 追踪每个 content block 的状态
   - `content: AssistantContentBlock[]` — 当前累积的 content 数组
   - `addBlock(index, block)` — 添加新 block
   - `updateBlock(index, delta)` — 更新 block 内容 (追加文本/JSON)
   - `completeBlock(index)` — 标记 block 完成
   - `getContent()` — 返回当前 content 快照

3. `BlockState` 结构:
   - `type: "text" | "tool_use" | "thinking" | "redacted_thinking" | "server_tool_use"`
   - `startTime: number`
   - `finalTime?: number`
   - `data: Partial<AssistantContentBlock>` — 累积数据
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - BaseMessageTransformer 提供足够的公共辅助方法
    - TransformState 正确追踪 block 级状态
    - 抽象方法留给具体实现
    - tsc 通过
  </acceptance_criteria>
  <done>消息转换器基类实现完成</done>
</task>

<task type="auto">
  <name>Task 4: 实现工具转换器基类</name>
  <files>packages/llm/src/transformers/tool-transformer.ts</files>
  <read_first>
    - packages/llm/src/types.ts
    - packages/llm/src/provider.ts
  </read_first>
  <action>
创建 `packages/llm/src/transformers/tool-transformer.ts`:

1. 实现 `BaseToolTransformer` 抽象类:
   - `validateToolDefinition(tool)` — 验证 ToolDefinition 必需字段
   - `normalizeInputSchema(schema)` — 规范化 JSON Schema (确保有 type: "object")
   - `abstract toProviderTools(tools)` — 留给具体实现

2. 实现 `zodToJsonSchema(schema: ZodType): Record<string, unknown>` 辅助函数:
   - 将 Zod schema 转换为 JSON Schema 格式
   - 用于工具定义中的 inputSchema 转换
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 工具验证和规范化逻辑正确
    - tsc 通过
  </acceptance_criteria>
  <done>工具转换器基类实现完成</done>
</task>

<task type="auto">
  <name>Task 5: 编写类型和转换器基类测试</name>
  <files>packages/llm/src/types.test.ts</files>
  <read_first>
    - packages/llm/src/types.ts
    - packages/llm/src/provider.ts
    - packages/llm/src/transformers/message-transformer.ts
    - packages/llm/src/transformers/tool-transformer.ts
  </read_first>
  <action>
创建 `packages/llm/src/types.test.ts`:

**类型测试组:**
1. MODEL_REGISTRY 包含 Anthropic/OpenAI/Gemini/xAI 模型
2. resolveModel 返回已知模型的 ModelInfo
3. resolveModel 对未知模型返回 undefined
4. ProviderError 包含 status/provider/retryable 字段
5. ProviderError.retryable 对 429/500+ 为 true

**TransformState 测试组:**
6. addBlock 添加新 block
7. updateBlock 追加文本到 text block
8. updateBlock 追加 JSON 到 tool_use block
9. completeBlock 设置 finalTime
10. getContent 返回正确的 AssistantContentBlock 数组
11. 多 block 混合 (text + tool_use + thinking) 正确追踪

**BaseToolTransformer 测试组:**
12. validateToolDefinition 接受合法工具
13. validateToolDefinition 拒绝缺少 name 的工具
14. normalizeInputSchema 添加缺失的 type: "object"

**BaseMessageTransformer 辅助方法测试组:**
15. createEmptyDelta 返回 streaming 状态
16. createCompleteDelta 返回 complete 状态 + usage
17. createErrorDelta 返回 error 状态 + 错误消息
18. filterToolResults 过滤 incomplete tool_use
19. buildCacheControl 正确构建缓存控制对象

使用 `node:test` 的 `describe/it` + `node:assert/strict`。
  </action>
  <verify>
    <automated>npx tsx --test packages/llm/src/types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 19 个测试用例
    - 覆盖 MODEL_REGISTRY、ProviderError、TransformState、BaseToolTransformer、BaseMessageTransformer
    - 所有测试通过
  </acceptance_criteria>
  <done>类型和转换器基类测试全部通过</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit && npx tsx --test packages/llm/src/types.test.ts
```
</verification>

<success_criteria>
- `tsc --noEmit` 零错误
- 所有测试通过 (>= 19 个)
- LLMProvider 接口使用 AsyncGenerator<StreamDelta> 返回类型
- StreamParams/StreamDelta 复用 @flitter/schemas 类型
- MODEL_REGISTRY 覆盖 4 个 Provider 的主要模型
- TransformState 正确管理 block 级状态
- 零 `any` 类型
</success_criteria>

<output>
After completion, create `.planning/phases/07-llm-core/07-01-SUMMARY.md`
</output>
