# Phase 9: Streaming Experience — Plan 01

**Wave:** 1 of 1
**Scope:** STREAM-01 through STREAM-04

## Tasks

### Task 1: Streaming cursor indicator (STREAM-01)

**File:** `packages/flitter-amp/src/widgets/chat-view.ts`

In `buildAssistantMessage`, use the `_isStreaming` parameter:
- When streaming and text exists: append `▌` cursor to the markdown text
- When streaming and no text: show blinking cursor placeholder
- When not streaming: render text normally (no cursor)

### Task 2: setState throttle (STREAM-02)

**File:** `packages/flitter-amp/src/app.ts`

Add 50ms throttle to the stateListener to batch rapid streaming chunks:
- Track `lastUpdate` timestamp
- If less than 50ms since last setState, schedule via setTimeout
- Ensure pending update fires when streaming stops

### Task 3: Non-text content placeholder (STREAM-03)

**File:** `packages/flitter-amp/src/state/app-state.ts`

In the `agent_message_chunk` case, handle non-text content types:
- If `content.type !== 'text'`: log the type and append placeholder `[unsupported content type: {type}]`

### Task 4: ThinkingBlock display limit (STREAM-04)

**File:** `packages/flitter-amp/src/widgets/thinking-block.ts`

Change the 500 character truncation limit to 10000.

---

*Plan: 09-PLAN-01*
*Created: 2026-03-28*
