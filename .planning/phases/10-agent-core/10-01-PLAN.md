---
phase: 10-agent-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/agent-core/src/tools/types.ts
  - packages/agent-core/src/tools/registry.ts
  - packages/agent-core/src/tools/registry.test.ts
autonomous: true
requirements: [AGNT-02]
user_setup: []

must_haves:
  truths:
    - "ToolSpec 接口定义 name/description/inputSchema/execute/source 必需字段"
    - "ToolSpec.execute(args, context) 返回 Promise<ToolResult> | Observable<ToolResult>"
    - "ToolSpec.executionProfile 可选字段包含 serial 和 resourceKeys"
    - "ToolSpec.isReadOnly 可选布尔值用于权限系统提示"
    - "ToolSpec.isEnabled 可选函数接受 Settings 返回布尔值"
    - "ToolSpec.source 类型为 ToolSource ('builtin' | { mcp: string } | { toolbox: string })"
    - "ExecutionProfile 接口定义 serial 和 resourceKeys 字段"
    - "ResourceKey 接口定义 key (string) 和 mode ('read' | 'write') 字段"
    - "ToolResult 接口包含 status (ToolRunInternalStatus)、content、error、data、outputFiles"
    - "ToolContext 接口包含 workingDirectory/signal/threadId/config/toolMessages/userInput"
    - "ToolRegistry.register(spec) 对重复 name 抛出错误"
    - "ToolRegistry.unregister(name) 返回 boolean 表示是否成功移除"
    - "ToolRegistry.get(name) 返回 ToolSpec | undefined"
    - "ToolRegistry.has(name) 返回 boolean"
    - "ToolRegistry.list() 返回所有已注册的 ToolSpec[]"
    - "ToolRegistry.listEnabled(config) 过滤 isEnabled 和 config.tools.disable/enable"
    - "ToolRegistry.getToolDefinitions(config) 返回 ToolDefinition[] (name + description + inputSchema)"
    - "ToolRegistry.normalizeToolName(name) 剥离 mcp__ 前缀用于查找"
    - "ToolRegistry 内部使用 Map<string, ToolSpec> 存储工具"
  artifacts:
    - "packages/agent-core/src/tools/types.ts"
    - "packages/agent-core/src/tools/registry.ts"
    - "packages/agent-core/src/tools/registry.test.ts"
  key_links:
    - "ToolSource, ToolRunInternalStatus, Settings 从 @flitter/schemas 导入"
    - "ToolDefinition 从 @flitter/llm 导入"
    - "Subject, Observable 从 @flitter/util 导入"
    - "Config 从 @flitter/schemas 导入"
    - "ToolRegistry 被 ToolOrchestrator (Plan 10-02) 使用"
    - "ToolSpec 被所有内置工具 (Plans 10-03..10-05) 实现"
    - "ExecutionProfile/ResourceKey 被 ToolOrchestrator 的冲突检测使用"
---

<objective>
定义工具系统的基础类型与注册表: ToolSpec 接口、执行配置、工具结果、工具上下文、ToolRegistry 类。

Purpose: 为工具执行引擎和所有内置/外部工具提供统一的类型契约和注册机制
Output: `packages/agent-core/src/tools/` 下 2 个源文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-core/CONTEXT.md
@packages/agent-core/package.json
@packages/schemas/src/permissions.ts
@packages/schemas/src/config.ts
@packages/llm/src/types.ts
@packages/util/src/reactive/subject.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `FWT` (部分) | tool-execution-engine.js | 857-870 | ToolRegistry (工具注册/查找部分) |
| `MwR` (参数) | tool-execution-engine.js | 820-837 | ExecutionProfile, ResourceKey 类型定义 |
| `yy` | tool-permissions.js | 342-382 | isEnabled + config-based enable/disable 逻辑 |
| `Xf` | tool-permissions.js | 323-340 | normalizeToolName (mcp__ 前缀处理) |

## 实现细节

### 1. types.ts — 工具系统核心类型

```ts
import type { ToolRunInternalStatus, ToolSource, Settings, Config } from "@flitter/schemas";
import type { ToolDefinition } from "@flitter/llm";
import type { Observable, Subject } from "@flitter/util";

// ─── 执行配置 ────────────────────────────────────────────

/**
 * 资源键: 标识工具访问的资源及其模式
 * 用于 ToolOrchestrator 的并行/串行冲突检测
 */
export interface ResourceKey {
  /** 资源标识 (如文件路径、"bash"、"network") */
  key: string;
  /** 访问模式 */
  mode: "read" | "write";
}

/**
 * 执行配置: 控制工具的并行/串行行为
 * 逆向: MwR 中的冲突检测参数
 */
export interface ExecutionProfile {
  /** 如果 true, 工具必须独占执行 (不与其他工具并行) */
  serial?: boolean;
  /** 工具访问的资源列表 */
  resourceKeys?: ResourceKey[];
}

// ─── 工具结果 ────────────────────────────────────────────

/**
 * 工具执行结果
 * status 映射到 @flitter/schemas 的 ToolRunInternalStatus
 */
export interface ToolResult {
  /** 执行状态 */
  status: ToolRunInternalStatus;
  /** 文本输出 (供 LLM 阅读) */
  content?: string;
  /** 错误消息 */
  error?: string;
  /** 结构化数据 */
  data?: Record<string, unknown>;
  /** 工具写入的文件路径列表 */
  outputFiles?: string[];
}

// ─── 工具上下文 ──────────────────────────────────────────

/**
 * 工具执行上下文: 每次工具调用时注入
 */
export interface ToolContext {
  /** 当前工作目录 */
  workingDirectory: string;
  /** 取消信号 */
  signal: AbortSignal;
  /** 所属线程 ID */
  threadId: string;
  /** 运行时配置 */
  config: Config;
  /** 流式消息输出管道 (工具可写入中间消息) */
  toolMessages?: Subject<string>;
  /** 用户输入 (blocked-on-user 恢复时传入) */
  userInput?: string;
}

// ─── 工具定义 ────────────────────────────────────────────

/**
 * ToolSpec: 完整的工具定义接口
 * 所有内置工具和外部工具 (MCP/Toolbox) 必须实现此接口
 */
export interface ToolSpec {
  /** 唯一工具名 */
  name: string;
  /** 工具描述 (用于 LLM prompt) */
  description: string;
  /** 输入参数 JSON Schema */
  inputSchema: Record<string, unknown>;
  /** 工具执行函数 */
  execute(
    args: Record<string, unknown>,
    context: ToolContext
  ): Promise<ToolResult> | Observable<ToolResult>;
  /** 并行/串行执行配置 */
  executionProfile?: ExecutionProfile;
  /** 是否为只读操作 (权限系统提示) */
  isReadOnly?: boolean;
  /** 动态启用检查 (返回 false 时工具不出现在 LLM prompt) */
  isEnabled?: (config: Settings) => boolean;
  /** 工具来源: "builtin" | { mcp: string } | { toolbox: string } */
  source: ToolSource;
}
```

### 2. registry.ts — ToolRegistry 工具注册表

```ts
import type { ToolSpec, ToolResult, ToolContext, ExecutionProfile, ResourceKey } from "./types";
import type { Settings } from "@flitter/schemas";
import type { ToolDefinition } from "@flitter/llm";

/**
 * ToolRegistry: 管理所有已注册工具
 * 逆向: FWT 中的工具查找部分 + yy 的 enable/disable 逻辑
 */
export class ToolRegistry {
  /** 内部工具存储 */
  private tools: Map<string, ToolSpec> = new Map();

  /**
   * 注册工具
   * @throws Error 如果同名工具已注册
   */
  register(spec: ToolSpec): void {
    if (this.tools.has(spec.name)) {
      throw new Error(`Tool "${spec.name}" is already registered`);
    }
    this.tools.set(spec.name, spec);
  }

  /**
   * 移除工具
   * @returns true 如果成功移除, false 如果不存在
   */
  unregister(name: string): boolean {
    return this.tools.delete(name);
  }

  /** 按名称获取工具 */
  get(name: string): ToolSpec | undefined {
    return this.tools.get(name);
  }

  /** 检查工具是否已注册 */
  has(name: string): boolean {
    return this.tools.has(name);
  }

  /** 返回所有已注册工具 */
  list(): ToolSpec[] {
    return Array.from(this.tools.values());
  }

  /**
   * 返回当前启用的工具列表
   * 过滤逻辑 (逆向 yy):
   * 1. 如果 spec.isEnabled 存在且返回 false → 排除
   * 2. 如果 config.tools?.disable 包含匹配名称 → 排除
   * 3. 如果 config.tools?.enable 存在且不包含匹配名称 → 排除
   */
  listEnabled(config: Settings): ToolSpec[] {
    return this.list().filter((spec) => {
      // 动态启用检查
      if (spec.isEnabled && !spec.isEnabled(config)) {
        return false;
      }
      // config 禁用列表
      const disabled = (config as any).tools?.disable as string[] | undefined;
      if (disabled?.includes(spec.name)) {
        return false;
      }
      // config 启用白名单 (如果存在, 只允许白名单中的工具)
      const enabled = (config as any).tools?.enable as string[] | undefined;
      if (enabled && !enabled.includes(spec.name)) {
        return false;
      }
      return true;
    });
  }

  /**
   * 生成 LLM 工具定义列表
   * 仅包含 name/description/inputSchema
   */
  getToolDefinitions(config: Settings): ToolDefinition[] {
    return this.listEnabled(config).map((spec) => ({
      name: spec.name,
      description: spec.description,
      inputSchema: spec.inputSchema,
    }));
  }

  /**
   * 规范化工具名: 剥离 mcp__ 前缀用于查找
   * "mcp__server__tool" → 尝试按原名查找, 失败则按剥离后名称查找
   * 逆向: Xf
   */
  normalizeToolName(name: string): string {
    if (name.startsWith("mcp__")) {
      const parts = name.split("__");
      if (parts.length >= 3) {
        return parts.slice(2).join("__");
      }
    }
    return name;
  }
}
```

### 3. 测试策略

**registry.test.ts** (~20 tests):

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

```
describe("ToolRegistry")
├── describe("register")
│   ├── it("注册工具成功并可通过 get 获取")
│   ├── it("注册工具后 has 返回 true")
│   ├── it("重复注册同名工具抛出 Error")
│   └── it("不同名工具可同时注册")
├── describe("unregister")
│   ├── it("移除已注册工具返回 true")
│   ├── it("移除未注册工具返回 false")
│   └── it("移除后 get 返回 undefined, has 返回 false")
├── describe("get / has")
│   ├── it("未注册工具 get 返回 undefined")
│   └── it("未注册工具 has 返回 false")
├── describe("list")
│   ├── it("空 registry 返回空数组")
│   └── it("返回所有已注册工具")
├── describe("listEnabled")
│   ├── it("无过滤条件时返回所有工具")
│   ├── it("isEnabled 返回 false 时排除该工具")
│   ├── it("config.tools.disable 中的工具被排除")
│   ├── it("config.tools.enable 白名单外的工具被排除")
│   └── it("isEnabled + config 过滤组合正确工作")
├── describe("getToolDefinitions")
│   ├── it("返回 ToolDefinition[] 仅含 name/description/inputSchema")
│   ├── it("过滤逻辑与 listEnabled 一致")
│   └── it("空 registry 返回空数组")
└── describe("normalizeToolName")
    ├── it("普通名称原样返回")
    ├── it("mcp__server__tool 格式返回 tool 部分")
    └── it("mcp__server__multi__part 返回 multi__part")
```

**测试辅助: createMockToolSpec**

```ts
function createMockToolSpec(overrides?: Partial<ToolSpec>): ToolSpec {
  return {
    name: "mock-tool",
    description: "A mock tool for testing",
    inputSchema: { type: "object", properties: {} },
    execute: async () => ({ status: "done" as const }),
    source: "builtin" as const,
    ...overrides,
  };
}
```

预期测试数: ~20

<tasks>

<task type="auto">
  <name>Task 1: 定义工具系统核心类型</name>
  <files>packages/agent-core/src/tools/types.ts</files>
  <read_first>
    - packages/schemas/src/permissions.ts
    - packages/schemas/src/config.ts
    - packages/llm/src/types.ts
    - packages/util/src/reactive/subject.ts
    - packages/agent-core/package.json
  </read_first>
  <action>
创建 `packages/agent-core/src/tools/types.ts`:

1. 导入 `ToolRunInternalStatus`, `ToolSource`, `Settings`, `Config` 从 `@flitter/schemas`
2. 导入 `ToolDefinition` 从 `@flitter/llm`
3. 导入 `Observable`, `Subject` 从 `@flitter/util`
4. 定义 `ResourceKey` 接口: `key: string`, `mode: "read" | "write"`
5. 定义 `ExecutionProfile` 接口: `serial?: boolean`, `resourceKeys?: ResourceKey[]`
6. 定义 `ToolResult` 接口: `status: ToolRunInternalStatus`, `content?: string`, `error?: string`, `data?: Record<string, unknown>`, `outputFiles?: string[]`
7. 定义 `ToolContext` 接口: `workingDirectory: string`, `signal: AbortSignal`, `threadId: string`, `config: Config`, `toolMessages?: Subject<string>`, `userInput?: string`
8. 定义 `ToolSpec` 接口: `name`, `description`, `inputSchema`, `execute(args, context)`, `executionProfile?`, `isReadOnly?`, `isEnabled?`, `source`
9. 从此文件重新导出 `ToolDefinition` 以便下游直接从 tools/types 引用
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 所有类型正确引用 @flitter/schemas、@flitter/llm、@flitter/util
    - ToolSpec.execute 返回联合类型 Promise<ToolResult> | Observable<ToolResult>
    - ToolSpec.source 使用 @flitter/schemas 的 ToolSource 类型
    - tsc 通过
  </acceptance_criteria>
  <done>工具系统核心类型定义完成</done>
</task>

<task type="auto">
  <name>Task 2: 实现 ToolRegistry</name>
  <files>packages/agent-core/src/tools/registry.ts</files>
  <read_first>
    - packages/agent-core/src/tools/types.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/tools/registry.ts`:

1. 导入 `ToolSpec` 从 `./types`
2. 导入 `Settings` 从 `@flitter/schemas`
3. 导入 `ToolDefinition` 从 `@flitter/llm`
4. 实现 `ToolRegistry` 类:
   - `private tools: Map<string, ToolSpec>`
   - `register(spec)` — 重复 name 抛出 Error
   - `unregister(name)` — 返回 boolean
   - `get(name)` — 返回 ToolSpec | undefined
   - `has(name)` — 返回 boolean
   - `list()` — 返回 ToolSpec[]
   - `listEnabled(config: Settings)` — 过滤逻辑: isEnabled + config.tools.disable + config.tools.enable
   - `getToolDefinitions(config: Settings)` — 返回 ToolDefinition[] (name/description/inputSchema)
   - `normalizeToolName(name)` — 剥离 mcp__ 前缀
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - register 对重复 name 抛出 Error
    - listEnabled 正确实现三级过滤
    - getToolDefinitions 仅返回 name/description/inputSchema
    - normalizeToolName 正确处理 mcp__ 前缀
    - tsc 通过
  </acceptance_criteria>
  <done>ToolRegistry 实现完成</done>
</task>

<task type="auto">
  <name>Task 3: 编写 ToolRegistry 测试</name>
  <files>packages/agent-core/src/tools/registry.test.ts</files>
  <read_first>
    - packages/agent-core/src/tools/types.ts
    - packages/agent-core/src/tools/registry.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/tools/registry.test.ts`:

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

1. 创建 `createMockToolSpec(overrides?)` 辅助函数
2. register 组:
   - 注册成功 + get 获取
   - has 返回 true
   - 重复注册抛出 Error
   - 不同名工具共存
3. unregister 组:
   - 移除已注册返回 true
   - 移除未注册返回 false
   - 移除后不可 get/has
4. get/has 组:
   - 未注册 get 返回 undefined
   - 未注册 has 返回 false
5. list 组:
   - 空 registry 返回 []
   - 返回所有已注册工具
6. listEnabled 组:
   - 无过滤返回全部
   - isEnabled false 排除
   - config.tools.disable 排除
   - config.tools.enable 白名单过滤
   - 组合过滤
7. getToolDefinitions 组:
   - 返回正确的 ToolDefinition[]
   - 与 listEnabled 过滤一致
   - 空 registry 返回 []
8. normalizeToolName 组:
   - 普通名称原样
   - mcp__server__tool → tool
   - mcp__server__multi__part → multi__part

~20 个测试用例
  </action>
  <verify>
    <automated>bun test packages/agent-core/src/tools/registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 20 个测试用例
    - 覆盖所有 ToolRegistry 公共方法
    - 覆盖边界情况: 空 registry、重复注册、未注册查找
    - 所有测试通过
  </acceptance_criteria>
  <done>ToolRegistry 测试全部通过</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit && bun test packages/agent-core/src/tools/registry.test.ts
```
</verification>

<success_criteria>
- `tsc --noEmit` 零错误
- 所有测试通过 (>= 20 个)
- ToolSpec 接口覆盖所有必需字段 (name, description, inputSchema, execute, source)
- ToolResult.status 使用 @flitter/schemas 的 ToolRunInternalStatus
- ToolContext 包含 workingDirectory/signal/threadId/config
- ToolRegistry 实现完整的 CRUD + 过滤 + 名称规范化
- 零 `any` 类型 (测试中 config mock 除外)
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-core/10-01-SUMMARY.md`
</output>
