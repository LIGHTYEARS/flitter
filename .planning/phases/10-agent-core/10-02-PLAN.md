---
phase: 10-agent-core
plan: 02
type: tdd
wave: 1
depends_on: [10-01]
files_modified:
  - packages/agent-core/src/tools/orchestrator.ts
  - packages/agent-core/src/tools/orchestrator.test.ts
autonomous: true
requirements: [AGNT-02]
user_setup: []

must_haves:
  truths:
    - "hasResourceConflict(a, b, registry) 检测两个 tool_use 是否存在资源冲突"
    - "hasResourceConflict: 任一工具 serial=true → 冲突"
    - "hasResourceConflict: 两工具共享资源键且至少一方 mode='write' → 冲突"
    - "hasResourceConflict: 两工具共享资源键但都是 mode='read' → 无冲突"
    - "hasResourceConflict: 无共享资源键 → 无冲突"
    - "batchToolsByDependency(toolUses, registry) 将 tool_use 按依赖分组为批次"
    - "batchToolsByDependency: 无冲突工具归入同一批次 (可并行)"
    - "batchToolsByDependency: 有冲突工具分到不同批次 (串行执行)"
    - "batchToolsByDependency: 算法为贪心 — 逐个尝试加入当前批次，冲突则开新批次"
    - "ToolOrchestrator 构造参数: threadId, toolRegistry, callbacks"
    - "ToolOrchestrator.callbacks 接口定义 getConfig/updateThread/handle/getToolRunEnvironment/applyHookResult/applyPostHookResult/updateFileChanges/getDisposed$"
    - "ToolOrchestrator.runningTools 为 Map<string, { abort: AbortController }>"
    - "ToolOrchestrator.cancelledToolUses 为 Set<string>"
    - "ToolOrchestrator.executeToolsWithPlan(toolUses) 是主入口: 分批 → 顺序执行批次 → 批内 Promise.allSettled"
    - "ToolOrchestrator.invokeTool(toolUse) 核心流程: pre-hook → 注册 AbortController → updateThread(in-progress) → registry.get → execute → post-hook → updateThread(result) → 清理"
    - "ToolOrchestrator.invokeTool 错误处理: 包装错误 → updateThread(error status) → 清理 runningTools"
    - "ToolOrchestrator.cancelAll() 中止所有运行中的工具"
    - "ToolOrchestrator.cancelTool(toolUseId) 中止特定工具"
    - "ToolOrchestrator.hasRunningTools() 返回布尔值"
    - "ToolOrchestrator.dispose() 调用 cancelAll + 清理"
  artifacts:
    - "packages/agent-core/src/tools/orchestrator.ts"
    - "packages/agent-core/src/tools/orchestrator.test.ts"
  key_links:
    - "ToolSpec, ToolRegistry, ToolResult, ToolContext, ExecutionProfile, ResourceKey 从 Plan 10-01 的 types.ts 导入"
    - "ToolRegistry 从 Plan 10-01 的 registry.ts 导入"
    - "ToolUseBlock (tool_use content block) 从 @flitter/schemas 导入"
    - "Observable, Subject 从 @flitter/util 导入"
    - "Config 从 @flitter/schemas 导入"
    - "ToolOrchestrator 被 ThreadWorker (Plan 10-09) 使用"
    - "逆向对应: FWT (ToolOrchestrator), wwR (batchToolsByDependency), MwR (hasResourceConflict)"
---

<objective>
实现工具执行引擎: 资源冲突检测、依赖分批、ToolOrchestrator 批处理并行执行。

Purpose: 提供安全的工具并行执行能力 — 无冲突工具并行、有冲突工具串行、支持取消与清理
Output: `packages/agent-core/src/tools/` 下 1 个源文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-core/CONTEXT.md
@.planning/phases/10-agent-core/10-01-PLAN.md
@packages/agent-core/package.json
@packages/schemas/src/permissions.ts
@packages/schemas/src/messages.ts
@packages/schemas/src/config.ts
@packages/agent-core/src/tools/types.ts
@packages/agent-core/src/tools/registry.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `FWT` | tool-execution-engine.js | 857-1444 | ToolOrchestrator 类 (工具调度 + 并行执行) |
| `wwR` | tool-execution-engine.js | 838-856 | batchToolsByDependency (工具依赖分析 + 批处理) |
| `MwR` | tool-execution-engine.js | 820-837 | hasResourceConflict (资源冲突检测) |
| `FWT.runningTools` | tool-execution-engine.js | ~870 | Map<string, { abort }> 活跃工具追踪 |
| `FWT.cancelledToolUses` | tool-execution-engine.js | ~875 | Set<string> 已取消工具 ID |
| `QwR` | tool-execution-engine.js | ~2213 | 检查所有工具完成 (关联但不在本 Plan 范围) |

## 实现细节

### 1. orchestrator.ts — 冲突检测与分批函数

```ts
import type { ToolSpec, ToolResult, ToolContext, ExecutionProfile, ResourceKey } from "./types";
import { ToolRegistry } from "./registry";
import type { Config } from "@flitter/schemas";
import type { Observable } from "@flitter/util";

// ─── ToolUse 类型 (tool_use content block 的简化表示) ────

/** 从 @flitter/schemas ToolUseBlock 中提取 orchestrator 需要的字段 */
export interface ToolUseItem {
  /** tool_use block 的 ID */
  id: string;
  /** 工具名 */
  name: string;
  /** 工具参数 */
  input: Record<string, unknown>;
}

// ─── 资源冲突检测 ───────────────────────────────────────

/**
 * 检测两个工具调用是否存在资源冲突
 * 逆向: MwR
 *
 * 冲突条件 (任一满足即冲突):
 * 1. 任一工具的 executionProfile.serial === true
 * 2. 两工具共享资源键 (key 相同) 且至少一方 mode === "write"
 *
 * @returns true 表示有冲突, 不能并行
 */
export function hasResourceConflict(
  a: ToolUseItem,
  b: ToolUseItem,
  registry: ToolRegistry
): boolean;

/**
 * 将 tool_use 列表按依赖关系分组为批次
 * 逆向: wwR
 *
 * 算法: 贪心
 * - 遍历每个 toolUse
 * - 尝试加入当前批次的末尾批次
 * - 如果与该批次中任一工具冲突, 则创建新批次
 *
 * 结果: 同一批次内的工具可并行, 不同批次顺序执行
 *
 * @returns ToolUseItem[][] — 批次数组, 每个批次是可并行的工具数组
 */
export function batchToolsByDependency(
  toolUses: ToolUseItem[],
  registry: ToolRegistry
): ToolUseItem[][];
```

### 2. orchestrator.ts — OrchestratorCallbacks 接口

```ts
/**
 * ToolOrchestrator 回调接口
 * 由上层 (ThreadWorker) 实现, 传入 Orchestrator 用于通信
 */
export interface OrchestratorCallbacks {
  /** 获取当前运行时配置 */
  getConfig(): Promise<Config>;

  /** 更新线程状态 (向上层发送 tool:data 事件) */
  updateThread(event: ToolThreadEvent): Promise<void>;

  /** 处理工具数据事件 */
  handle(event: ToolDataEvent, signal?: AbortSignal): Promise<void>;

  /** 获取工具运行环境 (创建 ToolContext) */
  getToolRunEnvironment(toolUseId: string, signal: AbortSignal): Promise<ToolContext>;

  /** 执行 pre-hook 并返回结果 */
  applyHookResult(hookResult: HookResult): Promise<{ abortOp: boolean }>;

  /** 执行 post-hook */
  applyPostHookResult(
    hookResult: HookResult,
    opts: { toolUseId: string; result: ToolResult }
  ): Promise<void>;

  /** 更新文件变更追踪 */
  updateFileChanges(): Promise<void>;

  /** 获取 disposed 信号 */
  getDisposed$(): Observable<boolean>;
}

// ─── 事件类型 ────────────────────────────────────────────

export interface ToolThreadEvent {
  type: "tool:data";
  toolUseId: string;
  toolName: string;
  status: "in-progress" | "completed" | "error" | "cancelled";
  result?: ToolResult;
  error?: string;
}

export interface ToolDataEvent {
  type: "tool:data";
  toolUseId: string;
  data: ToolResult;
}

export interface HookResult {
  type: "pre" | "post";
  toolName: string;
  toolUseId: string;
  decision?: "allow" | "deny" | "ask";
  modifications?: Record<string, unknown>;
}
```

### 3. orchestrator.ts — ToolOrchestrator 类

```ts
/**
 * ToolOrchestrator: 工具批处理执行引擎
 * 逆向: FWT
 *
 * 职责:
 * 1. 将一组 tool_use 分批 (batchToolsByDependency)
 * 2. 顺序执行每个批次
 * 3. 批内使用 Promise.allSettled 并行执行
 * 4. 管理运行中工具的 AbortController
 * 5. 支持单个工具取消和全部取消
 */
export class ToolOrchestrator {
  /** 活跃工具追踪: toolUseId → { abort: AbortController } */
  readonly runningTools: Map<string, { abort: AbortController }> = new Map();

  /** 已取消的工具 ID 集合 */
  readonly cancelledToolUses: Set<string> = new Set();

  private disposed = false;

  constructor(
    private readonly threadId: string,
    private readonly toolRegistry: ToolRegistry,
    private readonly callbacks: OrchestratorCallbacks
  ) {}

  /**
   * 主入口: 执行一组 tool_use
   * 1. batchToolsByDependency 分批
   * 2. for 循环顺序执行每个批次
   * 3. 每个批次: Promise.allSettled(batch.map(t => invokeToolAndWait(t)))
   */
  async executeToolsWithPlan(toolUses: ToolUseItem[]): Promise<void>;

  /**
   * 执行单个工具并等待完成
   * 包装 invokeTool, 返回可 await 的 Promise
   */
  private async invokeToolAndWait(toolUse: ToolUseItem): Promise<void>;

  /**
   * 核心工具执行流程
   * 逆向: FWT 中的工具执行循环
   *
   * 流程:
   * 1. 检查是否已取消 → 如果是, 直接返回
   * 2. applyHookResult (pre-hook) → 如果 abortOp, 中止
   * 3. 创建 AbortController, 注册到 runningTools
   * 4. callbacks.updateThread({ status: "in-progress" })
   * 5. registry.get(toolName) → 获取 ToolSpec
   * 6. callbacks.getToolRunEnvironment(toolUseId, signal) → 获取 ToolContext
   * 7. spec.execute(args, context) → 获取 ToolResult
   *    - 如果返回 Observable, 订阅并收集最终结果
   *    - 如果返回 Promise, 直接 await
   * 8. applyPostHookResult (post-hook)
   * 9. callbacks.updateThread({ status: "completed", result })
   * 10. callbacks.updateFileChanges()
   * 11. 从 runningTools 移除
   *
   * 错误处理:
   * - 捕获所有错误 → 包装为 ToolResult { status: "error", error: message }
   * - callbacks.updateThread({ status: "error", error })
   * - 确保 runningTools 清理 (finally)
   */
  private async invokeTool(toolUse: ToolUseItem): Promise<void>;

  /** 取消所有运行中的工具 */
  cancelAll(): void {
    for (const [id, { abort }] of this.runningTools) {
      abort.abort();
      this.cancelledToolUses.add(id);
    }
  }

  /** 取消特定工具 */
  cancelTool(toolUseId: string): void {
    const entry = this.runningTools.get(toolUseId);
    if (entry) {
      entry.abort.abort();
      this.cancelledToolUses.add(toolUseId);
    }
  }

  /** 是否有工具正在运行 */
  hasRunningTools(): boolean {
    return this.runningTools.size > 0;
  }

  /** 销毁: 取消所有 + 清理 */
  dispose(): void {
    if (this.disposed) return;
    this.disposed = true;
    this.cancelAll();
    this.runningTools.clear();
    this.cancelledToolUses.clear();
  }
}
```

### 4. 测试策略

**orchestrator.test.ts** (~25 tests):

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

```
describe("hasResourceConflict")
├── it("两工具无 executionProfile → 无冲突")
├── it("工具 A serial=true → 冲突")
├── it("工具 B serial=true → 冲突")
├── it("两工具都 serial=true → 冲突")
├── it("共享资源键, 一方 write → 冲突")
├── it("共享资源键, 双方 write → 冲突")
├── it("共享资源键, 双方 read → 无冲突")
├── it("不同资源键 → 无冲突")
└── it("一方有资源键另一方没有 → 无冲突")

describe("batchToolsByDependency")
├── it("无冲突工具 → 1 个批次")
├── it("全部 serial → 每个工具一个批次")
├── it("read-write 冲突 → 分到不同批次")
├── it("read-read 同资源 → 同一批次")
├── it("空 toolUses → 空批次数组")
├── it("单个工具 → 1 个批次含 1 个工具")
└── it("混合冲突: 3 个工具, A↔B 冲突, B↔C 无冲突 → [A,C] + [B] 或 [A] + [B,C]")

describe("ToolOrchestrator")
├── describe("executeToolsWithPlan")
│   ├── it("并行执行: 无冲突工具同时开始 (计时验证)")
│   ├── it("串行批次: 有冲突工具顺序执行")
│   ├── it("空 toolUses 不报错")
│   └── it("单工具正常执行")
├── describe("invokeTool")
│   ├── it("成功路径: pre-hook → execute → post-hook → updateThread(completed)")
│   ├── it("错误路径: execute 抛出 → updateThread(error) + 错误包装")
│   ├── it("取消路径: 已取消的工具跳过执行")
│   └── it("工具未找到: updateThread(error)")
├── describe("cancelAll")
│   ├── it("中止所有运行中工具的 AbortController")
│   └── it("将所有运行中工具 ID 加入 cancelledToolUses")
├── describe("cancelTool")
│   ├── it("中止指定工具")
│   └── it("对不存在的 toolUseId 静默处理")
├── describe("hasRunningTools")
│   ├── it("无运行工具返回 false")
│   └── it("有运行工具返回 true")
└── describe("dispose")
    ├── it("调用 cancelAll")
    └── it("清理 runningTools 和 cancelledToolUses")
```

**测试辅助:**

```ts
// 创建 mock registry 并注册测试工具
function createTestRegistry(tools: Partial<ToolSpec>[]): ToolRegistry;

// 创建 mock callbacks (所有方法返回空/默认值)
function createMockCallbacks(overrides?: Partial<OrchestratorCallbacks>): OrchestratorCallbacks;

// 创建延迟执行的工具 (用于并行/计时测试)
function createDelayedTool(name: string, delayMs: number, result?: ToolResult): ToolSpec;

// 创建带资源键的工具
function createResourceTool(name: string, keys: ResourceKey[]): ToolSpec;

// 创建 serial 工具
function createSerialTool(name: string): ToolSpec;
```

预期测试数: ~25

<tasks>

<task type="auto">
  <name>Task 1: 实现冲突检测与分批函数</name>
  <files>packages/agent-core/src/tools/orchestrator.ts</files>
  <read_first>
    - packages/agent-core/src/tools/types.ts
    - packages/agent-core/src/tools/registry.ts
    - packages/schemas/src/messages.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/tools/orchestrator.ts`:

1. 定义 `ToolUseItem` 接口 (id, name, input)
2. 定义 `ToolThreadEvent`, `ToolDataEvent`, `HookResult` 事件类型
3. 定义 `OrchestratorCallbacks` 接口
4. 实现 `hasResourceConflict(a, b, registry)`:
   - 从 registry 获取两个工具的 ToolSpec
   - 检查 serial 标志
   - 检查 resourceKeys 交集 + write 模式
5. 实现 `batchToolsByDependency(toolUses, registry)`:
   - 贪心算法: 遍历工具, 尝试加入最后一个批次
   - 如与批次内任一工具冲突, 开新批次
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - hasResourceConflict 正确处理 serial + resourceKeys 冲突
    - batchToolsByDependency 生成正确的批次
    - tsc 通过
  </acceptance_criteria>
  <done>冲突检测与分批函数实现完成</done>
</task>

<task type="auto">
  <name>Task 2: 实现 ToolOrchestrator 类</name>
  <files>packages/agent-core/src/tools/orchestrator.ts</files>
  <read_first>
    - packages/agent-core/src/tools/orchestrator.ts (Task 1 产物)
    - packages/agent-core/src/tools/types.ts
    - packages/agent-core/src/tools/registry.ts
  </read_first>
  <action>
在 `orchestrator.ts` 中追加 ToolOrchestrator 类:

1. 构造函数: threadId, toolRegistry, callbacks
2. runningTools Map 和 cancelledToolUses Set
3. executeToolsWithPlan:
   - 调用 batchToolsByDependency 分批
   - for...of 遍历批次
   - Promise.allSettled(batch.map(invokeToolAndWait))
4. invokeToolAndWait: 包装 invokeTool 为可 await Promise
5. invokeTool:
   - 检查 cancelledToolUses
   - pre-hook 执行
   - AbortController 创建 + 注册
   - updateThread(in-progress)
   - registry.get + execute
   - Observable 处理 (如果 execute 返回 Observable, 转为 Promise)
   - post-hook 执行
   - updateThread(completed)
   - finally: 清理 runningTools
   - catch: 包装错误 + updateThread(error)
6. cancelAll, cancelTool, hasRunningTools, dispose
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - executeToolsWithPlan 正确分批 + Promise.allSettled
    - invokeTool 完整流程: hook → execute → hook → update → cleanup
    - 错误处理: 捕获 + 包装 + 清理
    - cancelAll/cancelTool 正确中止 AbortController
    - tsc 通过
  </acceptance_criteria>
  <done>ToolOrchestrator 类实现完成</done>
</task>

<task type="auto">
  <name>Task 3: 编写 ToolOrchestrator 测试</name>
  <files>packages/agent-core/src/tools/orchestrator.test.ts</files>
  <read_first>
    - packages/agent-core/src/tools/orchestrator.ts
    - packages/agent-core/src/tools/types.ts
    - packages/agent-core/src/tools/registry.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/tools/orchestrator.test.ts`:

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

1. 创建测试辅助函数:
   - createTestRegistry(tools) — 注册一组测试工具
   - createMockCallbacks(overrides?) — 创建 mock OrchestratorCallbacks
   - createDelayedTool(name, delayMs, result?) — 创建延迟执行工具 (用于计时验证)
   - createResourceTool(name, keys) — 创建带资源键的工具
   - createSerialTool(name) — 创建 serial 工具

2. hasResourceConflict 组 (~9 tests):
   - 无 profile、serial、read-write、read-read、不同 key 等

3. batchToolsByDependency 组 (~7 tests):
   - 无冲突、全 serial、read-write 冲突、混合场景等

4. ToolOrchestrator 组 (~9 tests):
   - executeToolsWithPlan: 并行/串行/空 input
   - invokeTool: 成功/错误/取消/工具不存在
   - cancelAll/cancelTool/hasRunningTools/dispose

~25 个测试用例
  </action>
  <verify>
    <automated>bun test packages/agent-core/src/tools/orchestrator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 25 个测试用例
    - hasResourceConflict 覆盖所有冲突/无冲突组合
    - batchToolsByDependency 覆盖边界情况
    - ToolOrchestrator 覆盖成功/错误/取消路径
    - 并行执行通过计时验证 (两个 50ms 工具总时间 < 120ms)
    - 所有测试通过
  </acceptance_criteria>
  <done>ToolOrchestrator 测试全部通过</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit && bun test packages/agent-core/src/tools/orchestrator.test.ts
```
</verification>

<success_criteria>
- `tsc --noEmit` 零错误
- 所有测试通过 (>= 25 个)
- hasResourceConflict 正确检测 serial + 资源键冲突
- batchToolsByDependency 贪心分批逻辑正确
- ToolOrchestrator.executeToolsWithPlan 实现批次顺序 + 批内并行
- invokeTool 完整覆盖: pre-hook → execute → post-hook → cleanup
- 错误路径: 异常捕获 + ToolResult 错误包装 + runningTools 清理
- 取消路径: AbortController.abort() + cancelledToolUses 标记
- 零 `any` 类型 (测试中 mock 除外)
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-core/10-02-SUMMARY.md`
</output>
