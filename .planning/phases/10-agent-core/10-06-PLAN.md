---
phase: 10-agent-core
plan: 06
type: tdd
wave: 3
depends_on: [10-01]
files_modified:
  - packages/agent-core/src/permissions/matcher.ts
  - packages/agent-core/src/permissions/matcher.test.ts
autonomous: true
requirements: [AGNT-07, AGNT-08]
user_setup: []

must_haves:
  truths:
    - "matchToolPattern(pattern, toolName) 使用 glob 匹配: * → [^/]*, ** → .*, ? → [^/]"
    - "matchToolPattern 大小写不敏感匹配"
    - "matchToolPattern 精确字符串匹配 (无通配符时)"
    - "matchToolPattern 支持 mcp__*__* 风格的前缀通配"
    - "matchDisablePattern(patterns, toolName) 返回 true 当任意 pattern 匹配 toolName"
    - "matchDisablePattern 空数组返回 false"
    - "checkToolEnabled(toolName, config) 三步评估: tools.disable → tools.enable → 默认 enabled"
    - "checkToolEnabled 返回 ToolEnableResult { enabled: boolean, disabledReason?: 'settings' }"
    - "checkToolEnabled 工具被 disable 后可被 enable 列表重新启用"
    - "matchPermissionMatcher 字符串 matcher: glob 匹配字符串值"
    - "matchPermissionMatcher 字符串 matcher: /regex/ 包裹时使用正则匹配"
    - "matchPermissionMatcher boolean/number/null matcher: 严格相等"
    - "matchPermissionMatcher undefined matcher: value 为 undefined 时匹配"
    - "matchPermissionMatcher 数组 matcher: OR 语义, 任一元素匹配即通过"
    - "matchPermissionMatcher record matcher: 每个 key 递归匹配目标对象对应 value"
    - "matchPermissionEntry(entry, toolName, args) 同时检查 entry.tool 和 entry.matches"
    - "matchPermissionEntry entry.tool 使用 matchToolPattern 匹配 toolName"
    - "matchPermissionEntry entry.matches 中每个 key-value 使用 matchPermissionMatcher 匹配 args 对应值"
    - "matchPermissionEntry 两者均匹配才返回 true"
    - "自实现最小 glob→regex 转换, 不依赖 picomatch 外部包"
  artifacts:
    - "packages/agent-core/src/permissions/matcher.ts"
    - "packages/agent-core/src/permissions/matcher.test.ts"
  key_links:
    - "PermissionEntry, PermissionMatcher, PermissionAction, ToolEnableResult 从 @flitter/schemas/permissions 导入"
    - "Settings 从 @flitter/schemas/config 导入"
    - "matchToolPattern 被 matchPermissionEntry 内部使用"
    - "matchPermissionEntry 被 PermissionEngine (Plan 10-07) 使用进行规则评估"
    - "checkToolEnabled 被 ToolRegistry.listEnabled (Plan 10-01) 和 PermissionEngine 使用"
    - "matchDisablePattern 被 checkToolEnabled 内部使用"
    - "逆向对应: tool-permissions.js 中的 Vf (matchToolPattern), Xf (matchDisablePattern), yy (checkToolEnabled)"
---

<objective>
实现权限 DSL 解析器: glob 模式匹配、工具启用/禁用检查、递归权限匹配器、规则条目匹配。

Purpose: 为权限执行引擎 (Plan 10-07) 提供底层匹配原语 -- 将权限 DSL 规则解析为可执行的匹配逻辑
Output: `packages/agent-core/src/permissions/` 下 1 个源文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-core/CONTEXT.md
@.planning/phases/10-agent-core/10-01-PLAN.md
@packages/agent-core/package.json
@packages/schemas/src/permissions.ts
@packages/schemas/src/config.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `Vf` | tool-permissions.js | 304-321 | matchToolPattern: picomatch glob 匹配工具名 |
| `Xf` | tool-permissions.js | 323-340 | matchDisablePattern: 禁用列表批量匹配 |
| `yy` | tool-permissions.js | 342-382 | checkToolEnabled: 三步 enable/disable 评估 |
| (内联) | tool-permissions.js | ~160-230 | matchPermissionMatcher: 递归深度匹配 |
| (内联) | tool-permissions.js | ~230-270 | matchPermissionEntry: tool + matches 双重检查 |

## 实现细节

### 1. matcher.ts -- 权限 DSL 匹配原语

```ts
import type {
  PermissionEntry,
  PermissionMatcher,
  ToolEnableResult,
} from "@flitter/schemas";
import type { Settings } from "@flitter/schemas";

// ─── Glob → Regex 转换 ─────────────────────────────────

/**
 * 将 glob 模式转换为正则表达式 (自实现, 不依赖 picomatch)
 *
 * 转换规则:
 * - `**` → `.*` (匹配任意字符包括分隔符)
 * - `*`  → `[^/]*` (匹配除 / 外的任意字符)
 * - `?`  → `[^/]` (匹配单个非 / 字符)
 * - 其他特殊正则字符转义: . + ^ $ { } | ( ) [ ] \
 *
 * 注意: 先处理 ** 再处理 *, 避免 ** 被拆成两个 *
 *
 * 返回的 RegExp 使用 'i' 标志 (大小写不敏感)
 */
function globToRegex(pattern: string): RegExp {
  // 1. 转义所有正则特殊字符 (除 * 和 ? 外)
  // 2. 替换 \*\* → .* (必须先替换双星)
  // 3. 替换剩余 \* → [^/]*
  // 4. 替换 \? → [^/]
  // 5. 返回 new RegExp(`^${result}$`, "i")
}

// ─── 工具名 Glob 匹配 ──────────────────────────────────

/**
 * 匹配工具名是否符合 glob 模式
 * 逆向: Vf (matchToolPattern)
 *
 * @param pattern - Glob 模式 (如 "mcp__*__*", "Bash", "*")
 * @param toolName - 工具名 (如 "mcp__server__tool", "Bash")
 * @returns 是否匹配
 *
 * @example
 * matchToolPattern("*", "anything")           // true
 * matchToolPattern("Bash", "Bash")            // true (精确)
 * matchToolPattern("Bash", "bash")            // true (大小写不敏感)
 * matchToolPattern("mcp__*", "mcp__server")   // true
 * matchToolPattern("mcp__*__*", "mcp__s__t")  // true
 */
export function matchToolPattern(pattern: string, toolName: string): boolean {
  const regex = globToRegex(pattern);
  return regex.test(toolName);
}

// ─── 禁用列表匹配 ──────────────────────────────────────

/**
 * 检查工具名是否匹配禁用列表中的任意模式
 * 逆向: Xf (matchDisablePattern)
 *
 * @param patterns - 禁用模式列表 (如 ["Bash", "mcp__*"])
 * @param toolName - 工具名
 * @returns true 如果任意 pattern 匹配
 */
export function matchDisablePattern(
  patterns: string[],
  toolName: string,
): boolean {
  return patterns.some((pattern) => matchToolPattern(pattern, toolName));
}

// ─── 工具启用检查 ──────────────────────────────────────

/**
 * 三步评估工具是否启用
 * 逆向: yy (checkToolEnabled)
 *
 * 评估顺序:
 * 1. 如果 config["tools.disable"] 包含匹配模式 → disabled (reason: "settings")
 * 2. 如果上一步 disabled 且 config["tools.enable"] 包含匹配模式 → 重新启用
 * 3. 默认: enabled
 *
 * @param toolName - 工具名
 * @param config - Settings 配置
 * @returns ToolEnableResult { enabled, disabledReason? }
 */
export function checkToolEnabled(
  toolName: string,
  config: Settings,
): ToolEnableResult {
  const disablePatterns = config["tools.disable"] ?? [];
  const enablePatterns = config["tools.enable"] ?? [];

  // Step 1: 检查禁用列表
  const isDisabled = matchDisablePattern(disablePatterns, toolName);

  if (isDisabled) {
    // Step 2: 检查启用列表是否覆盖禁用
    const isReEnabled = matchDisablePattern(enablePatterns, toolName);
    if (isReEnabled) {
      return { enabled: true };
    }
    return { enabled: false, disabledReason: "settings" };
  }

  // Step 3: 默认启用
  return { enabled: true };
}

// ─── 递归权限匹配器 ────────────────────────────────────

/**
 * 递归匹配 PermissionMatcher 与实际值
 * 逆向: tool-permissions.js ~160-230 的递归匹配逻辑
 *
 * 匹配规则:
 * - string matcher:
 *   - 如果以 "/" 包裹 (如 "/^src\/.+/") → 提取正则并匹配
 *   - 否则 → glob 匹配 (对 string value)
 *   - value 不是 string → false (glob/regex 仅匹配字符串)
 * - boolean/number/null matcher: 严格相等 (===)
 * - undefined matcher: value === undefined 时匹配
 * - array matcher: OR 语义 — 数组中任一元素与 value 匹配即返回 true
 * - record matcher: 每个 key 的 matcher 递归匹配 value[key]
 *   - value 必须是非 null 对象
 *   - 所有 key 均需匹配才返回 true (AND 语义)
 *
 * @param matcher - PermissionMatcher (递归联合类型)
 * @param value - 实际的参数值
 * @returns 是否匹配
 */
export function matchPermissionMatcher(
  matcher: PermissionMatcher,
  value: unknown,
): boolean {
  // undefined matcher
  if (matcher === undefined) {
    return value === undefined;
  }

  // null matcher
  if (matcher === null) {
    return value === null;
  }

  // boolean matcher
  if (typeof matcher === "boolean") {
    return value === matcher;
  }

  // number matcher
  if (typeof matcher === "number") {
    return value === matcher;
  }

  // string matcher: glob or regex
  if (typeof matcher === "string") {
    if (typeof value !== "string") return false;

    // 检查是否为 /regex/ 格式
    const regexMatch = matcher.match(/^\/(.+)\/([gimsuy]*)$/);
    if (regexMatch) {
      const regex = new RegExp(regexMatch[1], regexMatch[2]);
      return regex.test(value);
    }

    // 否则 glob 匹配
    return matchToolPattern(matcher, value);
  }

  // array matcher: OR
  if (Array.isArray(matcher)) {
    return matcher.some((m) => matchPermissionMatcher(m, value));
  }

  // record matcher: AND (each key must match)
  if (typeof matcher === "object") {
    if (typeof value !== "object" || value === null) return false;
    const record = value as Record<string, unknown>;
    return Object.entries(matcher).every(([key, subMatcher]) =>
      matchPermissionMatcher(subMatcher, record[key]),
    );
  }

  return false;
}

// ─── 权限条目匹配 ──────────────────────────────────────

/**
 * 检查 PermissionEntry 是否匹配给定的工具调用
 * 逆向: tool-permissions.js ~230-270
 *
 * 双重检查:
 * 1. entry.tool glob 模式匹配 toolName → 使用 matchToolPattern
 * 2. entry.matches (如果定义) 中每个 key 的 matcher 匹配 args 对应值
 *    → 使用 matchPermissionMatcher
 *
 * 两者均通过才返回 true
 *
 * @param entry - 权限规则条目
 * @param toolName - 工具名
 * @param args - 工具调用参数
 * @returns 是否匹配
 */
export function matchPermissionEntry(
  entry: PermissionEntry,
  toolName: string,
  args: Record<string, unknown>,
): boolean {
  // 1. 工具名模式匹配
  if (!matchToolPattern(entry.tool, toolName)) {
    return false;
  }

  // 2. 参数匹配 (如果定义了 matches)
  if (entry.matches) {
    for (const [key, matcher] of Object.entries(entry.matches)) {
      if (!matchPermissionMatcher(matcher, args[key])) {
        return false;
      }
    }
  }

  return true;
}
```

### 2. 测试策略

使用 `node:test` 的 `describe/it` + `node:assert/strict`。纯单元测试, 无文件系统依赖。

**matcher.test.ts** (~20 tests):

```
describe("Permission DSL Matcher")
├── describe("matchToolPattern")
│   ├── it("精确匹配: 'Bash' 匹配 'Bash'")
│   ├── it("大小写不敏感: 'bash' 匹配 'Bash'")
│   ├── it("单星通配: '*' 匹配任意工具名")
│   ├── it("前缀通配: 'mcp__*' 匹配 'mcp__server'")
│   ├── it("多段通配: 'mcp__*__*' 匹配 'mcp__srv__tool'")
│   ├── it("不匹配: 'Read' 不匹配 'Write'")
│   └── it("问号通配: 'Rea?' 匹配 'Read' 但不匹配 'Ready'... 不对, 匹配 'Read'")
├── describe("matchDisablePattern")
│   ├── it("单个匹配: ['Bash'] 匹配 'Bash'")
│   ├── it("无匹配: ['Bash'] 不匹配 'Read'")
│   ├── it("空数组: [] 不匹配任何工具")
│   └── it("多模式: ['Bash', 'mcp__*'] 匹配 'mcp__srv'")
├── describe("checkToolEnabled")
│   ├── it("默认启用: 无 disable/enable 配置")
│   ├── it("禁用: tools.disable 匹配")
│   ├── it("重新启用: tools.disable 匹配但 tools.enable 也匹配")
│   └── it("返回 disabledReason: 'settings' 当禁用时")
├── describe("matchPermissionMatcher")
│   ├── it("字符串 glob: '*.ts' 匹配 'foo.ts'")
│   ├── it("字符串 regex: '/^src/' 匹配 'src/index.ts'")
│   ├── it("字符串 matcher 不匹配非字符串 value")
│   ├── it("boolean: true 匹配 true, 不匹配 false")
│   ├── it("number: 42 匹配 42, 不匹配 43")
│   ├── it("null: null 匹配 null, 不匹配 undefined")
│   ├── it("undefined: undefined 匹配 undefined, 不匹配 null")
│   ├── it("数组 OR: ['*.ts', '*.js'] 匹配 'foo.ts'")
│   ├── it("数组 OR: 无匹配则 false")
│   ├── it("record AND: { path: '*.ts' } 匹配 { path: 'foo.ts' }")
│   └── it("record AND: 嵌套 { opts: { recursive: true } } 深度匹配")
└── describe("matchPermissionEntry")
    ├── it("tool 匹配 + 无 matches: 仅检查工具名")
    ├── it("tool 匹配 + matches 匹配: 全部通过")
    ├── it("tool 匹配 + matches 不匹配: 返回 false")
    ├── it("tool 不匹配: 直接返回 false")
    └── it("多 matches 键: 所有键均需匹配")
```

预期测试数: ~25 (充裕覆盖, 最低要求 ~20)

<tasks>

<task type="auto">
  <name>Task 1: 实现权限 DSL 匹配器</name>
  <files>packages/agent-core/src/permissions/matcher.ts</files>
  <read_first>
    - packages/schemas/src/permissions.ts
    - packages/schemas/src/config.ts
    - packages/agent-core/package.json
    - packages/agent-core/src/tools/types.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/permissions/matcher.ts`:

1. 导入 `PermissionEntry`, `PermissionMatcher`, `ToolEnableResult` 从 `@flitter/schemas`
2. 导入 `Settings` 从 `@flitter/schemas`
3. 实现 `globToRegex(pattern)`: 自实现 glob → RegExp 转换
   - 转义特殊字符 (. + ^ $ { } | ( ) [ ] \)
   - ** → .* (先替换)
   - * → [^/]* (后替换)
   - ? → [^/]
   - 返回 new RegExp(`^${result}$`, "i")
4. 导出 `matchToolPattern(pattern, toolName)`: 使用 globToRegex 测试
5. 导出 `matchDisablePattern(patterns, toolName)`: 遍历 patterns 调用 matchToolPattern
6. 导出 `checkToolEnabled(toolName, config)`: 三步评估逻辑
   - Step 1: config["tools.disable"] → 匹配则标记 disabled
   - Step 2: disabled 时检查 config["tools.enable"] → 匹配则重新启用
   - Step 3: 默认 enabled
   - 返回 ToolEnableResult
7. 导出 `matchPermissionMatcher(matcher, value)`: 递归匹配
   - undefined → value === undefined
   - null → value === null
   - boolean/number → 严格相等
   - string → /regex/ 检测 → 否则 glob 匹配 (仅对 string value)
   - array → OR (some)
   - record → AND (every key 递归)
8. 导出 `matchPermissionEntry(entry, toolName, args)`: 双重检查
   - matchToolPattern(entry.tool, toolName)
   - entry.matches 存在时: 每个 key 用 matchPermissionMatcher 匹配 args[key]
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 所有函数正确导出
    - globToRegex 不依赖外部 glob/picomatch 包
    - 类型正确引用 @flitter/schemas
    - tsc 通过
  </acceptance_criteria>
  <done>权限 DSL 匹配器实现完成</done>
</task>

<task type="auto">
  <name>Task 2: 编写权限 DSL 匹配器测试</name>
  <files>packages/agent-core/src/permissions/matcher.test.ts</files>
  <read_first>
    - packages/agent-core/src/permissions/matcher.ts
    - packages/schemas/src/permissions.ts
    - packages/schemas/src/config.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/permissions/matcher.test.ts`:

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

1. matchToolPattern 组 (~7 tests):
   - 精确匹配 (Bash → Bash)
   - 大小写不敏感 (bash → Bash)
   - 单星 (* → 任意)
   - 前缀通配 (mcp__* → mcp__server)
   - 多段通配 (mcp__*__* → mcp__srv__tool)
   - 不匹配 (Read → Write)
   - 问号 (Rea? → Read)

2. matchDisablePattern 组 (~4 tests):
   - 单个匹配
   - 无匹配
   - 空数组
   - 多模式

3. checkToolEnabled 组 (~4 tests):
   - 默认启用
   - 禁用 (disable 匹配)
   - 重新启用 (disable + enable 均匹配)
   - disabledReason 为 "settings"

4. matchPermissionMatcher 组 (~11 tests):
   - 字符串 glob 匹配
   - 字符串 regex 匹配
   - 字符串 matcher 对非字符串 value 返回 false
   - boolean 严格相等
   - number 严格相等
   - null 匹配 null, 不匹配 undefined
   - undefined 匹配 undefined, 不匹配 null
   - 数组 OR 匹配
   - 数组 OR 无匹配
   - record AND 匹配
   - record 嵌套深度匹配

5. matchPermissionEntry 组 (~5 tests):
   - tool 匹配 + 无 matches
   - tool + matches 全部匹配
   - tool 匹配 + matches 不匹配
   - tool 不匹配
   - 多 matches 键全部匹配

~25 个测试用例 (最低要求 ~20)
  </action>
  <verify>
    <automated>bun test packages/agent-core/src/permissions/matcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 20 个测试用例
    - 覆盖所有导出函数
    - 覆盖边界: 空数组、不匹配、大小写、嵌套 record
    - 所有测试通过
  </acceptance_criteria>
  <done>权限 DSL 匹配器测试全部通过</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit && bun test packages/agent-core/src/permissions/matcher.test.ts
```
</verification>

<success_criteria>
- `tsc --noEmit` 零错误
- 所有测试通过 (>= 20 个)
- matchToolPattern: 自实现 glob→regex, 支持 *, **, ?, 精确匹配, 大小写不敏感
- matchDisablePattern: 批量 OR 匹配
- checkToolEnabled: 三步评估 (disable → enable 覆盖 → 默认), 返回 ToolEnableResult
- matchPermissionMatcher: 递归匹配 string/boolean/number/null/undefined/array/record 全类型
- matchPermissionEntry: tool glob + matches 递归双重检查
- 不依赖 picomatch 或其他外部 glob 库
- 零 `any` 类型
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-core/10-06-SUMMARY.md`
</output>
