---
phase: 10-agent-core
plan: 07
type: tdd
wave: 3
depends_on: [10-01, 10-06]
files_modified:
  - packages/agent-core/src/permissions/engine.ts
  - packages/agent-core/src/permissions/guarded-files.ts
  - packages/agent-core/src/permissions/engine.test.ts
autonomous: true
requirements: [AGNT-07, AGNT-08]
user_setup: []

must_haves:
  truths:
    - "PermissionEngine.checkPermission(toolName, args, context?) 返回 PermissionCheckResult"
    - "PermissionEngine 四级决策: 受保护文件 → 用户规则 → 默认规则 → fallback ask"
    - "PermissionEngine 用户规则 (config.permissions) 按顺序评估, 第一个匹配的规则生效"
    - "PermissionEngine 用户规则优先级高于默认规则"
    - "PermissionEngine fallback 动作为 'ask'"
    - "PermissionEngine 支持 context 过滤: entry.context 存在时仅在匹配的上下文中生效"
    - "PermissionEngine.requestApproval(request) 推送到 pendingApprovals$ Subject"
    - "DEFAULT_PERMISSION_RULES 定义内置默认权限 (Read/Grep/Glob 允许, Bash 需确认, 等)"
    - "DEFAULT_PERMISSION_RULES 中 Write/Edit 在 workspace 内允许 (matches file_path 通配)"
    - "DEFAULT_PERMISSION_RULES 中 mcp__* 工具默认需确认"
    - "DEFAULT_PERMISSION_RULES 中 Task 工具默认允许"
    - "getToolFilePaths(toolName, args) 从 Read/Write/Edit 的 args 中提取 file_path"
    - "getToolFilePaths 从 Bash args 中解析简单文件路径"
    - "getToolFilePaths 返回绝对路径数组"
    - "checkGuardedFile(filePath, allowlist) 使用 glob 匹配检查文件是否受保护"
    - "checkGuardedFile 返回 true 当文件不在 allowlist 中 (即受保护)"
    - "受保护文件触发 action: 'ask', reason 包含 'guarded file'"
    - "allowlist 中的文件绕过受保护检查"
  artifacts:
    - "packages/agent-core/src/permissions/engine.ts"
    - "packages/agent-core/src/permissions/guarded-files.ts"
    - "packages/agent-core/src/permissions/engine.test.ts"
  key_links:
    - "matchToolPattern, matchPermissionEntry 从 Plan 10-06 的 matcher.ts 导入"
    - "PermissionEntry, PermissionAction, PermissionContext, PermissionCheckResult, ToolApprovalRequest 从 @flitter/schemas 导入"
    - "Settings, Config 从 @flitter/schemas 导入"
    - "Subject 从 @flitter/util 导入"
    - "PermissionEngine 被 ToolOrchestrator (Plan 10-02) 在执行工具前调用"
    - "PermissionEngine 被 ThreadWorker (Plan 10-09) 用于审批流程"
    - "逆向对应: tool-permissions.js 的 ToolPermissionsService (171-283)"
    - "逆向对应: tool-permissions.js 的 jmR (getToolFilePaths), rcT (checkGuardedFile)"
    - "逆向对应: permission-rule-defs.js 的默认权限规则"
---

<objective>
实现权限执行引擎: 四级决策引擎 (受保护文件 → 用户规则 → 默认规则 → fallback), 受保护文件检查, 审批请求推送。

Purpose: 在工具执行前进行权限校验 -- 决定工具调用应被允许、拒绝、还是需要用户确认
Output: `packages/agent-core/src/permissions/` 下 2 个源文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-core/CONTEXT.md
@.planning/phases/10-agent-core/10-01-PLAN.md
@.planning/phases/10-agent-core/10-06-PLAN.md
@packages/agent-core/package.json
@packages/agent-core/src/permissions/matcher.ts
@packages/schemas/src/permissions.ts
@packages/schemas/src/config.ts
@packages/util/src/reactive/subject.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| ToolPermissionsService | tool-permissions.js | 171-283 | PermissionEngine: 四级决策引擎主体 |
| `jmR` | tool-permissions.js | 388-400 | getToolFilePaths: 从工具参数提取文件路径 |
| `rcT` | tool-permissions.js | ~410 | checkGuardedFile: 受保护文件检查 |
| (内联) | permission-rule-defs.js | ~1-200 | DEFAULT_PERMISSION_RULES: 内置默认权限规则 |

## 实现细节

### 1. guarded-files.ts -- 受保护文件检查

```ts
import * as path from "node:path";
import { matchToolPattern } from "./matcher";

// ─── 工具文件路径提取 ──────────────────────────────────

/**
 * 从工具参数中提取该工具将访问的文件路径
 * 逆向: jmR (getToolFilePaths)
 *
 * 提取策略:
 * - Read/Write/Edit: args.file_path (单个绝对路径)
 * - Grep/Glob/FuzzyFind: args.path (搜索目录)
 * - Bash: 从 args.command 中解析简单文件路径
 *   - 基础解析: 匹配以 / 开头的路径 token
 *   - 不做完整 shell 解析, 仅提取明显的绝对路径
 * - 其他工具: 返回空数组
 *
 * @param toolName - 工具名
 * @param args - 工具调用参数
 * @returns 绝对路径数组
 */
export function getToolFilePaths(
  toolName: string,
  args: Record<string, unknown>,
): string[] {
  const paths: string[] = [];

  switch (toolName) {
    case "Read":
    case "Write":
    case "Edit": {
      const filePath = args.file_path as string | undefined;
      if (filePath && path.isAbsolute(filePath)) {
        paths.push(filePath);
      }
      break;
    }
    case "Grep":
    case "Glob":
    case "FuzzyFind": {
      const searchPath = args.path as string | undefined;
      if (searchPath && path.isAbsolute(searchPath)) {
        paths.push(searchPath);
      }
      break;
    }
    case "Bash": {
      const command = args.command as string | undefined;
      if (command) {
        // 基础解析: 提取以 / 开头、不含引号/空白的路径 token
        const pathRegex = /(?:^|\s)(\/[^\s'"`;|&><]+)/g;
        let match: RegExpExecArray | null;
        while ((match = pathRegex.exec(command)) !== null) {
          paths.push(match[1]);
        }
      }
      break;
    }
  }

  return paths;
}

// ─── 受保护文件检查 ────────────────────────────────────

/**
 * 检查文件是否受保护 (即不在 allowlist 中)
 * 逆向: rcT (checkGuardedFile)
 *
 * 逻辑:
 * - 如果 filePath 匹配 allowlist 中任一 glob 模式 → 不受保护 (返回 false)
 * - 否则 → 受保护 (返回 true)
 *
 * 用于受保护文件的概念: 所有文件默认受保护, 只有在 allowlist 中的文件才不受保护
 * 注意: 这个函数只在 guardedFiles 功能开启时调用, 调用方负责判断是否启用
 *
 * @param filePath - 文件绝对路径
 * @param allowlist - 允许的 glob 模式列表
 * @returns true 如果文件受保护 (不在 allowlist)
 */
export function checkGuardedFile(
  filePath: string,
  allowlist: string[],
): boolean {
  // allowlist 为空 → 所有文件受保护
  if (allowlist.length === 0) return true;

  // 任一模式匹配 → 不受保护
  return !allowlist.some((pattern) => matchToolPattern(pattern, filePath));
}
```

### 2. engine.ts -- 权限执行引擎

```ts
import type {
  PermissionEntry,
  PermissionAction,
  PermissionContext,
  PermissionCheckResult,
  ToolApprovalRequest,
} from "@flitter/schemas";
import type { Config, Settings } from "@flitter/schemas";
import type { Subject } from "@flitter/util";
import { matchPermissionEntry, matchToolPattern } from "./matcher";
import { getToolFilePaths, checkGuardedFile } from "./guarded-files";

// ─── 默认权限规则 ──────────────────────────────────────

/**
 * 内置默认权限规则
 * 逆向: permission-rule-defs.js
 *
 * 规则按优先级排列, 第一个匹配的生效:
 * 1. 只读工具: 始终允许
 * 2. 写工具: workspace 内允许 (通过 ${workspaceRoot}/** 匹配)
 * 3. Bash: 始终需要确认
 * 4. MCP 工具: 默认需要确认
 * 5. Task (子代理): 允许
 *
 * 注意: ${workspaceRoot} 是占位符, 在评估时由 PermissionEngine 替换为实际路径
 */
export const DEFAULT_PERMISSION_RULES: PermissionEntry[] = [
  // 只读工具: 始终允许
  { tool: "Read", action: "allow" },
  { tool: "Grep", action: "allow" },
  { tool: "Glob", action: "allow" },
  { tool: "FuzzyFind", action: "allow" },
  { tool: "TodoWrite", action: "allow" },

  // 写工具: workspace 内允许
  { tool: "Write", action: "allow", matches: { file_path: "${workspaceRoot}/**" } },
  { tool: "Edit", action: "allow", matches: { file_path: "${workspaceRoot}/**" } },

  // Bash: 始终需要确认
  { tool: "Bash", action: "ask" },

  // MCP 工具: 默认需要确认
  { tool: "mcp__*", action: "ask" },

  // Task (子代理): 允许
  { tool: "Task", action: "allow" },
];

// ─── 权限引擎选项 ──────────────────────────────────────

export interface PermissionEngineOpts {
  /** 获取当前配置 */
  getConfig: () => Config;
  /** 审批请求推送管道 */
  pendingApprovals$: Subject<ToolApprovalRequest[]>;
  /** 工作区根目录 (用于替换 ${workspaceRoot}) */
  workspaceRoot: string;
}

// ─── PermissionEngine 类 ───────────────────────────────

/**
 * 权限执行引擎: 四级决策引擎
 * 逆向: ToolPermissionsService (tool-permissions.js 171-283)
 *
 * 决策层级 (按优先级):
 * 1. 受保护文件检查: 文件在 guardedFiles 中且不在 allowlist → ask
 * 2. 用户规则: config.permissions 按顺序评估, 第一个匹配生效
 * 3. 默认规则: DEFAULT_PERMISSION_RULES 按顺序评估
 * 4. Fallback: ask (未匹配任何规则时)
 */
export class PermissionEngine {
  private readonly getConfig: () => Config;
  private readonly pendingApprovals$: Subject<ToolApprovalRequest[]>;
  private readonly workspaceRoot: string;

  constructor(opts: PermissionEngineOpts) {
    this.getConfig = opts.getConfig;
    this.pendingApprovals$ = opts.pendingApprovals$;
    this.workspaceRoot = opts.workspaceRoot;
  }

  /**
   * 检查工具调用权限
   *
   * @param toolName - 工具名
   * @param args - 工具调用参数
   * @param context - 权限上下文 ("thread" 或 "subagent"), 默认 "thread"
   * @returns PermissionCheckResult
   */
  checkPermission(
    toolName: string,
    args: Record<string, unknown>,
    context: PermissionContext = "thread",
  ): PermissionCheckResult {
    const config = this.getConfig();
    const settings = config.settings;

    // ─── Level 1: 受保护文件检查 ─────────────────────
    const guardedResult = this.checkGuardedFiles(toolName, args, settings);
    if (guardedResult) return guardedResult;

    // ─── Level 2: 用户规则 ───────────────────────────
    const userRules = settings.permissions ?? [];
    const userResult = this.evaluateRules(
      userRules,
      toolName,
      args,
      context,
      "user-settings",
    );
    if (userResult) return userResult;

    // ─── Level 3: 默认规则 ───────────────────────────
    const resolvedDefaults = this.resolveWorkspaceRoot(DEFAULT_PERMISSION_RULES);
    const defaultResult = this.evaluateRules(
      resolvedDefaults,
      toolName,
      args,
      context,
      "default",
    );
    if (defaultResult) return defaultResult;

    // ─── Level 4: Fallback ───────────────────────────
    return {
      permitted: false,
      reason: "No matching permission rule found",
      action: "ask",
    };
  }

  /**
   * 推送审批请求
   * TUI 层订阅 pendingApprovals$ 展示审批对话框
   */
  requestApproval(request: ToolApprovalRequest): void {
    // 获取当前列表, 追加新请求, 推送更新
    // 简化实现: 直接推送包含新请求的数组
    this.pendingApprovals$.next([request]);
  }

  // ─── 内部方法 ────────────────────────────────────────

  /**
   * Level 1: 受保护文件检查
   * 从工具参数提取文件路径, 检查是否受保护
   */
  private checkGuardedFiles(
    toolName: string,
    args: Record<string, unknown>,
    settings: Settings,
  ): PermissionCheckResult | null {
    const allowlist = settings["guardedFiles.allowlist"];

    // guardedFiles 未配置 → 跳过
    if (!allowlist) return null;

    const filePaths = getToolFilePaths(toolName, args);
    if (filePaths.length === 0) return null;

    // 检查是否有任何文件受保护
    for (const filePath of filePaths) {
      if (checkGuardedFile(filePath, allowlist)) {
        return {
          permitted: false,
          reason: `Guarded file: ${filePath}`,
          action: "ask",
        };
      }
    }

    return null;
  }

  /**
   * 按顺序评估规则列表, 返回第一个匹配规则的结果
   */
  private evaluateRules(
    rules: PermissionEntry[],
    toolName: string,
    args: Record<string, unknown>,
    context: PermissionContext,
    source: string,
  ): PermissionCheckResult | null {
    for (const rule of rules) {
      // 上下文过滤: 如果规则指定了 context, 仅在匹配时生效
      if (rule.context && rule.context !== context) {
        continue;
      }

      if (matchPermissionEntry(rule, toolName, args)) {
        return this.ruleToResult(rule, source);
      }
    }
    return null;
  }

  /**
   * 将匹配的规则转换为 PermissionCheckResult
   */
  private ruleToResult(
    rule: PermissionEntry,
    source: string,
  ): PermissionCheckResult {
    if (rule.action === "allow") {
      return {
        permitted: true,
        reason: `Allowed by ${source} rule: ${rule.tool}`,
        action: "ask", // action 字段在 permitted=true 时语义不重要, schema 要求非空
        matchedEntry: rule,
        source,
      };
    }

    if (rule.action === "reject") {
      return {
        permitted: false,
        reason: rule.message ?? `Rejected by ${source} rule: ${rule.tool}`,
        action: "reject",
        matchedEntry: rule,
        source,
      };
    }

    if (rule.action === "delegate") {
      return {
        permitted: false,
        reason: `Delegated by ${source} rule: ${rule.tool} → ${rule.to}`,
        action: "delegate",
        matchedEntry: rule,
        source,
      };
    }

    // action === "ask"
    return {
      permitted: false,
      reason: `Requires approval by ${source} rule: ${rule.tool}`,
      action: "ask",
      matchedEntry: rule,
      source,
    };
  }

  /**
   * 替换默认规则中的 ${workspaceRoot} 占位符
   */
  private resolveWorkspaceRoot(rules: PermissionEntry[]): PermissionEntry[] {
    return rules.map((rule) => {
      if (!rule.matches) return rule;

      const resolvedMatches: Record<string, unknown> = {};
      for (const [key, value] of Object.entries(rule.matches)) {
        if (typeof value === "string" && value.includes("${workspaceRoot}")) {
          resolvedMatches[key] = value.replace(
            /\$\{workspaceRoot\}/g,
            this.workspaceRoot,
          );
        } else {
          resolvedMatches[key] = value;
        }
      }

      return { ...rule, matches: resolvedMatches };
    });
  }
}
```

### 3. 测试策略

使用 `node:test` 的 `describe/it` + `node:assert/strict`。使用 mock Subject 和 mock Config。

**engine.test.ts** (~25 tests):

**测试辅助:**

```ts
import { describe, it, beforeEach } from "node:test";
import assert from "node:assert/strict";
import type { Config, Settings, ToolApprovalRequest } from "@flitter/schemas";
import type { Subject } from "@flitter/util";
import { PermissionEngine, DEFAULT_PERMISSION_RULES } from "./engine";

/** 创建最小 Settings mock */
function createMockSettings(overrides?: Partial<Settings>): Settings {
  return {
    ...overrides,
  } as Settings;
}

/** 创建最小 Config mock */
function createMockConfig(settings?: Partial<Settings>): Config {
  return {
    settings: createMockSettings(settings),
    secrets: {
      getToken: async () => undefined,
      isSet: () => false,
    },
  };
}

/** 创建 mock Subject */
function createMockSubject<T>(): Subject<T> & { lastValue: T | undefined } {
  const mock = {
    lastValue: undefined as T | undefined,
    next(value: T) { mock.lastValue = value; },
    subscribe: () => ({ unsubscribe: () => {} }),
    // ... minimal Subject interface
  } as any;
  return mock;
}

function createEngine(opts?: {
  settings?: Partial<Settings>;
  workspaceRoot?: string;
}): { engine: PermissionEngine; approvals$: ReturnType<typeof createMockSubject<ToolApprovalRequest[]>> } {
  const approvals$ = createMockSubject<ToolApprovalRequest[]>();
  const config = createMockConfig(opts?.settings);
  const engine = new PermissionEngine({
    getConfig: () => config,
    pendingApprovals$: approvals$,
    workspaceRoot: opts?.workspaceRoot ?? "/workspace",
  });
  return { engine, approvals$ };
}
```

```
describe("PermissionEngine")
├── describe("getToolFilePaths")
│   ├── it("Read: 提取 file_path")
│   ├── it("Write: 提取 file_path")
│   ├── it("Edit: 提取 file_path")
│   ├── it("Bash: 从 command 提取绝对路径")
│   ├── it("Bash: command 无路径返回空数组")
│   ├── it("Grep: 提取 path 参数")
│   └── it("未知工具: 返回空数组")
├── describe("checkGuardedFile")
│   ├── it("文件不在 allowlist 中 → 受保护 (true)")
│   ├── it("文件匹配 allowlist glob → 不受保护 (false)")
│   └── it("空 allowlist → 受保护 (true)")
├── describe("checkPermission — Level 1: Guarded Files")
│   ├── it("受保护文件触发 ask + 'guarded file' reason")
│   └── it("allowlist 中的文件绕过检查")
├── describe("checkPermission — Level 2: User Rules")
│   ├── it("用户 allow 规则: 匹配时 permitted=true")
│   ├── it("用户 reject 规则: 匹配时 permitted=false, action=reject")
│   ├── it("用户 ask 规则: 匹配时 permitted=false, action=ask")
│   ├── it("第一个匹配的规则生效 (顺序优先)")
│   └── it("用户规则优先于默认规则")
├── describe("checkPermission — Level 3: Default Rules")
│   ├── it("Read 工具默认允许")
│   ├── it("Grep 工具默认允许")
│   ├── it("Glob 工具默认允许")
│   ├── it("FuzzyFind 工具默认允许")
│   ├── it("TodoWrite 工具默认允许")
│   ├── it("Write 在 workspace 内默认允许")
│   ├── it("Bash 默认需要确认 (ask)")
│   ├── it("mcp__* 工具默认需要确认 (ask)")
│   └── it("Task 工具默认允许")
├── describe("checkPermission — Level 4: Fallback")
│   └── it("无规则匹配时返回 ask")
├── describe("checkPermission — Context Filtering")
│   ├── it("规则 context='subagent' 在 thread 上下文中不生效")
│   └── it("规则 context='subagent' 在 subagent 上下文中生效")
├── describe("requestApproval")
│   └── it("推送到 pendingApprovals$ Subject")
└── describe("DEFAULT_PERMISSION_RULES")
    └── it("包含预期的规则集合")
```

预期测试数: ~25 (充裕覆盖, 最低要求 ~20)

<tasks>

<task type="auto">
  <name>Task 1: 实现受保护文件检查</name>
  <files>packages/agent-core/src/permissions/guarded-files.ts</files>
  <read_first>
    - packages/agent-core/src/permissions/matcher.ts
    - packages/schemas/src/permissions.ts
    - packages/schemas/src/config.ts
    - packages/agent-core/package.json
  </read_first>
  <action>
创建 `packages/agent-core/src/permissions/guarded-files.ts`:

1. 导入 `path` 从 `node:path`
2. 导入 `matchToolPattern` 从 `./matcher`
3. 导出 `getToolFilePaths(toolName, args)`:
   - switch 语句按工具名提取路径:
     - Read/Write/Edit → args.file_path (验证绝对路径)
     - Grep/Glob/FuzzyFind → args.path
     - Bash → 从 args.command 正则提取以 / 开头的路径 token
     - 默认 → 空数组
   - 返回 string[]
4. 导出 `checkGuardedFile(filePath, allowlist)`:
   - 如果 allowlist 为空 → 返回 true (受保护)
   - 如果 filePath 匹配 allowlist 任一模式 (matchToolPattern) → 返回 false (不受保护)
   - 否则 → 返回 true (受保护)
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - getToolFilePaths 正确提取 Read/Write/Edit/Bash/Grep 的文件路径
    - checkGuardedFile 正确使用 glob 匹配
    - tsc 通过
  </acceptance_criteria>
  <done>受保护文件检查实现完成</done>
</task>

<task type="auto">
  <name>Task 2: 实现权限执行引擎</name>
  <files>packages/agent-core/src/permissions/engine.ts</files>
  <read_first>
    - packages/agent-core/src/permissions/matcher.ts
    - packages/agent-core/src/permissions/guarded-files.ts
    - packages/schemas/src/permissions.ts
    - packages/schemas/src/config.ts
    - packages/util/src/reactive/subject.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/permissions/engine.ts`:

1. 导入依赖:
   - PermissionEntry, PermissionAction, PermissionContext, PermissionCheckResult, ToolApprovalRequest 从 @flitter/schemas
   - Config, Settings 从 @flitter/schemas
   - Subject 从 @flitter/util
   - matchPermissionEntry, matchToolPattern 从 ./matcher
   - getToolFilePaths, checkGuardedFile 从 ./guarded-files

2. 导出 `DEFAULT_PERMISSION_RULES: PermissionEntry[]`:
   - Read, Grep, Glob, FuzzyFind, TodoWrite → allow
   - Write, Edit → allow + matches: { file_path: "${workspaceRoot}/**" }
   - Bash → ask
   - mcp__* → ask
   - Task → allow

3. 导出 `PermissionEngineOpts` 接口:
   - getConfig: () => Config
   - pendingApprovals$: Subject<ToolApprovalRequest[]>
   - workspaceRoot: string

4. 导出 `PermissionEngine` 类:
   a. constructor(opts: PermissionEngineOpts)
   b. checkPermission(toolName, args, context?):
      - Level 1: checkGuardedFiles → 受保护文件 → ask
      - Level 2: evaluateRules(userRules) → 第一个匹配
      - Level 3: evaluateRules(resolvedDefaults) → 第一个匹配
      - Level 4: fallback → { permitted: false, action: "ask" }
   c. requestApproval(request): 推送到 pendingApprovals$
   d. private checkGuardedFiles(): 提取路径 + 检查 allowlist
   e. private evaluateRules(): 遍历规则 + context 过滤 + matchPermissionEntry
   f. private ruleToResult(): allow/reject/ask/delegate → PermissionCheckResult
   g. private resolveWorkspaceRoot(): 替换 ${workspaceRoot} 占位符
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PermissionEngine 实现四级决策
    - DEFAULT_PERMISSION_RULES 包含所有内置规则
    - ${workspaceRoot} 占位符被正确替换
    - context 过滤正确 (规则 context 存在时需匹配)
    - tsc 通过
  </acceptance_criteria>
  <done>权限执行引擎实现完成</done>
</task>

<task type="auto">
  <name>Task 3: 编写权限引擎测试</name>
  <files>packages/agent-core/src/permissions/engine.test.ts</files>
  <read_first>
    - packages/agent-core/src/permissions/engine.ts
    - packages/agent-core/src/permissions/guarded-files.ts
    - packages/agent-core/src/permissions/matcher.ts
    - packages/schemas/src/permissions.ts
    - packages/schemas/src/config.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/permissions/engine.test.ts`:

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

1. 创建辅助函数:
   - createMockSettings(overrides?): Settings
   - createMockConfig(settings?): Config
   - createMockSubject<T>(): mock Subject
   - createEngine(opts?): { engine, approvals$ }

2. getToolFilePaths 组 (~7 tests):
   - Read/Write/Edit 提取 file_path
   - Bash 从 command 提取绝对路径
   - Bash command 无路径返回空数组
   - Grep 提取 path
   - 未知工具返回空数组

3. checkGuardedFile 组 (~3 tests):
   - 不在 allowlist → 受保护
   - 匹配 allowlist → 不受保护
   - 空 allowlist → 受保护

4. Level 1 - Guarded Files 组 (~2 tests):
   - 受保护文件触发 ask
   - allowlist 绕过

5. Level 2 - User Rules 组 (~5 tests):
   - allow 规则
   - reject 规则
   - ask 规则
   - 顺序优先
   - 优先于默认规则

6. Level 3 - Default Rules 组 (~9 tests):
   - Read/Grep/Glob/FuzzyFind/TodoWrite 允许
   - Write workspace 内允许
   - Bash ask
   - mcp__* ask
   - Task 允许

7. Level 4 - Fallback 组 (~1 test):
   - 无匹配 → ask

8. Context Filtering 组 (~2 tests):
   - subagent 规则在 thread 中不生效
   - subagent 规则在 subagent 中生效

9. requestApproval 组 (~1 test):
   - 推送到 Subject

~25 个测试用例 (最低要求 ~20)
  </action>
  <verify>
    <automated>bun test packages/agent-core/src/permissions/engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 20 个测试用例
    - 覆盖四级决策的所有层级
    - 覆盖 context 过滤
    - 覆盖 getToolFilePaths 和 checkGuardedFile
    - 覆盖 requestApproval
    - 所有测试通过
  </acceptance_criteria>
  <done>权限引擎测试全部通过</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit && bun test packages/agent-core/src/permissions/engine.test.ts
```
</verification>

<success_criteria>
- `tsc --noEmit` 零错误
- 所有测试通过 (>= 20 个)
- PermissionEngine: 四级决策 (guarded → user → default → fallback) 正确实现
- Level 1: getToolFilePaths 从 Read/Write/Edit/Bash/Grep 正确提取路径
- Level 1: checkGuardedFile 使用 glob 匹配 allowlist
- Level 2: 用户规则按顺序评估, 第一个匹配生效
- Level 3: DEFAULT_PERMISSION_RULES 覆盖 Read/Grep/Glob/FuzzyFind/TodoWrite/Write/Edit/Bash/mcp__*/Task
- Level 3: ${workspaceRoot} 占位符被替换为实际路径
- Level 4: Fallback 返回 ask
- Context 过滤: 规则 context 存在时仅在匹配上下文中生效
- requestApproval: 正确推送到 pendingApprovals$ Subject
- 零 `any` 类型 (mock 除外)
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-core/10-07-SUMMARY.md`
</output>
