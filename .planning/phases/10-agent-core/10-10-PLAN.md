---
phase: 10-agent-core
plan: 10
type: tdd
wave: 5
depends_on: [10-01, 10-02, 10-07, 10-09]
files_modified:
  - packages/agent-core/src/subagent/hooks.ts
  - packages/agent-core/src/subagent/subagent.ts
  - packages/agent-core/src/subagent/hooks.test.ts
  - packages/agent-core/src/subagent/subagent.test.ts
autonomous: true
requirements: [AGNT-09, AGNT-10, AGNT-11]
user_setup: []

must_haves:
  truths:
    - "HookType 为 'PreToolUse' | 'PostToolUse' | 'Notification'"
    - "HookConfig 接口包含 type (HookType), matcher? (string), command (string), timeout? (number, 默认 60000)"
    - "HookResult 接口包含 abort? (boolean), modifiedArgs? (Record<string, unknown>), message? (string), exitCode (number), stdout (string), stderr (string)"
    - "parseHooksConfig(hooks) 从 Settings.hooks 解析并返回 HookConfig[]"
    - "parseHooksConfig 跳过无效条目而不抛出错误"
    - "parseHooksConfig 空输入返回空数组"
    - "matchHookToTool(hook, toolName) 无 matcher 时匹配所有工具"
    - "matchHookToTool(hook, toolName) 有 matcher 时使用 glob 匹配工具名"
    - "executePreHook(hookConfig, context) 执行 hook 命令为子进程"
    - "executePreHook 通过环境变量传递 TOOL_NAME 和 TOOL_INPUT (JSON)"
    - "executePreHook 解析 stdout 中的 JSON 控制指令 (abort, modifiedArgs)"
    - "executePreHook 返回 HookResult"
    - "executePostHook(hookConfig, context) 通过环境变量传递 TOOL_NAME, TOOL_INPUT, TOOL_RESULT"
    - "executePostHook 返回 HookResult"
    - "executePreHook 和 executePostHook 在超时时终止子进程并返回非零 exitCode"
    - "SubAgentOptions 接口包含 parentThreadId, description, prompt, type, model?, maxTurns?, timeout?"
    - "SubAgentResult 接口包含 threadId, response, status ('completed' | 'timeout' | 'cancelled' | 'error'), error?"
    - "SubAgentManager.activeAgents$ 为 BehaviorSubject<Map<string, SubAgentInfo>>"
    - "SubAgentManager.spawn(opts) 创建子线程、创建 ThreadWorker、运行推理循环、返回 SubAgentResult"
    - "SubAgentManager.spawn 设置 PermissionContext 为 'subagent'"
    - "SubAgentManager.spawn 在超时时取消推理并返回 timeout 状态"
    - "SubAgentManager.spawn 在达到 maxTurns 时停止推理"
    - "SubAgentManager.cancel(threadId) 取消指定子代理"
    - "SubAgentManager.cancelAll() 取消所有运行中的子代理"
    - "SubAgentManager.dispose() 调用 cancelAll 并清理资源"
    - "子代理不能嵌套 spawn (深度限制 = 1)"
    - "子代理共享 ThreadStore 但拥有独立 ToolOrchestrator"
    - "SubAgentInfo 包含 threadId, parentThreadId, description, type, status, startTime"
  artifacts:
    - "packages/agent-core/src/subagent/hooks.ts"
    - "packages/agent-core/src/subagent/subagent.ts"
    - "packages/agent-core/src/subagent/hooks.test.ts"
    - "packages/agent-core/src/subagent/subagent.test.ts"
  key_links:
    - "matchToolPattern 从 Plan 10-06 的 matcher.ts 导入 (hook glob 匹配复用)"
    - "ToolSpec, ToolResult, ToolContext 从 Plan 10-01 的 types.ts 导入"
    - "ToolOrchestrator 从 Plan 10-02 的 orchestrator.ts 导入"
    - "ToolRegistry 从 Plan 10-01 的 registry.ts 导入"
    - "PermissionEngine 从 Plan 10-07 的 engine.ts 导入"
    - "ThreadWorker, ThreadWorkerOptions 从 Plan 10-09 的 thread-worker.ts 导入"
    - "Settings, Config, PermissionContext 从 @flitter/schemas 导入"
    - "BehaviorSubject, Subject, Observable 从 @flitter/util 导入"
    - "ThreadStore, ConfigService 从 @flitter/data 导入"
    - "逆向对应: u7R (executePreHook), y7R (executePostHook) — tool-execution-engine.js hook 执行"
    - "逆向对应: skills-agents-system.js 子代理生命周期 (KD-36)"
---

<objective>
实现子代理框架与 Hook 系统: Sub-agent spawn/cancel 生命周期管理, PreToolUse/PostToolUse/Notification hook 配置解析与子进程执行。

Purpose: 为 Agent 提供任务委派 (子代理) 和工具执行钩子 (hook) 两大扩展能力
Output: `packages/agent-core/src/subagent/` 下 2 个源文件 + 2 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-core/CONTEXT.md
@.planning/phases/10-agent-core/10-01-PLAN.md
@.planning/phases/10-agent-core/10-02-PLAN.md
@.planning/phases/10-agent-core/10-06-PLAN.md
@.planning/phases/10-agent-core/10-07-PLAN.md
@.planning/phases/10-agent-core/10-09-PLAN.md
@packages/agent-core/package.json
@packages/agent-core/src/tools/types.ts
@packages/agent-core/src/tools/registry.ts
@packages/agent-core/src/tools/orchestrator.ts
@packages/agent-core/src/permissions/matcher.ts
@packages/agent-core/src/permissions/engine.ts
@packages/agent-core/src/worker/thread-worker.ts
@packages/agent-core/src/worker/events.ts
@packages/schemas/src/config.ts
@packages/schemas/src/permissions.ts
@packages/data/src/thread/thread-store.ts
@packages/data/src/config/config-service.ts
@packages/util/src/reactive/behavior-subject.ts
@packages/util/src/reactive/subject.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `u7R` | tool-execution-engine.js | ~1500-1560 | executePreHook: pre-tool hook 子进程执行 |
| `y7R` | tool-execution-engine.js | ~1570-1630 | executePostHook: post-tool hook 子进程执行 |
| (内联) | tool-execution-engine.js | ~1450-1490 | matchHookToTool: hook matcher glob 匹配 |
| (内联) | tool-execution-engine.js | ~1640-1680 | parseHooksConfig: Settings.hooks 解析 |
| (子代理) | skills-agents-system.js | ~2800-3100 | SubAgentManager: spawn/cancel 生命周期 |
| (子代理) | skills-agents-system.js | ~3100-3200 | SubAgentInfo 状态追踪, activeAgents$ |
| (子代理) | skills-agents-system.js | ~2900-2950 | 子代理 ThreadWorker 创建 + 独立 ToolOrchestrator |
| (子代理) | skills-agents-system.js | ~2960-3000 | maxTurns 限制 + timeout 取消 |

## 实现细节

### 1. hooks.ts -- Hook 系统

```ts
import { matchToolPattern } from "../permissions/matcher";
import { spawn } from "node:child_process";
import type { ToolResult } from "../tools/types";

// ─── Hook 类型 ──────────────────────────────────────────

/**
 * Hook 类型
 * - PreToolUse: 工具执行前触发, 可中止或修改参数
 * - PostToolUse: 工具执行后触发, 可观察结果
 * - Notification: 通知类 hook (如工具完成通知)
 */
export type HookType = "PreToolUse" | "PostToolUse" | "Notification";

// ─── Hook 配置 ──────────────────────────────────────────

/**
 * 单个 Hook 配置项
 * 从 Settings.hooks 解析而来
 */
export interface HookConfig {
  /** Hook 类型 */
  type: HookType;
  /** Glob 模式匹配工具名 (无 matcher → 匹配所有工具) */
  matcher?: string;
  /** 要执行的 Shell 命令 */
  command: string;
  /** 超时时间 (ms), 默认 60000 */
  timeout?: number;
}

// ─── Hook 结果 ──────────────────────────────────────────

/**
 * Hook 执行结果
 */
export interface HookResult {
  /** 是否中止工具调用 (仅 PreToolUse 有效) */
  abort?: boolean;
  /** 修改后的工具参数 (仅 PreToolUse 有效) */
  modifiedArgs?: Record<string, unknown>;
  /** 显示给用户的消息 */
  message?: string;
  /** Hook 命令的退出码 */
  exitCode: number;
  /** Hook 命令的 stdout */
  stdout: string;
  /** Hook 命令的 stderr */
  stderr: string;
}

// ─── Pre-hook 执行上下文 ────────────────────────────────

export interface PreHookContext {
  threadId: string;
  toolUse: {
    name: string;
    input: Record<string, unknown>;
  };
}

// ─── Post-hook 执行上下文 ───────────────────────────────

export interface PostHookContext {
  threadId: string;
  toolUse: {
    name: string;
    input: Record<string, unknown>;
  };
  result: ToolResult;
}

// ─── 默认超时 ───────────────────────────────────────────

const DEFAULT_HOOK_TIMEOUT = 60_000;

// ─── 有效 Hook 类型集合 ────────────────────────────────

const VALID_HOOK_TYPES = new Set<string>(["PreToolUse", "PostToolUse", "Notification"]);

// ─── parseHooksConfig ───────────────────────────────────

/**
 * 从 Settings.hooks 解析 Hook 配置列表
 * 逆向: tool-execution-engine.js ~1640-1680
 *
 * Settings.hooks 格式:
 * {
 *   "PreToolUse": [{ matcher: "Bash", command: "echo pre" }],
 *   "PostToolUse": [{ command: "echo post" }],
 *   "Notification": [{ command: "notify-send done" }]
 * }
 *
 * 跳过无效条目 (缺少 command, 未知 type 等), 不抛出错误
 *
 * @param hooks - Settings.hooks 的原始值
 * @returns 解析后的 HookConfig[]
 */
export function parseHooksConfig(
  hooks: Record<string, unknown>,
): HookConfig[] {
  if (!hooks || typeof hooks !== "object") return [];

  const result: HookConfig[] = [];

  for (const [type, entries] of Object.entries(hooks)) {
    if (!VALID_HOOK_TYPES.has(type)) continue;
    if (!Array.isArray(entries)) continue;

    for (const entry of entries) {
      if (!entry || typeof entry !== "object") continue;

      const raw = entry as Record<string, unknown>;
      const command = raw.command;
      if (typeof command !== "string" || command.trim() === "") continue;

      const config: HookConfig = {
        type: type as HookType,
        command,
      };

      if (typeof raw.matcher === "string" && raw.matcher.trim() !== "") {
        config.matcher = raw.matcher;
      }

      if (typeof raw.timeout === "number" && raw.timeout > 0) {
        config.timeout = raw.timeout;
      }

      result.push(config);
    }
  }

  return result;
}

// ─── matchHookToTool ────────────────────────────────────

/**
 * 检查 hook 是否匹配指定工具
 * 逆向: tool-execution-engine.js ~1450-1490
 *
 * - 无 matcher → 匹配所有工具
 * - 有 matcher → 复用 matchToolPattern (glob 匹配)
 *
 * @param hook - Hook 配置
 * @param toolName - 工具名
 * @returns true 如果匹配
 */
export function matchHookToTool(
  hook: HookConfig,
  toolName: string,
): boolean {
  if (!hook.matcher) return true;
  return matchToolPattern(hook.matcher, toolName);
}

// ─── executePreHook ─────────────────────────────────────

/**
 * 执行 PreToolUse Hook
 * 逆向: u7R (tool-execution-engine.js ~1500-1560)
 *
 * 流程:
 * 1. 检查 hook matcher 是否匹配 toolName
 * 2. 通过环境变量传递工具信息: TOOL_NAME, TOOL_INPUT (JSON)
 * 3. 执行 hook 命令为子进程 (sh -c)
 * 4. 解析 stdout 为 JSON, 提取控制指令 (abort, modifiedArgs)
 * 5. 超时时杀死子进程, 返回非零 exitCode
 *
 * @param hookConfig - Hook 配置
 * @param context - Pre-hook 执行上下文
 * @returns HookResult
 */
export async function executePreHook(
  hookConfig: HookConfig,
  context: PreHookContext,
): Promise<HookResult> {
  const env: Record<string, string> = {
    ...process.env as Record<string, string>,
    TOOL_NAME: context.toolUse.name,
    TOOL_INPUT: JSON.stringify(context.toolUse.input),
  };

  const timeout = hookConfig.timeout ?? DEFAULT_HOOK_TIMEOUT;
  const { exitCode, stdout, stderr } = await runCommand(hookConfig.command, env, timeout);

  // 解析 stdout 中的 JSON 控制指令
  const directives = parseDirectives(stdout);

  return {
    abort: directives.abort,
    modifiedArgs: directives.modifiedArgs,
    message: directives.message,
    exitCode,
    stdout,
    stderr,
  };
}

// ─── executePostHook ────────────────────────────────────

/**
 * 执行 PostToolUse Hook
 * 逆向: y7R (tool-execution-engine.js ~1570-1630)
 *
 * 流程:
 * 1. 通过环境变量传递: TOOL_NAME, TOOL_INPUT (JSON), TOOL_RESULT (JSON)
 * 2. 执行 hook 命令为子进程
 * 3. 超时时杀死子进程
 *
 * @param hookConfig - Hook 配置
 * @param context - Post-hook 执行上下文
 * @returns HookResult
 */
export async function executePostHook(
  hookConfig: HookConfig,
  context: PostHookContext,
): Promise<HookResult> {
  const env: Record<string, string> = {
    ...process.env as Record<string, string>,
    TOOL_NAME: context.toolUse.name,
    TOOL_INPUT: JSON.stringify(context.toolUse.input),
    TOOL_RESULT: JSON.stringify(context.result),
  };

  const timeout = hookConfig.timeout ?? DEFAULT_HOOK_TIMEOUT;
  const { exitCode, stdout, stderr } = await runCommand(hookConfig.command, env, timeout);

  // Post-hooks 也可能输出消息
  const directives = parseDirectives(stdout);

  return {
    message: directives.message,
    exitCode,
    stdout,
    stderr,
  };
}

// ─── 内部: runCommand ───────────────────────────────────

/**
 * 执行 shell 命令, 收集 stdout/stderr, 支持超时
 */
function runCommand(
  command: string,
  env: Record<string, string>,
  timeout: number,
): Promise<{ exitCode: number; stdout: string; stderr: string }> {
  return new Promise((resolve) => {
    const child = spawn("sh", ["-c", command], {
      env,
      stdio: ["ignore", "pipe", "pipe"],
    });

    let stdout = "";
    let stderr = "";
    let killed = false;

    child.stdout.on("data", (data: Buffer) => {
      stdout += data.toString();
    });

    child.stderr.on("data", (data: Buffer) => {
      stderr += data.toString();
    });

    const timer = setTimeout(() => {
      killed = true;
      child.kill("SIGKILL");
    }, timeout);

    child.on("close", (code) => {
      clearTimeout(timer);
      resolve({
        exitCode: killed ? 124 : (code ?? 1),
        stdout,
        stderr,
      });
    });

    child.on("error", (err) => {
      clearTimeout(timer);
      resolve({
        exitCode: 1,
        stdout,
        stderr: stderr + (stderr ? "\n" : "") + err.message,
      });
    });
  });
}

// ─── 内部: parseDirectives ──────────────────────────────

/**
 * 从 stdout 解析 JSON 控制指令
 * 尝试将整个 stdout 解析为 JSON, 提取已知字段
 * 解析失败时返回空指令 (不抛出错误)
 */
function parseDirectives(stdout: string): {
  abort?: boolean;
  modifiedArgs?: Record<string, unknown>;
  message?: string;
} {
  const trimmed = stdout.trim();
  if (!trimmed) return {};

  try {
    const parsed = JSON.parse(trimmed);
    if (typeof parsed !== "object" || parsed === null) return {};

    const result: {
      abort?: boolean;
      modifiedArgs?: Record<string, unknown>;
      message?: string;
    } = {};

    if (typeof parsed.abort === "boolean") {
      result.abort = parsed.abort;
    }

    if (
      parsed.modifiedArgs &&
      typeof parsed.modifiedArgs === "object" &&
      !Array.isArray(parsed.modifiedArgs)
    ) {
      result.modifiedArgs = parsed.modifiedArgs;
    }

    if (typeof parsed.message === "string") {
      result.message = parsed.message;
    }

    return result;
  } catch {
    return {};
  }
}
```

### 2. subagent.ts -- 子代理框架

```ts
import type { BehaviorSubject } from "@flitter/util";
import type { ThreadStore, ConfigService } from "@flitter/data";
import type { ThreadWorker } from "../worker/thread-worker";
import type { PermissionContext } from "@flitter/schemas";

// ─── 子代理选项 ─────────────────────────────────────────

/**
 * 创建子代理的选项
 */
export interface SubAgentOptions {
  /** 父线程 ID */
  parentThreadId: string;
  /** 任务描述 (3-5 word summary) */
  description: string;
  /** 任务提示词 (详细指令) */
  prompt: string;
  /** 子代理类型 (决定可用工具集和能力范围) */
  type: string;
  /** 模型覆盖 (不指定时使用父线程配置) */
  model?: string;
  /** 最大推理轮次 (防止无限循环) */
  maxTurns?: number;
  /** 超时时间 (ms) */
  timeout?: number;
}

// ─── 子代理结果 ─────────────────────────────────────────

/**
 * 子代理执行结果
 */
export interface SubAgentResult {
  /** 子代理线程 ID */
  threadId: string;
  /** 最终响应文本 (从最后一条 assistant 消息提取) */
  response: string;
  /** 完成状态 */
  status: "completed" | "timeout" | "cancelled" | "error";
  /** 错误信息 (status 为 "error" 时) */
  error?: string;
}

// ─── 子代理状态信息 ─────────────────────────────────────

/**
 * 运行中子代理的状态信息
 * 用于 activeAgents$ 追踪
 */
export interface SubAgentInfo {
  /** 子代理线程 ID */
  threadId: string;
  /** 父线程 ID */
  parentThreadId: string;
  /** 任务描述 */
  description: string;
  /** 子代理类型 */
  type: string;
  /** 当前状态 */
  status: "running" | "completed" | "cancelled" | "timeout" | "error";
  /** 启动时间 (Date.now()) */
  startTime: number;
}

// ─── ThreadWorker 创建选项 ──────────────────────────────

/**
 * 创建子代理 ThreadWorker 的选项
 * 由 SubAgentManager 的 createWorker 回调使用
 */
export interface SubAgentWorkerOptions {
  /** 子代理线程 ID */
  threadId: string;
  /** 父线程 ID */
  parentThreadId: string;
  /** 子代理类型 */
  type: string;
  /** 模型覆盖 */
  model?: string;
  /** 权限上下文 (子代理固定为 "subagent") */
  permissionContext: PermissionContext;
}

// ─── SubAgentManager 选项 ───────────────────────────────

export interface SubAgentManagerOptions {
  /** 创建子代理 ThreadWorker 的工厂回调 */
  createWorker: (opts: SubAgentWorkerOptions) => ThreadWorker;
  /** 线程存储 (创建子线程) */
  threadStore: ThreadStore;
  /** 配置服务 (获取默认配置) */
  configService: ConfigService;
}

// ─── 默认值 ─────────────────────────────────────────────

const DEFAULT_MAX_TURNS = 20;
const DEFAULT_TIMEOUT = 300_000; // 5 分钟

// ─── SubAgentManager 类 ────────────────────────────────

/**
 * 子代理管理器: 管理子代理的生命周期
 * 逆向: skills-agents-system.js ~2800-3200
 *
 * 核心设计 (KD-36):
 * - 子代理共享 ThreadStore (父线程可见子代理进度)
 * - 子代理拥有独立 ToolOrchestrator (隔离工具执行)
 * - 权限上下文为 "subagent" (可能有更严格的限制)
 * - 深度限制 = 1 (子代理不能再 spawn 子代理)
 * - 超时可取消 (取消推理 + 取消运行中的工具)
 *
 * Spawn 流程:
 * 1. 在 ThreadStore 创建子线程 (关联父线程)
 * 2. 通过 createWorker 回调创建 ThreadWorker
 *    - 独立 ToolOrchestrator
 *    - permissionContext: "subagent"
 *    - 系统提示词包含子代理修饰符 + 任务描述
 * 3. 添加初始 user 消息 (prompt)
 * 4. 运行推理循环 (受 maxTurns 限制)
 * 5. 完成: 从最后一条 assistant 消息提取响应
 * 6. 超时: 取消推理, 返回 timeout 状态
 * 7. 返回 SubAgentResult
 */
export class SubAgentManager {
  /** 活跃子代理追踪 (BehaviorSubject) */
  readonly activeAgents$: BehaviorSubject<Map<string, SubAgentInfo>>;

  private readonly createWorker: (opts: SubAgentWorkerOptions) => ThreadWorker;
  private readonly threadStore: ThreadStore;
  private readonly configService: ConfigService;
  private readonly runningWorkers: Map<string, { worker: ThreadWorker; abort: AbortController }> =
    new Map();
  private disposed = false;

  constructor(opts: SubAgentManagerOptions) {
    this.createWorker = opts.createWorker;
    this.threadStore = opts.threadStore;
    this.configService = opts.configService;
    // 初始化为空 Map (由调用方注入 BehaviorSubject, 或在此创建)
    this.activeAgents$ = null as any; // 实际实现: new BehaviorSubject(new Map())
  }

  // ─── 公共方法 ──────────────────────────────────────

  /**
   * 创建并运行子代理
   *
   * @param opts - 子代理选项
   * @returns SubAgentResult
   */
  async spawn(opts: SubAgentOptions): Promise<SubAgentResult> {
    if (this.disposed) {
      return {
        threadId: "",
        response: "",
        status: "error",
        error: "SubAgentManager is disposed",
      };
    }

    // ─── Step 1: 创建子线程 ───────────────────────
    const threadId = this.threadStore.createChildThread(opts.parentThreadId);

    // ─── Step 2: 注册到 activeAgents$ ─────────────
    const info: SubAgentInfo = {
      threadId,
      parentThreadId: opts.parentThreadId,
      description: opts.description,
      type: opts.type,
      status: "running",
      startTime: Date.now(),
    };
    this.updateActiveAgent(threadId, info);

    // ─── Step 3: 创建 ThreadWorker ────────────────
    const worker = this.createWorker({
      threadId,
      parentThreadId: opts.parentThreadId,
      type: opts.type,
      model: opts.model,
      permissionContext: "subagent",
    });

    const abortController = new AbortController();
    this.runningWorkers.set(threadId, { worker, abort: abortController });

    // ─── Step 4: 添加初始 user 消息 ──────────────
    this.threadStore.addMessage(threadId, {
      role: "user",
      content: [{ type: "text", text: opts.prompt }],
    });

    // ─── Step 5: 设置超时 ─────────────────────────
    const timeout = opts.timeout ?? DEFAULT_TIMEOUT;
    const maxTurns = opts.maxTurns ?? DEFAULT_MAX_TURNS;

    let timeoutHandle: ReturnType<typeof setTimeout> | null = null;
    let timedOut = false;

    const timeoutPromise = new Promise<void>((resolve) => {
      timeoutHandle = setTimeout(() => {
        timedOut = true;
        worker.cancelInference();
        resolve();
      }, timeout);
    });

    // ─── Step 6: 运行推理循环 ─────────────────────
    try {
      let turn = 0;

      // 推理循环: 运行直到完成、超时、或达到 maxTurns
      const inferenceLoop = async (): Promise<void> => {
        while (turn < maxTurns && !timedOut && !abortController.signal.aborted) {
          turn++;
          await worker.runInference();

          // 检查是否有更多工具调用需要处理
          // ThreadWorker.runInference 已经处理了递归推理,
          // 这里只需要检查推理状态
          const state = worker.inferenceState$.getValue();
          if (state === "idle" || state === "cancelled") {
            break;
          }
        }
      };

      // 竞争: 推理循环 vs 超时
      await Promise.race([inferenceLoop(), timeoutPromise]);

      if (timeoutHandle) clearTimeout(timeoutHandle);

      // ─── Step 7: 确定结果 ─────────────────────
      if (timedOut) {
        this.updateActiveAgent(threadId, { ...info, status: "timeout" });
        this.cleanup(threadId);
        return {
          threadId,
          response: "",
          status: "timeout",
        };
      }

      if (abortController.signal.aborted) {
        this.updateActiveAgent(threadId, { ...info, status: "cancelled" });
        this.cleanup(threadId);
        return {
          threadId,
          response: "",
          status: "cancelled",
        };
      }

      // 提取最终响应
      const response = this.extractResponse(threadId);
      this.updateActiveAgent(threadId, { ...info, status: "completed" });
      this.cleanup(threadId);

      return {
        threadId,
        response,
        status: "completed",
      };
    } catch (error) {
      if (timeoutHandle) clearTimeout(timeoutHandle);

      const errorMessage =
        error instanceof Error ? error.message : String(error);
      this.updateActiveAgent(threadId, { ...info, status: "error" });
      this.cleanup(threadId);

      return {
        threadId,
        response: "",
        status: "error",
        error: errorMessage,
      };
    }
  }

  /**
   * 取消指定子代理
   */
  cancel(threadId: string): void {
    const entry = this.runningWorkers.get(threadId);
    if (!entry) return;

    entry.abort.abort();
    entry.worker.cancelInference();

    const agents = this.activeAgents$.getValue();
    const info = agents.get(threadId);
    if (info) {
      this.updateActiveAgent(threadId, { ...info, status: "cancelled" });
    }

    this.cleanup(threadId);
  }

  /**
   * 取消所有运行中的子代理
   */
  cancelAll(): void {
    for (const threadId of this.runningWorkers.keys()) {
      this.cancel(threadId);
    }
  }

  /**
   * 销毁管理器, 释放所有资源
   */
  dispose(): void {
    if (this.disposed) return;
    this.disposed = true;
    this.cancelAll();
  }

  // ─── 内部方法 ──────────────────────────────────────

  /**
   * 从子线程的最后一条 assistant 消息提取响应文本
   */
  private extractResponse(threadId: string): string {
    const snapshot = this.threadStore.getSnapshot(threadId);
    if (!snapshot) return "";

    const messages = snapshot.messages;
    for (let i = messages.length - 1; i >= 0; i--) {
      const msg = messages[i];
      if (msg.role === "assistant") {
        // 提取所有 text 块的内容
        const textParts: string[] = [];
        for (const block of msg.content) {
          if (block.type === "text") {
            textParts.push(block.text);
          }
        }
        return textParts.join("");
      }
    }

    return "";
  }

  /**
   * 更新 activeAgents$ 中的子代理信息
   */
  private updateActiveAgent(threadId: string, info: SubAgentInfo): void {
    const current = this.activeAgents$.getValue();
    const updated = new Map(current);
    updated.set(threadId, info);
    this.activeAgents$.next(updated);
  }

  /**
   * 清理完成/取消的子代理
   */
  private cleanup(threadId: string): void {
    const entry = this.runningWorkers.get(threadId);
    if (entry) {
      entry.worker.dispose();
      this.runningWorkers.delete(threadId);
    }
  }
}
```

### 3. 测试策略

使用 `node:test` 的 `describe/it/beforeEach/afterEach` + `node:assert/strict`。

#### hooks.test.ts (~15 tests)

**测试辅助:**

```ts
import { describe, it, beforeEach, mock } from "node:test";
import assert from "node:assert/strict";
import {
  parseHooksConfig,
  matchHookToTool,
  executePreHook,
  executePostHook,
  type HookConfig,
  type HookResult,
  type PreHookContext,
  type PostHookContext,
} from "./hooks";
import type { ToolResult } from "../tools/types";

/** 创建最小 PreHookContext */
function createPreHookContext(overrides?: Partial<PreHookContext>): PreHookContext {
  return {
    threadId: "thread-1",
    toolUse: {
      name: "Bash",
      input: { command: "echo hello" },
    },
    ...overrides,
  };
}

/** 创建最小 PostHookContext */
function createPostHookContext(overrides?: Partial<PostHookContext>): PostHookContext {
  return {
    threadId: "thread-1",
    toolUse: {
      name: "Bash",
      input: { command: "echo hello" },
    },
    result: {
      status: "success" as any,
      content: "hello",
    },
    ...overrides,
  };
}
```

```
describe("Hook System")
├── describe("parseHooksConfig")
│   ├── it("有效配置返回 HookConfig[]")
│   ├── it("空对象返回空数组")
│   ├── it("null/undefined 输入返回空数组")
│   ├── it("跳过无效条目 (缺少 command)")
│   └── it("跳过未知 hook type")
├── describe("matchHookToTool")
│   ├── it("无 matcher 匹配所有工具")
│   ├── it("精确 matcher 匹配对应工具名")
│   ├── it("glob matcher 匹配 (如 mcp__*)")
│   └── it("不匹配时返回 false")
├── describe("executePreHook")
│   ├── it("执行命令并返回 exitCode/stdout/stderr")
│   ├── it("通过环境变量传递 TOOL_NAME 和 TOOL_INPUT")
│   ├── it("解析 stdout JSON 中的 abort 指令")
│   └── it("解析 stdout JSON 中的 modifiedArgs")
├── describe("executePostHook")
│   ├── it("执行命令并通过环境变量传递 TOOL_RESULT")
│   └── it("返回 HookResult (exitCode/stdout/stderr)")
└── describe("超时处理")
    └── it("hook 超时时终止子进程并返回非零 exitCode")
```

预期测试数: ~15

#### subagent.test.ts (~15 tests)

**测试辅助:**

```ts
import { describe, it, beforeEach, afterEach, mock } from "node:test";
import assert from "node:assert/strict";
import type { ThreadStore, ConfigService } from "@flitter/data";
import type { ThreadWorker } from "../worker/thread-worker";
import type { BehaviorSubject } from "@flitter/util";
import {
  SubAgentManager,
  type SubAgentOptions,
  type SubAgentResult,
  type SubAgentInfo,
  type SubAgentWorkerOptions,
} from "./subagent";

// ─── Mock ThreadStore ─────────────────────────────────

function createMockThreadStore(): ThreadStore & {
  _threads: Map<string, { messages: any[] }>;
} {
  let nextId = 0;
  const threads = new Map<string, { messages: any[] }>();

  return {
    _threads: threads,
    createChildThread: (parentId: string) => {
      const id = `child-${nextId++}`;
      threads.set(id, { messages: [] });
      return id;
    },
    addMessage: (threadId: string, msg: any) => {
      const thread = threads.get(threadId);
      if (thread) thread.messages.push(msg);
    },
    getSnapshot: (threadId?: string) => {
      if (!threadId) return null;
      const thread = threads.get(threadId);
      if (!thread) return null;
      return { messages: thread.messages };
    },
  } as any;
}

// ─── Mock ConfigService ──────────────────────────────

function createMockConfigService(): ConfigService {
  return {
    getConfig: () => ({
      settings: { model: "test-model" },
    }),
  } as any;
}

// ─── Mock ThreadWorker ──────────────────────────────

function createMockWorker(opts?: {
  runInferenceFn?: () => Promise<void>;
  cancelInferenceFn?: () => void;
}): ThreadWorker {
  let state = "idle";
  return {
    inferenceState$: {
      getValue: () => state,
      next: (s: string) => { state = s; },
      subscribe: () => ({ unsubscribe: () => {} }),
    },
    events$: {
      next: () => {},
      subscribe: () => ({ unsubscribe: () => {} }),
    },
    runInference: opts?.runInferenceFn ?? (async () => { state = "idle"; }),
    cancelInference: opts?.cancelInferenceFn ?? (() => { state = "cancelled"; }),
    dispose: () => {},
  } as any;
}

// ─── Mock BehaviorSubject ────────────────────────────

function createMockBehaviorSubject<T>(initial: T): BehaviorSubject<T> {
  let value = initial;
  return {
    getValue: () => value,
    next: (v: T) => { value = v; },
    subscribe: () => ({ unsubscribe: () => {} }),
  } as any;
}

// ─── 辅助: 创建 SubAgentOptions ─────────────────────

function createSpawnOpts(overrides?: Partial<SubAgentOptions>): SubAgentOptions {
  return {
    parentThreadId: "parent-1",
    description: "Test task",
    prompt: "Do the thing",
    type: "general-purpose",
    ...overrides,
  };
}
```

```
describe("SubAgentManager")
├── describe("spawn")
│   ├── it("创建子线程并运行推理, 返回 completed 结果")
│   ├── it("从最后 assistant 消息提取 response")
│   ├── it("通过 createWorker 回调创建 ThreadWorker, permissionContext 为 'subagent'")
│   ├── it("超时时取消推理并返回 timeout 状态")
│   ├── it("达到 maxTurns 时停止推理")
│   ├── it("worker 错误时返回 error 状态 + error 信息")
│   └── it("disposed 后 spawn 返回 error")
├── describe("cancel")
│   ├── it("取消指定子代理, abort + cancelInference")
│   └── it("取消不存在的 threadId 不报错")
├── describe("cancelAll")
│   └── it("取消所有运行中的子代理")
├── describe("activeAgents$")
│   ├── it("spawn 时更新为 running")
│   ├── it("完成后更新为 completed")
│   └── it("取消后更新为 cancelled")
├── describe("dispose")
│   └── it("调用 cancelAll 并标记 disposed")
└── describe("嵌套限制")
    └── it("子代理不能嵌套 spawn (由 createWorker 回调控制, 不注入 SubAgentManager)")
```

预期测试数: ~15

<tasks>

<task type="auto">
  <name>Task 1: 实现 Hook 系统</name>
  <files>packages/agent-core/src/subagent/hooks.ts</files>
  <read_first>
    - packages/agent-core/src/permissions/matcher.ts
    - packages/agent-core/src/tools/types.ts
    - packages/schemas/src/config.ts
    - packages/agent-core/package.json
  </read_first>
  <action>
创建 `packages/agent-core/src/subagent/hooks.ts`:

1. 导入 `matchToolPattern` 从 `../permissions/matcher`
2. 导入 `spawn` 从 `node:child_process`
3. 导入 `ToolResult` 类型从 `../tools/types`

4. 导出类型:
   - HookType: "PreToolUse" | "PostToolUse" | "Notification"
   - HookConfig: { type, matcher?, command, timeout? }
   - HookResult: { abort?, modifiedArgs?, message?, exitCode, stdout, stderr }
   - PreHookContext: { threadId, toolUse: { name, input } }
   - PostHookContext: { threadId, toolUse: { name, input }, result: ToolResult }

5. 导出 `parseHooksConfig(hooks: Record<string, unknown>): HookConfig[]`:
   - 遍历 hooks 对象的 key (必须是有效 HookType)
   - 每个 key 对应的 value 应为数组
   - 数组每项: 提取 command (必需), matcher (可选), timeout (可选)
   - 跳过无效条目, 不抛出错误
   - 返回 HookConfig[]

6. 导出 `matchHookToTool(hook: HookConfig, toolName: string): boolean`:
   - 无 matcher → 返回 true
   - 有 matcher → 复用 matchToolPattern(hook.matcher, toolName)

7. 导出 `executePreHook(hookConfig, context): Promise<HookResult>`:
   - 设置环境变量: TOOL_NAME, TOOL_INPUT (JSON)
   - 执行 `sh -c command` 子进程
   - 收集 stdout/stderr
   - 解析 stdout JSON 中的控制指令: abort, modifiedArgs, message
   - 超时时 SIGKILL 子进程, exitCode 设为 124
   - 返回 HookResult

8. 导出 `executePostHook(hookConfig, context): Promise<HookResult>`:
   - 设置环境变量: TOOL_NAME, TOOL_INPUT (JSON), TOOL_RESULT (JSON)
   - 执行子进程, 收集输出
   - 超时处理同 executePreHook
   - 返回 HookResult

9. 内部辅助函数:
   - `runCommand(command, env, timeout)`: spawn + stdout/stderr 收集 + 超时
   - `parseDirectives(stdout)`: JSON.parse + 提取已知字段, 静默失败
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - HookType/HookConfig/HookResult 类型完整
    - parseHooksConfig 正确解析 Settings.hooks 格式
    - matchHookToTool 复用 matchToolPattern
    - executePreHook/executePostHook 通过环境变量传递工具信息
    - 超时处理正确 (SIGKILL + exitCode 124)
    - parseDirectives 静默处理非 JSON 输出
    - tsc 通过
  </acceptance_criteria>
  <done>Hook 系统实现完成</done>
</task>

<task type="auto">
  <name>Task 2: 实现子代理框架</name>
  <files>packages/agent-core/src/subagent/subagent.ts</files>
  <read_first>
    - packages/agent-core/src/worker/thread-worker.ts
    - packages/agent-core/src/worker/events.ts
    - packages/agent-core/src/tools/orchestrator.ts
    - packages/data/src/thread/thread-store.ts
    - packages/data/src/config/config-service.ts
    - packages/schemas/src/permissions.ts
    - packages/util/src/reactive/behavior-subject.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/subagent/subagent.ts`:

1. 导入依赖:
   - BehaviorSubject 从 @flitter/util
   - ThreadStore, ConfigService 从 @flitter/data
   - ThreadWorker 从 ../worker/thread-worker
   - PermissionContext 从 @flitter/schemas

2. 导出类型:
   - SubAgentOptions: { parentThreadId, description, prompt, type, model?, maxTurns?, timeout? }
   - SubAgentResult: { threadId, response, status, error? }
   - SubAgentInfo: { threadId, parentThreadId, description, type, status, startTime }
   - SubAgentWorkerOptions: { threadId, parentThreadId, type, model?, permissionContext }
   - SubAgentManagerOptions: { createWorker, threadStore, configService }

3. 导出 SubAgentManager 类:
   a. 属性:
      - activeAgents$: BehaviorSubject<Map<string, SubAgentInfo>>
      - private runningWorkers: Map<string, { worker, abort }>
      - private disposed: boolean

   b. constructor(opts: SubAgentManagerOptions):
      - 保存 createWorker, threadStore, configService
      - 初始化 activeAgents$ 为空 Map

   c. async spawn(opts: SubAgentOptions): Promise<SubAgentResult>:
      - 检查 disposed
      - createChildThread (ThreadStore)
      - 注册到 activeAgents$ (status: "running")
      - createWorker({ ..., permissionContext: "subagent" })
      - addMessage (user + prompt)
      - 设置超时 (setTimeout + cancelInference)
      - 推理循环 (maxTurns 限制)
      - Promise.race([inferenceLoop, timeout])
      - 提取 response (extractResponse)
      - 返回 SubAgentResult
      - 清理 (dispose worker + cleanup)

   d. cancel(threadId):
      - abort + cancelInference
      - updateActiveAgent (cancelled)
      - cleanup

   e. cancelAll():
      - 遍历 runningWorkers, 逐个 cancel

   f. dispose():
      - cancelAll + disposed = true

   g. private extractResponse(threadId):
      - 从 ThreadStore 获取子线程 snapshot
      - 找到最后一条 assistant 消息
      - 提取 text 块并拼接

   h. private updateActiveAgent(threadId, info):
      - 更新 activeAgents$ 的 Map

   i. private cleanup(threadId):
      - dispose worker + 移除 runningWorkers
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - SubAgentManager.spawn 创建子线程 + ThreadWorker + 推理循环
    - permissionContext 固定为 "subagent"
    - 超时取消正确 (setTimeout → cancelInference → timeout status)
    - maxTurns 限制正确
    - cancel / cancelAll / dispose 正确清理
    - activeAgents$ 状态追踪正确
    - extractResponse 从 assistant 消息提取文本
    - tsc 通过
  </acceptance_criteria>
  <done>子代理框架实现完成</done>
</task>

<task type="auto">
  <name>Task 3: 编写 Hook 系统测试</name>
  <files>packages/agent-core/src/subagent/hooks.test.ts</files>
  <read_first>
    - packages/agent-core/src/subagent/hooks.ts
    - packages/agent-core/src/permissions/matcher.ts
    - packages/agent-core/src/tools/types.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/subagent/hooks.test.ts`:

使用 `node:test` 的 `describe/it` + `node:assert/strict`。

1. 创建辅助函数:
   - createPreHookContext(overrides?): PreHookContext
   - createPostHookContext(overrides?): PostHookContext

2. parseHooksConfig 组 (~5 tests):
   - 有效配置 → 正确解析为 HookConfig[]
   - 空对象 → 空数组
   - null/undefined → 空数组
   - 跳过缺少 command 的条目
   - 跳过未知 hook type

3. matchHookToTool 组 (~4 tests):
   - 无 matcher → 匹配所有
   - 精确 matcher → 匹配
   - glob matcher (如 mcp__*) → 匹配
   - 不匹配 → false

4. executePreHook 组 (~4 tests):
   - 命令执行 → 返回 exitCode/stdout/stderr
   - TOOL_NAME/TOOL_INPUT 环境变量传递 (echo $TOOL_NAME)
   - abort 指令解析 (stdout 输出 JSON)
   - modifiedArgs 解析

5. executePostHook 组 (~2 tests):
   - TOOL_RESULT 环境变量传递
   - 返回 HookResult

6. 超时处理组 (~1 test):
   - hook 超时 → 终止 + exitCode 124 (使用 sleep 命令 + 短超时)

~15 个测试用例 (最低要求 ~12)
  </action>
  <verify>
    <automated>bun test packages/agent-core/src/subagent/hooks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 12 个测试用例
    - 覆盖 parseHooksConfig 所有路径 (有效/空/无效)
    - 覆盖 matchHookToTool (无 matcher/精确/glob/不匹配)
    - 覆盖 executePreHook (环境变量/abort/modifiedArgs)
    - 覆盖 executePostHook (环境变量/TOOL_RESULT)
    - 覆盖超时处理
    - 所有测试通过
  </acceptance_criteria>
  <done>Hook 系统测试全部通过</done>
</task>

<task type="auto">
  <name>Task 4: 编写子代理框架测试</name>
  <files>packages/agent-core/src/subagent/subagent.test.ts</files>
  <read_first>
    - packages/agent-core/src/subagent/subagent.ts
    - packages/agent-core/src/worker/thread-worker.ts
    - packages/agent-core/src/worker/events.ts
    - packages/data/src/thread/thread-store.ts
    - packages/data/src/config/config-service.ts
    - packages/util/src/reactive/behavior-subject.ts
  </read_first>
  <action>
创建 `packages/agent-core/src/subagent/subagent.test.ts`:

使用 `node:test` 的 `describe/it/beforeEach/afterEach` + `node:assert/strict`。

1. 创建辅助函数:
   - createMockThreadStore(): 支持 createChildThread/addMessage/getSnapshot
   - createMockConfigService(): 返回测试配置
   - createMockWorker(opts?): 可配置 runInference/cancelInference 行为
   - createMockBehaviorSubject(initial): 最小 BehaviorSubject mock
   - createSpawnOpts(overrides?): SubAgentOptions

2. spawn 组 (~7 tests):
   - 创建子线程, 运行推理, 返回 completed
   - 从 assistant 消息提取 response
   - createWorker 回调收到 permissionContext: "subagent"
   - 超时 → timeout 状态 (使用短超时 + 慢 runInference)
   - maxTurns 限制 → 停止推理
   - worker 抛错 → error 状态 + error 信息
   - disposed 后 spawn → error

3. cancel 组 (~2 tests):
   - 取消指定子代理 → abort + cancelInference
   - 取消不存在的 threadId → 不报错

4. cancelAll 组 (~1 test):
   - 取消所有

5. activeAgents$ 组 (~3 tests):
   - spawn 时 status: running
   - 完成后 status: completed
   - 取消后 status: cancelled

6. dispose 组 (~1 test):
   - cancelAll + disposed

7. 嵌套限制组 (~1 test):
   - 验证 createWorker 回调不注入 SubAgentManager (无法嵌套 spawn)

~15 个测试用例 (最低要求 ~12)
  </action>
  <verify>
    <automated>bun test packages/agent-core/src/subagent/subagent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - >= 12 个测试用例
    - 覆盖 spawn 完整流程 (创建 → 推理 → 结果)
    - 覆盖超时处理 (timeout → cancelled → timeout status)
    - 覆盖 maxTurns 限制
    - 覆盖 cancel/cancelAll/dispose
    - 覆盖 activeAgents$ 状态追踪
    - 覆盖嵌套限制
    - 覆盖错误处理
    - 所有测试通过
  </acceptance_criteria>
  <done>子代理框架测试全部通过</done>
</task>

</tasks>

<verification>
```bash
npx tsc --noEmit && bun test packages/agent-core/src/subagent/hooks.test.ts && bun test packages/agent-core/src/subagent/subagent.test.ts
```
</verification>

<success_criteria>
- `tsc --noEmit` 零错误
- 所有测试通过 (hooks >= 12, subagent >= 12, 总计 >= 24)
- HookType: "PreToolUse" | "PostToolUse" | "Notification"
- HookConfig: type + matcher? + command + timeout?
- HookResult: abort? + modifiedArgs? + message? + exitCode + stdout + stderr
- parseHooksConfig: 正确解析 Settings.hooks 格式, 跳过无效条目
- matchHookToTool: 无 matcher 匹配全部, 有 matcher 复用 matchToolPattern
- executePreHook: 环境变量 (TOOL_NAME, TOOL_INPUT) + stdout JSON 指令解析 (abort, modifiedArgs)
- executePostHook: 环境变量 (TOOL_NAME, TOOL_INPUT, TOOL_RESULT)
- 超时处理: SIGKILL + exitCode 124
- SubAgentOptions: parentThreadId + description + prompt + type + model? + maxTurns? + timeout?
- SubAgentResult: threadId + response + status + error?
- SubAgentManager.activeAgents$: BehaviorSubject<Map<string, SubAgentInfo>>
- SubAgentManager.spawn: 创建子线程 → ThreadWorker (permissionContext: "subagent") → 推理循环 → 提取 response
- SubAgentManager.spawn: timeout → cancelInference → timeout status
- SubAgentManager.spawn: maxTurns 限制
- SubAgentManager.cancel: abort + cancelInference + cleanup
- SubAgentManager.cancelAll: 遍历取消所有
- SubAgentManager.dispose: cancelAll + disposed
- 子代理不能嵌套 spawn (深度限制 = 1, createWorker 不注入 SubAgentManager)
- 子代理共享 ThreadStore 但拥有独立 ToolOrchestrator (通过 createWorker 回调实现)
- 零 `any` 类型 (mock 除外)
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-core/10-10-SUMMARY.md`
</output>
