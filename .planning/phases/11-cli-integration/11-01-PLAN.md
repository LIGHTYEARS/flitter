---
phase: 11-cli-integration
plan: 01
type: tdd
wave: 1
depends_on: [11-06]
files_modified:
  - packages/cli/src/program.ts
  - packages/cli/src/context.ts
  - packages/cli/src/commands/auth.ts
  - packages/cli/src/commands/threads.ts
  - packages/cli/src/commands/config.ts
  - packages/cli/src/commands/update.ts
  - packages/cli/src/index.ts
  - packages/cli/src/program.test.ts
  - packages/cli/src/context.test.ts
autonomous: true
requirements: [CLI-01]
user_setup: []

must_haves:
  truths:
    - "createProgram() 返回 Commander.js Command 实例, 配置 name='flitter', version 从 package.json 读取"
    - "全局选项: --execute/-e (布尔), --headless (布尔), --stream-json (布尔), --no-color (布尔), --verbose/-v (布尔)"
    - "默认动作 (无子命令): 根据模式判定进入 interactive 或 execute 模式"
    - "子命令 login: 触发认证流程 (Plan 11-04)"
    - "子命令 logout: 清除存储的凭据"
    - "子命令 threads: 子子命令 list/new/continue/archive/delete"
    - "子命令 threads list: 列出所有 thread (--limit, --format json|table)"
    - "子命令 threads new: 创建新 thread (--model 可选)"
    - "子命令 threads continue <id>: 继续指定 thread"
    - "子命令 config: 子子命令 get/set/list"
    - "子命令 config get <key>: 获取配置值"
    - "子命令 config set <key> <value>: 设置配置值"
    - "子命令 config list: 列出所有配置"
    - "子命令 update: 检查并安装更新 (--target-version 可选)"
    - "resolveCliContext(program) 解析命令行为 CliContext 对象"
    - "CliContext 包含 executeMode (boolean), isTTY (boolean), headless (boolean), streamJson (boolean), verbose (boolean)"
    - "模式判定逻辑: --execute 或 !stdout.isTTY → executeMode=true, 否则 interactive"
    - "命令处理器接受 (container: ServiceContainer, context: CliContext) 参数"
    - "未知命令输出帮助信息而非报错"
    - "--help 输出完整命令树含所有子命令描述"
    - "Commander.js 使用 commander 包 (非自实现)"
  artifacts:
    - "packages/cli/src/program.ts"
    - "packages/cli/src/context.ts"
    - "packages/cli/src/commands/auth.ts"
    - "packages/cli/src/commands/threads.ts"
    - "packages/cli/src/commands/config.ts"
    - "packages/cli/src/commands/update.ts"
    - "packages/cli/src/program.test.ts"
    - "packages/cli/src/context.test.ts"
    - "packages/cli/src/index.ts"
  key_links:
    - "commander 第三方依赖需加入 packages/cli/package.json"
    - "ServiceContainer 从 @flitter/flitter (Plan 11-06) 导入"
    - "CliContext 被 interactive (Plan 11-02) 和 headless (Plan 11-03) 使用"
    - "login/logout 命令处理器委托给 auth 模块 (Plan 11-04)"
    - "update 命令处理器委托给 update 模块 (Plan 11-05)"
    - "threads 命令使用 ServiceContainer.threadStore"
    - "config 命令使用 ServiceContainer.configService"
---

<objective>
搭建 Commander.js 命令树和 CLI 上下文解析: 定义所有子命令结构、全局选项、模式判定逻辑。

Purpose: 提供 CLI 的命令路由骨架, 将用户输入映射到对应的命令处理器
Output: `packages/cli/src/` 下 6 个源文件 + 2 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-integration/CONTEXT.md
@packages/cli/package.json
@packages/flitter/src/container.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `Yz0` | claude-config-system.js | 1436-1448 | createProgram — Commander 命令树 |
| `aF0` | cli-entrypoint.js | 1013-1031 | main entry — argv 解析 + 命令执行 |
| `S8` | cli-entrypoint.js | 913-990 | resolveCliContext — 模式判定 |
| `eF0` | cli-entrypoint.js | 1032-1051 | login 命令骨架 |
| `tF0` | cli-entrypoint.js | ~1052 | logout 命令骨架 |

## 参考实现

### program.ts — Commander.js 命令树

```typescript
import { Command } from "commander";

/**
 * 创建 Commander.js 命令树
 *
 * 逆向: Yz0() in claude-config-system.js:1436-1448
 *
 * 命令结构:
 *   flitter [message]          默认: 交互式 TUI 或执行模式
 *   flitter login              API Key / OAuth 登录
 *   flitter logout             清除凭据
 *   flitter threads list       列出 thread
 *   flitter threads new        创建新 thread
 *   flitter threads continue   继续 thread
 *   flitter threads archive    归档 thread
 *   flitter threads delete     删除 thread
 *   flitter config get <key>   获取配置
 *   flitter config set <k> <v> 设置配置
 *   flitter config list        列出配置
 *   flitter update             检查更新
 */
export function createProgram(version: string): Command {
  const program = new Command()
    .name("flitter")
    .version(version)
    .description("Flitter — AI Agent in your terminal")
    .argument("[message...]", "Message to send (execute mode)")
    .option("-e, --execute", "Run in non-interactive execute mode")
    .option("--headless", "Run in headless JSON stream mode")
    .option("--stream-json", "Output JSON event stream to stdout")
    .option("--no-color", "Disable color output")
    .option("-v, --verbose", "Enable verbose logging");

  // Auth commands
  program.command("login").description("Authenticate with API key or OAuth");
  program.command("logout").description("Remove stored credentials");

  // Thread management
  const threads = program.command("threads").description("Manage conversation threads");
  threads.command("list").description("List all threads")
    .option("--limit <n>", "Max threads to show", "20")
    .option("--format <fmt>", "Output format (table|json)", "table");
  threads.command("new").description("Create a new thread")
    .option("--model <model>", "LLM model to use");
  threads.command("continue <id>").description("Continue an existing thread");
  threads.command("archive <id>").description("Archive a thread");
  threads.command("delete <id>").description("Delete a thread");

  // Config management
  const config = program.command("config").description("Manage configuration");
  config.command("get <key>").description("Get a config value");
  config.command("set <key> <value>").description("Set a config value");
  config.command("list").description("List all config values");

  // Update
  program.command("update").description("Check for and install updates")
    .option("--target-version <version>", "Install specific version");

  return program;
}
```

### context.ts — CLI 上下文解析

```typescript
import type { Command } from "commander";

/** CLI 运行上下文 */
export interface CliContext {
  /** 非交互式执行模式 */
  executeMode: boolean;
  /** stdout/stderr 是否为 TTY */
  isTTY: boolean;
  /** Headless JSON 流模式 */
  headless: boolean;
  /** 输出 JSON 事件流 */
  streamJson: boolean;
  /** 详细日志 */
  verbose: boolean;
  /** 用户消息 (execute 模式) */
  userMessage?: string;
}

/**
 * 从 Commander 解析结果构建 CLI 上下文
 *
 * 模式判定逻辑 (逆向 S8):
 * - --execute 或 stdout 非 TTY → execute mode
 * - --headless → headless mode (隐含 execute)
 * - 默认 (TTY) → interactive mode
 */
export function resolveCliContext(program: Command): CliContext {
  const opts = program.opts();
  const isTTY = Boolean(process.stdout.isTTY && process.stderr.isTTY);
  const executeMode = Boolean(opts.execute) || !isTTY || Boolean(opts.headless);

  return {
    executeMode,
    isTTY,
    headless: Boolean(opts.headless),
    streamJson: Boolean(opts.streamJson) || Boolean(opts.headless),
    verbose: Boolean(opts.verbose),
    userMessage: program.args.length > 0 ? program.args.join(" ") : undefined,
  };
}
```

## 测试策略

- **program.test.ts (~12 tests)**:
  - createProgram 返回 Command 实例
  - --version 输出版本号
  - --help 包含所有子命令 (login/logout/threads/config/update)
  - 全局选项解析: --execute, --headless, --stream-json, --no-color, --verbose
  - threads 子命令: list/new/continue/archive/delete 全部注册
  - config 子命令: get/set/list 全部注册
  - 未知命令不抛异常

- **context.test.ts (~8 tests)**:
  - resolveCliContext: TTY 默认 → executeMode=false
  - resolveCliContext: --execute → executeMode=true
  - resolveCliContext: !isTTY → executeMode=true
  - resolveCliContext: --headless → headless=true, executeMode=true, streamJson=true
  - resolveCliContext: --stream-json → streamJson=true
  - resolveCliContext: --verbose → verbose=true
  - resolveCliContext: args 拼接为 userMessage
  - resolveCliContext: 无 args → userMessage=undefined
