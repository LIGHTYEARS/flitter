---
phase: 11-cli-integration
plan: 02
type: tdd
wave: 2
depends_on: [11-01, 11-06]
files_modified:
  - packages/cli/src/modes/interactive.ts
  - packages/cli/src/modes/interactive.test.ts
autonomous: true
requirements: [CLI-02]
user_setup: []

must_haves:
  truths:
    - "launchInteractiveMode(container, context) 异步函数启动全屏 TUI"
    - "TUI 组件树层级: ThemeController → ConfigProvider → AppWidget → ThreadStateWidget"
    - "ThemeController 从 configService 读取 terminal.theme 设置, 默认 'terminal'"
    - "ThemeController 订阅 config 变更, 动态切换主题"
    - "ConfigProvider (InheritedWidget) 向子树提供 configService"
    - "AppWidget (StatefulWidget) 管理 ThreadPool / ThreadHandle 生命周期"
    - "ThreadStateWidget 连接 ThreadWorker 事件流到对话 UI"
    - "runApp(rootWidget, opts) 从 @flitter/tui 导入, 启动帧调度器和渲染循环"
    - "启动时创建或恢复 thread: 新建 thread 或从 threadStore 加载最近的"
    - "launchInteractiveMode 在 finally 中调用 container.asyncDispose()"
    - "退出前输出 thread URL (如配置了 ampURL)"
    - "process.stdin 在 TUI 模式下由 TUI InputParser 接管"
    - "Ctrl+C 触发 graceful shutdown: cancel 当前推理 → dispose → exit"
    - "组件树使用 @flitter/tui 的 Widget/StatefulWidget/State 基类"
    - "ThreadWorker 事件通过 agentEvents$ 驱动 UI 更新"
  artifacts:
    - "packages/cli/src/modes/interactive.ts"
    - "packages/cli/src/modes/interactive.test.ts"
  key_links:
    - "runApp 从 @flitter/tui 导入 (frame-scheduler.ts T1T)"
    - "Widget, StatefulWidget, State 从 @flitter/tui 导入"
    - "ServiceContainer 从 @flitter/flitter (Plan 11-06) 导入"
    - "CliContext 从 Plan 11-01 导入"
    - "ThreadWorker 从 @flitter/agent-core 导入"
    - "被 program.ts 默认动作调用 (非 execute/headless 模式)"
---

<objective>
实现交互式 TUI 模式入口: 组装组件树、启动 runApp、连接 ThreadWorker 事件到 UI。

Purpose: 提供端到端全屏交互式对话体验, 达成 Milestone M6
Output: `packages/cli/src/modes/` 下 1 个源文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-integration/CONTEXT.md
@packages/tui/src/index.ts
@packages/agent-core/src/worker/thread-worker.ts
@packages/agent-core/src/worker/events.ts
@packages/flitter/src/container.ts
@packages/cli/src/context.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `_70` | html-sanitizer-repl.js | 1327-1388 | launchTuiApp — TUI 主启动函数 |
| `T1T` | html-sanitizer-repl.js | ~1300 | runApp — 挂载根 Widget 并启动渲染 |
| `QJT` | html-sanitizer-repl.js | ~900 | AppWidget — 主应用 Widget |
| `Z8R` | html-sanitizer-repl.js | ~1100 | ThreadStateWidget — 线程状态管理 |
| `FJT` | html-sanitizer-repl.js | ~1200 | ThemeControllerWidget |
| `UJT` | html-sanitizer-repl.js | ~1250 | ConfigProvider (InheritedWidget) |
| `DJT` | html-sanitizer-repl.js | ~1270 | KeyBindings Widget |
| `zJT` | html-sanitizer-repl.js | ~1340 | ThemeState 初始化 |

## 参考实现

### interactive.ts — 交互式 TUI 模式

```typescript
import type { ServiceContainer } from "@flitter/flitter";
import type { CliContext } from "../context";
import { Logger } from "@flitter/util";

const log = Logger.create("interactive");

/**
 * 启动交互式 TUI 模式
 *
 * 组件树:
 *   ThemeController (动态主题)
 *     └── ConfigProvider (配置注入)
 *         └── AppWidget (线程管理)
 *             └── ThreadStateWidget (对话 UI)
 *
 * 逆向: _70() in html-sanitizer-repl.js:1327-1388
 */
export async function launchInteractiveMode(
  container: ServiceContainer,
  context: CliContext,
): Promise<void> {
  log.info("Launching interactive TUI mode...");

  // 1. 读取主题设置
  const config = await container.configService.getLatest();
  const themeName = config.settings["terminal.theme"] ?? "terminal";

  // 2. 创建或恢复 thread
  const threadId = await resolveThread(container, context);
  log.info("Thread resolved", { threadId });

  // 3. 创建 ThreadWorker
  const worker = container.createThreadWorker(threadId);

  // 4. 组装 Widget 树
  const app = buildWidgetTree(container, worker, themeName);

  // 5. 启动 TUI
  try {
    await runApp(app, {
      onRootElementMounted: () => {
        const bootMs = /* hrtime delta */;
        log.info(`Boot complete: ${bootMs.toFixed(0)}ms to interactive`);
      },
    });
  } finally {
    // 6. 清理
    log.info("TUI exited, cleaning up...");
    await container.asyncDispose();

    // 7. 退出前输出 thread URL
    const thread = container.threadStore.getThreadSnapshot(threadId);
    if (thread && thread.messages.length > 0) {
      const url = `${context.ampURL}/threads/${threadId}`;
      process.stdout.write(`\nThread: ${url}\n`);
    }
  }
}

/**
 * 解析要使用的 thread
 * - context.threadId 指定 → 恢复
 * - 否则 → 新建
 */
async function resolveThread(container: ServiceContainer, context: CliContext): Promise<string> {
  // 创建新 thread
  const id = crypto.randomUUID();
  container.threadStore.setCachedThread(id, {
    id,
    messages: [],
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  });
  return id;
}

/**
 * 组装 Widget 树
 */
function buildWidgetTree(container: ServiceContainer, worker: ThreadWorker, themeName: string): Widget {
  // ThemeController → ConfigProvider → AppWidget → ThreadStateWidget
  // 每层提供上下文给子树
}
```

## 测试策略

- **interactive.test.ts (~15 tests)**:
  - launchInteractiveMode 调用 runApp
  - launchInteractiveMode 在 finally 中调用 asyncDispose
  - resolveThread 无 threadId 时创建新 thread
  - resolveThread 有 threadId 时从 threadStore 加载
  - buildWidgetTree 返回正确层级的组件树
  - ThemeController 使用 configService 的 terminal.theme
  - ThemeController 默认主题为 "terminal"
  - ThreadWorker 创建时传递正确的 threadId
  - 退出时输出 thread URL (有消息时)
  - 退出时不输出 URL (无消息时)
  - Ctrl+C 信号 → cancel worker → dispose
  - 组件树根节点是 ThemeController 类型
  - ConfigProvider 注入 configService
  - process.stdin 模式切换 (raw mode for TUI)
  - 错误处理: runApp 抛异常仍清理资源
