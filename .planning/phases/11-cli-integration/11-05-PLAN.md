---
phase: 11-cli-integration
plan: 05
type: tdd
wave: 3
depends_on: [11-01]
files_modified:
  - packages/cli/src/update/checker.ts
  - packages/cli/src/update/installer.ts
  - packages/cli/src/commands/update.ts
  - packages/cli/src/update/checker.test.ts
  - packages/cli/src/update/installer.test.ts
autonomous: true
requirements: [CLI-05]
user_setup: []

must_haves:
  truths:
    - "checkForUpdate(currentVersion) 异步函数检查是否有新版本"
    - "checkForUpdate 返回 UpdateInfo | null (null = 无更新)"
    - "UpdateInfo 包含 version (string), downloadUrl (string), sha256 (string), releaseNotes (string)"
    - "checkForUpdate 通过 HTTP GET 请求版本检查端点"
    - "版本比较: semver 数字比较, 相同版本返回 null"
    - "installUpdate(info, opts) 异步函数执行更新安装"
    - "安装流程: 下载二进制 → SHA-256 校验 → 原子文件替换"
    - "SHA-256 校验: 计算下载文件哈希, 与 info.sha256 比较"
    - "SHA-256 校验失败时抛出 UpdateVerificationError, 不执行替换"
    - "原子替换: write temp → rename temp → old binary (不删除旧版)"
    - "下载进度回调 onProgress(downloaded, total)"
    - "installUpdate 验证新文件权限 (chmod +x)"
    - "detectInstallMethod() 检测安装方式: binary | npm | pnpm | bun | brew"
    - "npm/pnpm/bun 安装方式: 使用对应包管理器升级 (@flitter/cli@latest)"
    - "brew 安装方式: brew upgrade flitter"
    - "binary 安装方式: CDN 下载 + SHA-256 + 原子替换"
    - "更新设置: settings['updates.mode'] — 'auto' | 'warn' | 'disabled'"
    - "updates.mode='disabled' 时 checkForUpdate 直接返回 null"
    - "handleUpdateCommand(container) 命令处理器: 检查 + 确认 + 安装"
    - "compareVersions(a, b) 返回 -1 | 0 | 1"
    - "computeSHA256(filePath) 返回十六进制哈希字符串"
  artifacts:
    - "packages/cli/src/update/checker.ts"
    - "packages/cli/src/update/installer.ts"
    - "packages/cli/src/commands/update.ts"
    - "packages/cli/src/update/checker.test.ts"
    - "packages/cli/src/update/installer.test.ts"
  key_links:
    - "update 命令注册在 program.ts (Plan 11-01)"
    - "settings 从 ServiceContainer.configService 获取"
    - "crypto.createHash('sha256') 用于校验"
    - "fs.rename 用于原子替换"
    - "逆向 process-runner.js 的 pm0/mm0/FVT/zVT/qVT 函数"
---

<objective>
实现自动更新机制: 版本检测 + CDN 下载 + SHA-256 校验 + 原子文件替换 + 多包管理器支持。

Purpose: 支持用户通过 `flitter update` 安全地更新到最新版本
Output: `packages/cli/src/update/` 下 2 个源文件 + 2 个测试文件, 1 个命令文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cli-integration/CONTEXT.md
@packages/cli/src/program.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `pm0` | process-runner.js | ~830 | createUpdateService — 后台更新检查 |
| `mm0` | process-runner.js | 2938-2988 | handleUpdateCommand — update 子命令 |
| `FVT` | process-runner.js | ~2600 | checkForBinaryUpdate — CDN 版本检查 |
| `zVT` | process-runner.js | ~2700 | checkForNpmUpdate — npm registry 版本检查 |
| `qVT` | process-runner.js | ~2800 | installUpdate — 多方式安装 |

## 参考实现

### checker.ts — 版本检测

```typescript
import { createHash } from "node:crypto";
import { readFile } from "node:fs/promises";

export interface UpdateInfo {
  version: string;
  downloadUrl: string;
  sha256: string;
  releaseNotes: string;
}

export type InstallMethod = "binary" | "npm" | "pnpm" | "bun" | "brew";

/**
 * 比较语义版本号
 * @returns -1 (a < b), 0 (a === b), 1 (a > b)
 */
export function compareVersions(a: string, b: string): -1 | 0 | 1 {
  const pa = a.replace(/^v/, "").split(".").map(Number);
  const pb = b.replace(/^v/, "").split(".").map(Number);
  for (let i = 0; i < Math.max(pa.length, pb.length); i++) {
    const na = pa[i] ?? 0;
    const nb = pb[i] ?? 0;
    if (na < nb) return -1;
    if (na > nb) return 1;
  }
  return 0;
}

/**
 * 计算文件 SHA-256 哈希
 */
export async function computeSHA256(filePath: string): Promise<string> {
  const data = await readFile(filePath);
  return createHash("sha256").update(data).digest("hex");
}

/**
 * 检测安装方式
 */
export function detectInstallMethod(): InstallMethod {
  const execPath = process.execPath;
  if (execPath.includes("/.bun/")) return "bun";
  if (execPath.includes("/homebrew/") || execPath.includes("/Cellar/")) return "brew";
  // 检查全局 npm/pnpm 路径
  const npmGlobal = process.env.npm_config_prefix;
  if (npmGlobal && execPath.startsWith(npmGlobal)) return "npm";
  return "binary";
}

/**
 * 检查是否有新版本
 *
 * 逆向: FVT/zVT in process-runner.js
 */
export async function checkForUpdate(
  currentVersion: string,
  opts?: { checkUrl?: string; updateMode?: string },
): Promise<UpdateInfo | null> {
  if (opts?.updateMode === "disabled") return null;

  const url = opts?.checkUrl ?? "https://update.flitter.dev/latest";
  const resp = await fetch(url, {
    headers: { "X-Current-Version": currentVersion },
  });
  if (!resp.ok) return null;

  const info = await resp.json() as UpdateInfo;
  if (compareVersions(currentVersion, info.version) >= 0) return null;

  return info;
}
```

### installer.ts — 下载安装

```typescript
import { createWriteStream } from "node:fs";
import { rename, chmod } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { pipeline } from "node:stream/promises";

export class UpdateVerificationError extends Error {
  constructor(expected: string, actual: string) {
    super(`SHA-256 verification failed: expected ${expected}, got ${actual}`);
    this.name = "UpdateVerificationError";
  }
}

export interface InstallOptions {
  onProgress?: (downloaded: number, total: number) => void;
  targetPath?: string;
}

/**
 * 下载并安装更新 (二进制模式)
 *
 * 流程: 下载 → SHA-256 校验 → 原子替换
 * 逆向: qVT in process-runner.js
 */
export async function installBinaryUpdate(
  info: UpdateInfo,
  opts?: InstallOptions,
): Promise<void> {
  const targetPath = opts?.targetPath ?? process.execPath;
  const tempPath = join(tmpdir(), `flitter-update-${Date.now()}`);

  // 1. 下载到临时文件
  const resp = await fetch(info.downloadUrl);
  if (!resp.ok || !resp.body) throw new Error(`Download failed: ${resp.status}`);

  const total = Number(resp.headers.get("content-length") || 0);
  let downloaded = 0;
  const writer = createWriteStream(tempPath);

  const reader = resp.body.getReader();
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    writer.write(value);
    downloaded += value.byteLength;
    opts?.onProgress?.(downloaded, total);
  }
  writer.end();
  await new Promise<void>((resolve) => writer.on("finish", resolve));

  // 2. SHA-256 校验
  const actual = await computeSHA256(tempPath);
  if (actual !== info.sha256) {
    await unlink(tempPath).catch(() => {});
    throw new UpdateVerificationError(info.sha256, actual);
  }

  // 3. 设置执行权限
  await chmod(tempPath, 0o755);

  // 4. 原子替换
  await rename(tempPath, targetPath);
}

/**
 * 使用包管理器安装更新
 */
export async function installWithPackageManager(
  method: InstallMethod,
  version?: string,
): Promise<void> {
  const pkg = version ? `@flitter/cli@${version}` : "@flitter/cli@latest";

  switch (method) {
    case "npm": await exec(`npm install -g ${pkg}`); break;
    case "pnpm": await exec(`pnpm install -g ${pkg}`); break;
    case "bun": await exec(`bun install -g ${pkg}`); break;
    case "brew": await exec("brew upgrade flitter"); break;
    default: throw new Error(`Unsupported install method: ${method}`);
  }
}
```

## 测试策略

- **checker.test.ts (~8 tests)**:
  - compareVersions: "1.0.0" vs "1.0.1" → -1
  - compareVersions: "1.0.1" vs "1.0.0" → 1
  - compareVersions: "1.0.0" vs "1.0.0" → 0
  - compareVersions: "v1.0" vs "1.0.0" → 0 (v 前缀)
  - computeSHA256: 已知内容 → 已知哈希
  - detectInstallMethod: bun 路径 → "bun"
  - checkForUpdate: 有新版本 → 返回 UpdateInfo
  - checkForUpdate: 版本相同 → null
  - checkForUpdate: mode=disabled → null

- **installer.test.ts (~7 tests)**:
  - installBinaryUpdate: 下载 + 校验通过 → 替换文件
  - installBinaryUpdate: SHA-256 不匹配 → 抛 UpdateVerificationError
  - installBinaryUpdate: SHA-256 失败后删除临时文件
  - installBinaryUpdate: 进度回调被调用
  - installBinaryUpdate: 替换后文件有执行权限
  - installWithPackageManager: npm → 执行 npm install -g
  - installWithPackageManager: 未知方式 → 抛错
