# Phase 11: UX Polish — Plan 01

**Wave:** 1 of 1
**Scope:** UX-01 through UX-05

## Tasks

### Task 1: Permission Dialog background mask (UX-01)

**File:** `packages/flitter-amp/src/widgets/permission-dialog.ts`

Wrap the existing dialog content in a full-screen Container with a semi-transparent background. Use a Stack with a background layer + centered dialog.

### Task 2: Remove debug console.error logs (UX-02)

**File:** `packages/flitter-amp/src/app.ts`

Remove `console.error(\`[APP-DEBUG]...`)` at line 136. Use `log.debug()` instead for developer visibility.

### Task 3: Display agentInfo.name in BottomGrid (UX-03)

**Files:** `packages/flitter-amp/src/app.ts`, `packages/flitter-amp/src/widgets/bottom-grid.ts`

Pass `agentName` from appState to BottomGrid, display it in the status area.

### Task 4: Markdown paragraph merging (UX-04)

**File:** `packages/flitter-core/src/widgets/markdown.ts`

In `_parseMarkdownNoCache`, merge consecutive non-empty non-special lines into a single paragraph block (join with space).

### Task 5: Markdown heading prefixes (UX-05)

**File:** `packages/flitter-core/src/widgets/markdown.ts`

Change heading prefix from empty string to distinguishable characters:
- H1: `━ ` (heavy horizontal bar)
- H2: `─ ` (light horizontal bar)
- H3: `· ` (middle dot)
- H4: (no prefix, keep empty)

---

*Plan: 11-PLAN-01*
*Created: 2026-03-28*
