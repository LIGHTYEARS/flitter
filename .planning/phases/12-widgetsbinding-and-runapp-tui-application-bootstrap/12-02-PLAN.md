---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 02
type: tdd
wave: A
depends_on: []
files_modified:
  - packages/tui/src/focus/focus-node.ts
  - packages/tui/src/focus/index.ts
  - packages/tui/src/index.ts
  - packages/tui/src/focus/focus-node.test.ts
autonomous: true
requirements: [TUI-FOCUS-NODE]
user_setup: []

must_haves:
  truths:
    - "FocusNode 维护 parent/children 树结构 (parent: FocusNode | null, children: Set<FocusNode>)"
    - "FocusNode 属性: debugId, debugLabel, hasPrimaryFocus, hasFocus, canRequestFocus, skipTraversal"
    - "hasFocus 为 true 当 hasPrimaryFocus 为 true 或当前 primaryFocus 是其后代"
    - "canRequestFocus 可动态修改，设为 false 时若持有焦点则自动 unfocus"
    - "requestFocus() 通过静态回调委托给 FocusManager"
    - "unfocus() 通过静态回调请求 FocusManager 将焦点设为 null"
    - "_attach(parent) 将节点挂入焦点树，_detach() 从焦点树移除"
    - "_setFocus(hasFocus) 内部方法设置焦点状态并通知 listeners 和祖先 listeners"
    - "_handleKeyEvent(event) 调用注册的 key handler，返回 'handled' | 'ignored'"
    - "_handlePasteEvent(event) 调用注册的 paste handler，返回 'handled' | 'ignored'"
    - "addListener/removeListener 用于监听焦点状态变化"
    - "_isDescendantOf(node) 检查是否为某节点的后代"
    - "FocusNode 构造参数: { debugLabel?, canRequestFocus?, skipTraversal?, onKey?, onPaste? }"
    - "setRequestFocusCallback 为静态方法，FocusManager 初始化时设置"
  artifacts:
    - "packages/tui/src/focus/focus-node.ts"
    - "packages/tui/src/focus/index.ts"
    - "packages/tui/src/index.ts (re-export)"
    - "packages/tui/src/focus/focus-node.test.ts"
  key_links:
    - "逆向参考: conversation-ui-logic.js 中 class l8"
    - "被 12-03 FocusManager 和 12-12 InputField 依赖"
    - "KeyEvent 类型从 @flitter/tui vt/input-parser.ts 导入"
---

<objective>
实现 FocusNode — 焦点树节点，提供键盘事件路由和焦点管理的基础数据结构。

Purpose: 为 FocusManager 的焦点树提供节点原语，支持键盘/粘贴事件的冒泡式分发
Output: `packages/tui/src/focus/` 下 2 个新文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/vt/input-parser.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `l8` | conversation-ui-logic.js | (grep: class l8) | FocusNode 完整实现 |
| `l8._requestFocusCallback` | conversation-ui-logic.js | | 静态回调委托 FocusManager |
| `l8._nextDebugId` | conversation-ui-logic.js | | 自增 debug ID |

## 参考实现

### focus-node.ts

```typescript
import type { KeyEvent, PasteEvent } from "../vt/input-parser.js";

/**
 * 键盘事件处理结果。
 */
export type KeyEventResult = "handled" | "ignored";

/**
 * 键盘事件处理器类型。
 */
export type KeyHandler = (event: KeyEvent) => KeyEventResult;

/**
 * 粘贴事件处理器类型。
 */
export type PasteHandler = (event: PasteEvent) => KeyEventResult;

/**
 * FocusNode 构造参数。
 */
export interface FocusNodeOptions {
  debugLabel?: string;
  canRequestFocus?: boolean;
  skipTraversal?: boolean;
  onKey?: KeyHandler;
  onPaste?: PasteHandler;
}

/**
 * FocusNode — 焦点树节点。
 *
 * 逆向: l8 in conversation-ui-logic.js
 *
 * 维护 parent/children 树结构，处理键盘和粘贴事件的冒泡分发。
 * requestFocus() 通过静态回调委托给 FocusManager。
 */
export class FocusNode {
  private static _nextDebugId = 0;
  private static _requestFocusCallback:
    | ((node: FocusNode | null) => boolean)
    | null = null;

  private _debugId: string;
  private _parent: FocusNode | null = null;
  private _children: Set<FocusNode> = new Set();
  private _hasPrimaryFocus = false;
  private _canRequestFocus: boolean;
  private _skipTraversal: boolean;
  private _keyHandlers: KeyHandler[] = [];
  private _onPasteCallback: PasteHandler | null = null;
  private _listeners: Set<(node: FocusNode) => void> = new Set();
  private _debugLabel: string | null;

  constructor(options: FocusNodeOptions = {}) {
    this._debugId = `focus-${FocusNode._nextDebugId++}`;
    this._debugLabel = options.debugLabel ?? null;
    this._canRequestFocus = options.canRequestFocus ?? true;
    this._skipTraversal = options.skipTraversal ?? false;
    if (options.onKey) this._keyHandlers.push(options.onKey);
    this._onPasteCallback = options.onPaste ?? null;
  }

  get debugId(): string { return this._debugId; }
  get debugLabel(): string | null { return this._debugLabel; }
  get parent(): FocusNode | null { return this._parent; }
  get children(): Set<FocusNode> { return this._children; }

  get hasPrimaryFocus(): boolean { return this._hasPrimaryFocus; }

  get hasFocus(): boolean {
    if (this._hasPrimaryFocus) return true;
    const cb = FocusNode._requestFocusCallback as
      | (((n: FocusNode | null) => boolean) & { __focusManager?: { primaryFocus: FocusNode | null } })
      | null;
    const primary = cb?.__focusManager?.primaryFocus ?? null;
    return primary?._isDescendantOf(this) ?? false;
  }

  get canRequestFocus(): boolean { return this._canRequestFocus; }
  set canRequestFocus(value: boolean) {
    if (this._canRequestFocus !== value) {
      this._canRequestFocus = value;
      if (!value && this._hasPrimaryFocus) this.unfocus();
    }
  }

  get skipTraversal(): boolean { return this._skipTraversal; }
  set skipTraversal(value: boolean) { this._skipTraversal = value; }

  get onPaste(): PasteHandler | null { return this._onPasteCallback; }
  set onPaste(handler: PasteHandler | null) { this._onPasteCallback = handler; }

  static setRequestFocusCallback(
    callback: ((node: FocusNode | null) => boolean) & { __focusManager?: unknown },
  ): void {
    FocusNode._requestFocusCallback = callback;
  }

  requestFocus(): boolean {
    if (!this._canRequestFocus) return false;
    if (FocusNode._requestFocusCallback) return FocusNode._requestFocusCallback(this);
    return false;
  }

  unfocus(): void {
    if (this._hasPrimaryFocus) {
      FocusNode._requestFocusCallback?.(null);
    }
  }

  addListener(listener: (node: FocusNode) => void): void {
    this._listeners.add(listener);
  }

  removeListener(listener: (node: FocusNode) => void): void {
    this._listeners.delete(listener);
  }

  _isDescendantOf(target: FocusNode | null): boolean {
    if (target === null) return false;
    if (this._parent === target) return true;
    return this._parent?._isDescendantOf(target) ?? false;
  }

  _attach(parent: FocusNode): void {
    if (this._parent === parent) return;
    if (this._parent) this._parent._children.delete(this);
    this._parent = parent;
    parent._children.add(this);
  }

  _detach(): void {
    if (this._parent) {
      this._parent._children.delete(this);
      this._parent = null;
    }
    if (this._hasPrimaryFocus) this.unfocus();
  }

  _setFocus(value: boolean): void {
    if (this._hasPrimaryFocus === value) return;
    const hadFocus = this.hasFocus;
    this._hasPrimaryFocus = value;
    const hasFocusNow = this.hasFocus;
    this._notifyListeners();
    if (hadFocus !== hasFocusNow) this._notifyAncestorListeners();
  }

  _handleKeyEvent(event: KeyEvent): KeyEventResult {
    for (const handler of this._keyHandlers) {
      if (handler(event) === "handled") return "handled";
    }
    return "ignored";
  }

  _handlePasteEvent(event: PasteEvent): KeyEventResult {
    if (this._onPasteCallback) return this._onPasteCallback(event);
    return "ignored";
  }

  dispose(): void {
    this._detach();
    for (const child of [...this._children]) {
      child._detach();
    }
    this._children.clear();
    this._listeners.clear();
    this._keyHandlers.length = 0;
    this._onPasteCallback = null;
  }

  private _notifyListeners(): void {
    for (const listener of this._listeners) listener(this);
  }

  private _notifyAncestorListeners(): void {
    let current = this._parent;
    while (current) {
      current._notifyListeners();
      current = current._parent;
    }
  }
}
```

## 测试策略

- **focus-node.test.ts (~14 tests)**:
  - 构造函数默认值: canRequestFocus=true, skipTraversal=false
  - debugLabel 和 debugId 正确设置
  - _attach 建立 parent-child 关系
  - _detach 清除 parent-child 关系
  - _setFocus(true) 设置 hasPrimaryFocus
  - _setFocus(false) 清除 hasPrimaryFocus
  - _setFocus 变化时通知 listeners (验证参数)
  - _setFocus 变化时通知祖先 listeners
  - _handleKeyEvent 调用注册的 handler 并返回结果
  - _handlePasteEvent 调用 onPaste 回调
  - canRequestFocus 设为 false 时自动 unfocus
  - _isDescendantOf 正确判断祖先关系
  - requestFocus 通过 _requestFocusCallback 委托
  - dispose 清除所有状态和关系
