---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 03
type: tdd
wave: A
depends_on: [12-02]
files_modified:
  - packages/tui/src/focus/focus-manager.ts
  - packages/tui/src/focus/index.ts
  - packages/tui/src/focus/focus-manager.test.ts
autonomous: true
requirements: [TUI-FOCUS-MANAGER]
user_setup: []

must_haves:
  truths:
    - "FocusManager 为单例模式 (static _instance, static get instance)"
    - "构造时创建 _rootScope (FocusNode canRequestFocus=false, debugLabel='Root Focus Scope')"
    - "构造时设置 FocusNode.setRequestFocusCallback 指向 this.requestFocus"
    - "requestFocus(node) 切换 primaryFocus: 旧焦点 _setFocus(false), 新焦点 _setFocus(true)"
    - "requestFocus(null) 从 _primaryFocusStack 回退到上一个可用焦点节点"
    - "handleKeyEvent(event) 从 primaryFocus 向上冒泡，逐个调用 node._handleKeyEvent"
    - "handlePasteEvent(event) 从 primaryFocus 向上冒泡，逐个调用 node._handlePasteEvent"
    - "registerNode(node, parent?) 将节点挂入焦点树 (默认 parent 为 rootScope)"
    - "unregisterNode(node) 从焦点树移除，若为 primaryFocus 则自动回退"
    - "focusNext() / focusPrevious() 实现 Tab 导航 (循环遍历可聚焦节点)"
    - "findAllFocusableNodes() 深度优先遍历，返回 canRequestFocus && !skipTraversal 的节点"
    - "_primaryFocusStack 维护焦点历史栈用于回退"
    - "dispose() 清空全部状态并重置 _instance"
  artifacts:
    - "packages/tui/src/focus/focus-manager.ts"
    - "packages/tui/src/focus/index.ts (re-export)"
    - "packages/tui/src/focus/focus-manager.test.ts"
  key_links:
    - "逆向参考: conversation-ui-logic.js 中 class ic"
    - "依赖 12-02 FocusNode"
    - "被 12-08 WidgetsBinding 和 12-12 InputField 依赖"
    - "WidgetsBinding.setupEventHandlers 中调用 focusManager.handleKeyEvent"
---

<objective>
实现 FocusManager 单例 — 焦点树管理器，负责焦点切换、键盘/粘贴事件冒泡路由、Tab 导航。

Purpose: 管理全局焦点状态，将键盘输入路由到正确的 FocusNode
Output: `packages/tui/src/focus/` 下 1 个新文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/focus/focus-node.ts
@packages/tui/src/vt/input-parser.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `ic` | conversation-ui-logic.js | (grep: class ic) | FocusManager 完整实现 |
| `ic.instance` | conversation-ui-logic.js | | 单例访问 |
| `ic.requestFocus` | conversation-ui-logic.js | | 焦点切换核心逻辑 |
| `ic.handleKeyEvent` | conversation-ui-logic.js | | 键盘事件冒泡分发 |

## 参考实现

### focus-manager.ts

```typescript
import type { KeyEvent, PasteEvent } from "../vt/input-parser.js";
import { FocusNode, type KeyEventResult } from "./focus-node.js";

/**
 * FocusManager — 焦点树管理器单例。
 *
 * 逆向: ic in conversation-ui-logic.js
 *
 * 管理全局焦点状态，处理键盘事件从 primaryFocus 向上冒泡、
 * Tab 导航、焦点历史栈。
 */
export class FocusManager {
  private static _instance: FocusManager | null = null;

  private _rootScope: FocusNode;
  private _primaryFocus: FocusNode | null = null;
  private _cachedFocusableNodes: FocusNode[] | null = null;
  private _primaryFocusStack: FocusNode[] = [];

  constructor() {
    this._rootScope = new FocusNode({
      debugLabel: "Root Focus Scope",
      canRequestFocus: false,
    });
    const callback = ((node: FocusNode | null) =>
      this.requestFocus(node)) as ((node: FocusNode | null) => boolean) & {
      __focusManager?: FocusManager;
    };
    callback.__focusManager = this;
    FocusNode.setRequestFocusCallback(callback);
  }

  static get instance(): FocusManager {
    if (!FocusManager._instance) FocusManager._instance = new FocusManager();
    return FocusManager._instance;
  }

  get primaryFocus(): FocusNode | null { return this._primaryFocus; }
  get rootScope(): FocusNode { return this._rootScope; }

  requestFocus(node: FocusNode | null): boolean {
    if (this._primaryFocus === node) return true;
    if (node && !node.canRequestFocus) return false;
    if (node && !node.parent) return false;
    if (this._primaryFocus) this._primaryFocus._setFocus(false);
    if (node === null) {
      const old = this._primaryFocus;
      if (old) this._popFromFocusStack(old);
      const prev = this._findPreviousFocusableNode();
      this._primaryFocus = prev;
      if (prev) prev._setFocus(true);
      return true;
    }
    this._primaryFocus = node;
    node._setFocus(true);
    this._pushToFocusStack(node);
    return true;
  }

  handleKeyEvent(event: KeyEvent): boolean {
    if (!this._primaryFocus) return false;
    let current: FocusNode | null = this._primaryFocus;
    while (current) {
      if (current._handleKeyEvent(event) === "handled") return true;
      current = current.parent;
    }
    return false;
  }

  handlePasteEvent(event: PasteEvent): boolean {
    if (!this._primaryFocus) return false;
    let current: FocusNode | null = this._primaryFocus;
    while (current) {
      if (current._handlePasteEvent(event) === "handled") return true;
      current = current.parent;
    }
    return false;
  }

  registerNode(node: FocusNode, parent: FocusNode | null = null): void {
    this._invalidateFocusableNodesCache();
    const target = parent ?? this._rootScope;
    node._attach(target);
  }

  unregisterNode(node: FocusNode): void {
    this._invalidateFocusableNodesCache();
    this._popFromFocusStack(node);
    if (this._primaryFocus === node) this.requestFocus(null);
    node._detach();
  }

  focusNext(): boolean {
    const nodes = this.findAllFocusableNodes();
    if (nodes.length === 0) return false;
    if (!this._primaryFocus) return this.requestFocus(nodes[0] ?? null);
    const idx = nodes.indexOf(this._primaryFocus);
    if (idx === -1) return this.requestFocus(nodes[0] ?? null);
    const next = (idx + 1) % nodes.length;
    return this.requestFocus(nodes[next] ?? null);
  }

  focusPrevious(): boolean {
    const nodes = this.findAllFocusableNodes();
    if (nodes.length === 0) return false;
    if (!this._primaryFocus) return this.requestFocus(nodes[nodes.length - 1] ?? null);
    const idx = nodes.indexOf(this._primaryFocus);
    if (idx === -1) return this.requestFocus(nodes[nodes.length - 1] ?? null);
    const prev = idx === 0 ? nodes.length - 1 : idx - 1;
    return this.requestFocus(nodes[prev] ?? null);
  }

  findAllFocusableNodes(): FocusNode[] {
    if (this._cachedFocusableNodes !== null) return this._cachedFocusableNodes;
    const result: FocusNode[] = [];
    const walk = (node: FocusNode): void => {
      if (node.canRequestFocus && !node.skipTraversal) result.push(node);
      for (const child of node.children) walk(child);
    };
    walk(this._rootScope);
    this._cachedFocusableNodes = result;
    return result;
  }

  dispose(): void {
    this._primaryFocus = null;
    this._cachedFocusableNodes = null;
    this._primaryFocusStack = [];
    this._rootScope.dispose();
    FocusManager._instance = null;
  }

  private _invalidateFocusableNodesCache(): void {
    this._cachedFocusableNodes = null;
  }

  private _pushToFocusStack(node: FocusNode): void {
    const idx = this._primaryFocusStack.indexOf(node);
    if (idx !== -1) this._primaryFocusStack.splice(idx, 1);
    this._primaryFocusStack.push(node);
  }

  private _popFromFocusStack(node: FocusNode): void {
    let idx = this._primaryFocusStack.indexOf(node);
    while (idx !== -1) {
      this._primaryFocusStack.splice(idx, 1);
      idx = this._primaryFocusStack.indexOf(node);
    }
  }

  private _findPreviousFocusableNode(): FocusNode | null {
    while (this._primaryFocusStack.length > 0) {
      const top = this._primaryFocusStack[this._primaryFocusStack.length - 1]!;
      if (top.parent && top.canRequestFocus && !top.skipTraversal) return top;
      this._primaryFocusStack.pop();
    }
    return null;
  }
}
```

## 测试策略

- **focus-manager.test.ts (~14 tests)**:
  - 单例: FocusManager.instance 返回同一实例
  - 构造: rootScope 创建成功 (canRequestFocus=false)
  - requestFocus(node): primaryFocus 切换，旧焦点 _setFocus(false)
  - requestFocus(node): 返回 true 成功
  - requestFocus(node): canRequestFocus=false 时返回 false
  - requestFocus(null): 从 focusStack 回退到上一个可用焦点
  - handleKeyEvent: 从 primaryFocus 冒泡，handled 时停止
  - handleKeyEvent: 无 primaryFocus 返回 false
  - handlePasteEvent: 从 primaryFocus 冒泡
  - registerNode: 节点挂入 rootScope
  - registerNode(node, parent): 节点挂入指定 parent
  - unregisterNode: 从焦点树移除，primaryFocus 回退
  - focusNext: Tab 循环导航
  - focusPrevious: Shift+Tab 反向导航
  - dispose: 清空状态并重置 _instance
