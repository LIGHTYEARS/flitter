---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 04
type: tdd
wave: A
depends_on: []
files_modified:
  - packages/tui/src/gestures/hit-test.ts
  - packages/tui/src/tree/render-object.ts
  - packages/tui/src/gestures/index.ts
  - packages/tui/src/index.ts
  - packages/tui/src/gestures/hit-test.test.ts
autonomous: true
requirements: [TUI-HIT-TEST]
user_setup: []

must_haves:
  truths:
    - "HitTestEntry 接口: { target: RenderObject, localPosition: { x: number, y: number } }"
    - "HitTestResult 类维护 _hits: HitTestEntry[] 数组"
    - "HitTestResult.add(entry) 添加命中项"
    - "HitTestResult.addWithPaintOffset(target, offset, position) 计算局部坐标后添加"
    - "HitTestResult.hits 只读属性返回命中列表"
    - "oXT.hitTest(rootRenderObject, position) 静态工厂: 创建 HitTestResult 并调用 root.hitTest"
    - "RenderObject.hitTest(result, position, offsetX?, offsetY?) 递归子节点命中测试"
    - "hitTest 检查 size + offset 边界: position 在范围内则 add 自身并递归 children"
    - "children 以逆序遍历 (后绘制的优先命中)"
    - "命中测试返回 boolean: 至少有一个命中返回 true"
  artifacts:
    - "packages/tui/src/gestures/hit-test.ts"
    - "packages/tui/src/tree/render-object.ts (patch)"
    - "packages/tui/src/gestures/index.ts"
    - "packages/tui/src/index.ts (re-export)"
    - "packages/tui/src/gestures/hit-test.test.ts"
  key_links:
    - "逆向参考: tui-widget-framework.js:1852-1920 (oXT, nXT, Dy0)"
    - "RenderObject 在 packages/tui/src/tree/render-object.ts"
    - "被 12-06 MouseManager 依赖"
---

<objective>
实现 HitTestResult + HitTestEntry + RenderObject.hitTest: 鼠标事件的递归命中测试。

Purpose: 为 MouseManager 提供基于渲染树坐标的鼠标命中检测能力
Output: `packages/tui/src/gestures/` 下 2 个新文件 + 1 个 RenderObject patch + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/tree/render-object.ts
@packages/tui/src/tree/render-box.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `oXT` | tui-widget-framework.js | 1852-1856 | HitTestEntry 静态工厂 |
| `nXT` | tui-widget-framework.js | 1858-1877 | HitTestResult 累积器 |
| `Dy0` | tui-widget-framework.js | 1879-1921 | RenderObject.hitTest 猴子补丁 |

## 参考实现

### hit-test.ts

```typescript
import type { RenderObject } from "../tree/render-object.js";

/**
 * 命中测试条目。
 */
export interface HitTestEntry {
  target: RenderObject;
  localPosition: { x: number; y: number };
}

/**
 * HitTestResult — 命中测试结果累积器。
 *
 * 逆向: nXT in tui-widget-framework.js:1858-1877
 */
export class HitTestResult {
  private _hits: HitTestEntry[] = [];

  get hits(): readonly HitTestEntry[] {
    return this._hits;
  }

  add(entry: HitTestEntry): void {
    this._hits.push(entry);
  }

  addWithPaintOffset(
    target: RenderObject,
    offset: { x: number; y: number },
    position: { x: number; y: number },
  ): void {
    const localPosition = {
      x: position.x - offset.x,
      y: position.y - offset.y,
    };
    this.add({ target, localPosition });
  }

  /**
   * 静态工厂: 创建 HitTestResult 并从 root 开始命中测试。
   *
   * 逆向: oXT.hitTest in tui-widget-framework.js:1852-1856
   */
  static hitTest(
    root: RenderObject,
    position: { x: number; y: number },
  ): HitTestResult {
    const result = new HitTestResult();
    root.hitTest(result, position);
    return result;
  }
}
```

### render-object.ts — patch: 添加 hitTest 方法

```typescript
// 在 RenderObject 类中新增:

/**
 * 递归命中测试。
 *
 * 逆向: Dy0 in tui-widget-framework.js:1879-1921
 * 检查 position 是否在当前节点 size+offset 范围内，
 * 命中则加入 result 并逆序递归子节点。
 */
hitTest(
  result: HitTestResult,
  position: { x: number; y: number },
  offsetX = 0,
  offsetY = 0,
): boolean {
  if (this._size && this._offset) {
    const absX = offsetX + this._offset.x;
    const absY = offsetY + this._offset.y;
    const inX = position.x >= absX && position.x < absX + this._size.width;
    const inY = position.y >= absY && position.y < absY + this._size.height;
    if (inX && inY) {
      const localPosition = {
        x: position.x - absX,
        y: position.y - absY,
      };
      result.add({ target: this, localPosition });
      for (let i = this._children.length - 1; i >= 0; i--) {
        this._children[i]!.hitTest(result, position, absX, absY);
      }
      return true;
    }
    return false;
  }
  let hit = false;
  for (const child of this._children) {
    if (child.hitTest(result, position, offsetX, offsetY)) hit = true;
  }
  return hit;
}
```

## 测试策略

- **hit-test.test.ts (~10 tests)**:
  - HitTestResult.add 正确累积条目
  - HitTestResult.addWithPaintOffset 计算局部坐标
  - HitTestResult.hits 返回只读视图
  - HitTestResult.hitTest 静态工厂正确调用 root.hitTest
  - RenderObject.hitTest: position 在 bounds 内返回 true
  - RenderObject.hitTest: position 在 bounds 外返回 false
  - RenderObject.hitTest: 子节点逆序遍历 (最后绘制的先命中)
  - RenderObject.hitTest: 嵌套层级正确计算偏移
  - RenderObject.hitTest: 无 size/offset 时递归子节点
  - 空渲染树: hitTest 返回空 hits
