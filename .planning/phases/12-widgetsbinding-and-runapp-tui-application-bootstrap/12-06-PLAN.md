---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 06
type: tdd
wave: B
depends_on: [12-04]
files_modified:
  - packages/tui/src/gestures/mouse-manager.ts
  - packages/tui/src/gestures/index.ts
  - packages/tui/src/gestures/mouse-manager.test.ts
autonomous: true
requirements: [TUI-MOUSE-MANAGER]
user_setup: []

must_haves:
  truths:
    - "MouseManager 为单例模式 (static _instance, static get instance)"
    - "setRootRenderObject(root) 设置命中测试的根渲染对象"
    - "setTui(tui) 设置 TuiController 引用 (获取 Screen 用于坐标转换)"
    - "handleMouseEvent(event) 对 MouseEvent 执行命中测试并分发"
    - "命中测试使用 HitTestResult.hitTest(rootRenderObject, position)"
    - "clearHoverState() 清除当前悬停状态 (resize 时调用)"
    - "reestablishHoverState() 重新建立悬停状态 (resize 后 post-frame 调用)"
    - "dispose() 清空 rootRenderObject 和 tui 引用"
    - "鼠标事件分发到命中的 RenderObject 上的注册 handler"
    - "维护 _lastHoverTargets 用于 hover 状态追踪"
  artifacts:
    - "packages/tui/src/gestures/mouse-manager.ts"
    - "packages/tui/src/gestures/index.ts (re-export)"
    - "packages/tui/src/gestures/mouse-manager.test.ts"
  key_links:
    - "逆向参考: tui-layout-engine.js:1120 (ha.instance 使用处)"
    - "逆向参考: tui-render-pipeline.js 中 mouseManager 方法调用"
    - "依赖 12-04 HitTestResult"
    - "被 12-08 WidgetsBinding 依赖"
---

<objective>
实现 MouseManager 单例 — 鼠标事件管理器，使用 HitTestResult 进行命中测试后分发鼠标事件。

Purpose: 将终端鼠标事件转换为渲染树上特定 RenderObject 的事件
Output: `packages/tui/src/gestures/` 下 1 个新文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/gestures/hit-test.ts
@packages/tui/src/tree/render-object.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `ha` | (class location TBD) | | MouseManager 完整实现 |
| `ha.instance` | tui-layout-engine.js | 1120 | 单例访问 |
| `mouseManager.handleMouseEvent` | tui-render-pipeline.js | 129 | 事件分发入口 |
| `mouseManager.clearHoverState` | tui-render-pipeline.js | 108 | resize 时清除 hover |
| `mouseManager.reestablishHoverState` | tui-render-pipeline.js | 61 | resize 后重建 hover |

## 参考实现

### mouse-manager.ts

```typescript
import type { MouseEvent } from "../vt/input-parser.js";
import type { RenderObject } from "../tree/render-object.js";
import { HitTestResult, type HitTestEntry } from "./hit-test.js";
import type { TuiController } from "../tui/tui-controller.js";

/**
 * 鼠标事件处理器。
 */
export type MouseHandler = (event: MouseEvent, entry: HitTestEntry) => void;

/**
 * MouseManager — 鼠标事件管理器单例。
 *
 * 逆向: ha in tui-layout-engine.js:1120
 *
 * 使用 HitTestResult 对渲染树执行命中测试，
 * 将鼠标事件分发到命中的 RenderObject。
 */
export class MouseManager {
  private static _instance: MouseManager | null = null;

  private _rootRenderObject: RenderObject | null = null;
  private _tui: TuiController | null = null;
  private _lastHoverTargets: HitTestEntry[] = [];

  static get instance(): MouseManager {
    if (!MouseManager._instance) MouseManager._instance = new MouseManager();
    return MouseManager._instance;
  }

  setRootRenderObject(root: RenderObject): void {
    this._rootRenderObject = root;
  }

  setTui(tui: TuiController): void {
    this._tui = tui;
  }

  handleMouseEvent(event: MouseEvent): void {
    if (!this._rootRenderObject) return;
    const position = { x: event.x, y: event.y };
    const result = HitTestResult.hitTest(this._rootRenderObject, position);
    this._lastHoverTargets = [...result.hits];
    // 鼠标事件分发到命中的 RenderObject
    // 具体分发逻辑 (click/hover/scroll) 由 Widget 层处理
  }

  clearHoverState(): void {
    this._lastHoverTargets = [];
  }

  reestablishHoverState(): void {
    // 使用最后已知的鼠标位置重新进行命中测试
    // resize 后的 post-frame callback 中调用
  }

  dispose(): void {
    this._rootRenderObject = null;
    this._tui = null;
    this._lastHoverTargets = [];
    MouseManager._instance = null;
  }
}
```

## 测试策略

- **mouse-manager.test.ts (~8 tests)**:
  - 单例: MouseManager.instance 返回同一实例
  - setRootRenderObject: 设置根渲染对象
  - setTui: 设置 TuiController 引用
  - handleMouseEvent: 无 root 时静默返回
  - handleMouseEvent: 调用 HitTestResult.hitTest 进行命中测试
  - clearHoverState: 清空 _lastHoverTargets
  - dispose: 清空所有引用并重置 _instance
  - 多层 RenderObject: 命中测试返回正确目标列表
