---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 07
type: tdd
wave: B
depends_on: [12-01]
files_modified:
  - packages/tui/src/widgets/media-query.ts
  - packages/tui/src/widgets/media-query.test.ts
autonomous: true
requirements: [TUI-MEDIA-QUERY]
user_setup: []

must_haves:
  truths:
    - "MediaQueryData 类: { size: { width, height }, capabilities: TerminalCapabilities }"
    - "MediaQueryData.supportsEmojiWidth 便捷 getter"
    - "MediaQueryData.supportsSyncOutput 便捷 getter"
    - "MediaQuery 扩展 InheritedWidget，持有 data: MediaQueryData"
    - "MediaQuery.updateShouldNotify: 当 size 或 capabilities 变化时返回 true"
    - "MediaQuery.of(context): 静态方法，通过 dependOnInheritedWidgetOfExactType 获取 MediaQueryData"
    - "MediaQuery.sizeOf(context): 静态便捷方法，返回 size"
    - "MediaQuery.capabilitiesOf(context): 静态便捷方法，返回 capabilities"
    - "WidgetsBinding 用 MediaQuery 包裹 rootWidget，传递终端尺寸和能力信息"
    - "resize 时 WidgetsBinding 创建新 MediaQuery(newData, child) 更新 rootElement"
  artifacts:
    - "packages/tui/src/widgets/media-query.ts"
    - "packages/tui/src/widgets/media-query.test.ts"
  key_links:
    - "逆向参考: tui-layout-engine.js:1046-1058 (BM = MediaQueryData)"
    - "逆向参考: tui-render-pipeline.js:97-104 (createMediaQueryWrapper)"
    - "逆向参考: tui-render-pipeline.js:42-63 (processResizeIfPending)"
    - "依赖 12-01 InheritedWidget"
    - "依赖 12-05 TuiController (TerminalCapabilities 类型)"
    - "被 12-08 WidgetsBinding 在 createMediaQueryWrapper 中使用"
---

<objective>
实现 MediaQuery InheritedWidget — 向 Widget 树注入终端尺寸和能力信息。

Purpose: 让任何 Widget 通过 MediaQuery.of(context) 访问终端尺寸和能力
Output: `packages/tui/src/widgets/` 下 1 个新文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/tree/inherited-widget.ts
@packages/tui/src/tui/tui-controller.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `BM` | tui-layout-engine.js | 1046-1058 | MediaQueryData |
| `I9` | tui-layout-engine.js | (InheritedWidget) | MediaQuery |
| `createMediaQueryWrapper` | tui-render-pipeline.js | 97-104 | 包裹 root widget |
| `processResizeIfPending` | tui-render-pipeline.js | 42-63 | resize 时更新 MediaQuery |

## 参考实现

### media-query.ts

```typescript
import { InheritedWidget } from "../tree/inherited-widget.js";
import type { Widget, Element } from "../tree/element.js";
import type { TerminalCapabilities } from "../tui/tui-controller.js";

/**
 * MediaQueryData — 终端环境数据。
 *
 * 逆向: BM in tui-layout-engine.js:1046-1058
 */
export class MediaQueryData {
  readonly size: { width: number; height: number };
  readonly capabilities: TerminalCapabilities;

  constructor(
    size: { width: number; height: number },
    capabilities: TerminalCapabilities,
  ) {
    this.size = size;
    this.capabilities = capabilities;
  }

  get supportsEmojiWidth(): boolean {
    return this.capabilities.emojiWidth;
  }

  get supportsSyncOutput(): boolean {
    return this.capabilities.syncOutput;
  }
}

/**
 * MediaQuery — InheritedWidget 注入终端尺寸和能力。
 *
 * 逆向: I9 in tui-layout-engine.js
 *
 * @example
 * ```ts
 * // 消费侧
 * const data = MediaQuery.of(context);
 * const { width, height } = data.size;
 * ```
 */
export class MediaQuery extends InheritedWidget {
  readonly data: MediaQueryData;

  constructor(opts: { data: MediaQueryData; child: Widget }) {
    super({ child: opts.child });
    this.data = opts.data;
  }

  updateShouldNotify(oldWidget: MediaQuery): boolean {
    const oldData = oldWidget.data;
    return (
      this.data.size.width !== oldData.size.width ||
      this.data.size.height !== oldData.size.height ||
      this.data.capabilities.emojiWidth !== oldData.capabilities.emojiWidth ||
      this.data.capabilities.syncOutput !== oldData.capabilities.syncOutput
    );
  }

  /**
   * 从上下文获取 MediaQueryData。
   */
  static of(context: Element): MediaQueryData {
    const element = context.dependOnInheritedWidgetOfExactType(MediaQuery);
    if (!element) throw Error("MediaQuery not found in ancestor tree");
    return (element.widget as MediaQuery).data;
  }

  /**
   * 便捷方法: 获取终端尺寸。
   */
  static sizeOf(context: Element): { width: number; height: number } {
    return MediaQuery.of(context).size;
  }

  /**
   * 便捷方法: 获取终端能力。
   */
  static capabilitiesOf(context: Element): TerminalCapabilities {
    return MediaQuery.of(context).capabilities;
  }
}
```

## 测试策略

- **media-query.test.ts (~10 tests)**:
  - MediaQueryData 构造: size 和 capabilities 正确设置
  - MediaQueryData.supportsEmojiWidth getter
  - MediaQueryData.supportsSyncOutput getter
  - MediaQuery.createElement 返回 InheritedElement
  - MediaQuery.updateShouldNotify: size 变化返回 true
  - MediaQuery.updateShouldNotify: capabilities 变化返回 true
  - MediaQuery.updateShouldNotify: 相同数据返回 false
  - MediaQuery.of: 在祖先树中找到 MediaQuery
  - MediaQuery.of: 无 MediaQuery 时抛出 Error
  - MediaQuery.sizeOf / capabilitiesOf: 便捷方法正确返回
