---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 08
type: tdd
wave: C
depends_on: [12-01, 12-03, 12-04, 12-05, 12-06, 12-07]
files_modified:
  - packages/tui/src/binding/widgets-binding.ts
  - packages/tui/src/binding/index.ts
  - packages/tui/src/index.ts
  - packages/tui/src/binding/widgets-binding.test.ts
autonomous: true
requirements: [TUI-WIDGETS-BINDING]
user_setup: []

must_haves:
  truths:
    - "WidgetsBinding 为单例模式 (static _instance, static get instance)"
    - "构造时实例化: BuildOwner, PipelineOwner, FrameScheduler (已存在), FocusManager, MouseManager, TuiController"
    - "构造时注册 6 个 FrameScheduler 回调: frame-start, resize, build, layout, paint, render"
    - "构造时调用 Ly0 桥接: setBuildOwner + setPipelineOwner (已存在的 types.ts)"
    - "runApp(widget): async 方法 — 初始化 TUI → 等待能力 → 创建 MediaQuery wrapper → mount rootElement → 设置事件 → 等待退出"
    - "runApp: isRunning guard 防止重复调用"
    - "runApp: try/finally 确保 cleanup 执行"
    - "stop(): 设置 isRunning=false, resolve exitPromise"
    - "setupEventHandlers(): tui.onResize → pendingResize + requestFrame"
    - "setupEventHandlers(): tui.onKey → keyInterceptors → focusManager.handleKeyEvent → handleGlobalKeyEvent"
    - "setupEventHandlers(): tui.onMouse → mouseManager.handleMouseEvent"
    - "setupEventHandlers(): tui.onPaste → focusManager.handlePasteEvent"
    - "createMediaQueryWrapper(widget): 创建 MediaQuery(data, child=widget)"
    - "processResizeIfPending(): 更新 MediaQuery data, 标记根元素重建"
    - "beginFrame(): 判断是否需要 paint (dirty/needsLayout/needsPaint/forceRefresh)"
    - "paint(): flushPaint → renderRenderObject → screen"
    - "render(): screen diff → stdout"
    - "cleanup(): unmount → dispose all → tui.deinit()"
    - "addKeyInterceptor(fn): 返回 unsubscribe 函数 (command palette 等)"
    - "setRootElementMountedCallback(fn): 外部注入挂载完成回调"
    - "handleGlobalKeyEvent: Ctrl+Z → handleSuspend"
  artifacts:
    - "packages/tui/src/binding/widgets-binding.ts"
    - "packages/tui/src/binding/index.ts"
    - "packages/tui/src/index.ts (re-export)"
    - "packages/tui/src/binding/widgets-binding.test.ts"
  key_links:
    - "逆向参考: tui-layout-engine.js:1114-1193 (d9) + tui-render-pipeline.js:1-198"
    - "依赖 Wave A 全部 (InheritedWidget/FocusManager/HitTestResult) 和 Wave B 全部 (TuiController/MouseManager/MediaQuery)"
    - "复用已有: BuildOwner, PipelineOwner, FrameScheduler, setBuildOwner/setPipelineOwner"
    - "被 12-09 runApp() 和 12-13 interactive.ts 替换 依赖"
---

<objective>
实现 WidgetsBinding 单例 — 核心编排器，组合所有子系统并驱动 TUI 应用生命周期。

Purpose: 将 BuildOwner + PipelineOwner + FrameScheduler + FocusManager + MouseManager + TuiController 编排为完整的应用运行时
Output: `packages/tui/src/binding/` 下 2 个新文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/tree/build-owner.ts
@packages/tui/src/tree/pipeline-owner.ts
@packages/tui/src/tree/frame-scheduler.ts
@packages/tui/src/tree/types.ts
@packages/tui/src/focus/focus-manager.ts
@packages/tui/src/gestures/mouse-manager.ts
@packages/tui/src/tui/tui-controller.ts
@packages/tui/src/widgets/media-query.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `d9` | tui-layout-engine.js | 1114-1193 | WidgetsBinding 构造 + 字段 |
| `d9.runApp` | tui-layout-engine.js:1182 + tui-render-pipeline.js:1-27 | | runApp 完整流程 |
| `d9.setupEventHandlers` | tui-render-pipeline.js | 106-150 | 事件绑定 |
| `d9.processResizeIfPending` | tui-render-pipeline.js | 42-63 | resize 处理 |
| `d9.beginFrame` | tui-render-pipeline.js | 65-66 | 帧开始判断 |
| `d9.paint` | tui-render-pipeline.js | 71-84 | 绘制流程 |
| `d9.render` | tui-render-pipeline.js | 86-93 | 渲染到 stdout |
| `d9.cleanup` | tui-render-pipeline.js | 169-172 | 清理流程 |
| `d9.waitForExit` | tui-render-pipeline.js | 163-168 | Promise 等待退出 |

## 参考实现

### widgets-binding.ts (核心骨架)

```typescript
import { BuildOwner } from "../tree/build-owner.js";
import { PipelineOwner } from "../tree/pipeline-owner.js";
import { FrameScheduler } from "../tree/frame-scheduler.js";
import { setBuildOwner, setPipelineOwner } from "../tree/types.js";
import { FocusManager } from "../focus/focus-manager.js";
import { MouseManager } from "../gestures/mouse-manager.js";
import { TuiController } from "../tui/tui-controller.js";
import { MediaQuery, MediaQueryData } from "../widgets/media-query.js";
import type { Widget, Element } from "../tree/element.js";
import type { KeyEvent } from "../vt/input-parser.js";
import type { RenderObject } from "../tree/render-object.js";

/**
 * WidgetsBinding — TUI 应用核心编排器单例。
 *
 * 逆向: d9 in tui-layout-engine.js:1114-1193 + tui-render-pipeline.js:1-198
 *
 * 组合所有子系统 (BuildOwner, PipelineOwner, FrameScheduler, FocusManager,
 * MouseManager, TuiController) 并驱动 Widget → Element → RenderObject
 * 三棵树的完整生命周期。
 */
export class WidgetsBinding {
  private static _instance: WidgetsBinding | undefined;

  readonly frameScheduler: FrameScheduler;
  readonly buildOwner: BuildOwner;
  readonly pipelineOwner: PipelineOwner;
  readonly focusManager: FocusManager;
  readonly mouseManager: MouseManager;
  readonly tui: TuiController;

  private rootElement: Element | undefined;
  private isRunning = false;
  private rootElementMountedCallback?: (element: Element) => void;
  private forcePaintOnNextFrame = false;
  private shouldPaintCurrentFrame = false;
  private didPaintCurrentFrame = false;
  private pendingResizeEvent: { width: number; height: number } | null = null;
  private keyInterceptors: ((event: KeyEvent) => boolean)[] = [];
  private exitPromise: Promise<void> | null = null;
  private exitResolve: (() => void) | null = null;

  static get instance(): WidgetsBinding {
    return (WidgetsBinding._instance ??= new WidgetsBinding());
  }

  private constructor() {
    this.frameScheduler = FrameScheduler.instance;
    this.buildOwner = new BuildOwner();
    this.pipelineOwner = new PipelineOwner();
    this.focusManager = FocusManager.instance;
    this.mouseManager = MouseManager.instance;
    this.tui = new TuiController();

    this.registerFrameCallbacks();

    setBuildOwner({
      scheduleBuildFor: (e) => this.buildOwner.scheduleBuildFor(e),
    });
    setPipelineOwner({
      requestLayout: (r) => this.pipelineOwner.requestLayout(r),
      requestPaint: (r) => this.pipelineOwner.requestPaint(r),
      removeFromQueues: (r) => this.pipelineOwner.removeFromQueues(r),
    });
  }

  async runApp(widget: Widget): Promise<void> {
    if (this.isRunning) throw Error("App is already running");
    try {
      this.isRunning = true;
      this.tui.init();
      this.tui.enterAltScreen();
      await this.tui.waitForCapabilities(1000);
      const wrapper = this.createMediaQueryWrapper(widget);
      this.rootElement = wrapper.createElement();
      this.rootElement.mount();
      if (this.rootElementMountedCallback) {
        this.rootElementMountedCallback(this.rootElement);
      }
      this.updateRootRenderObject();
      this.mouseManager.setTui(this.tui);
      this.setupEventHandlers();
      this.frameScheduler.requestFrame();
      await this.waitForExit();
    } finally {
      await this.cleanup();
    }
  }

  stop(): void {
    this.isRunning = false;
    if (this.exitResolve) {
      this.exitResolve();
      this.exitResolve = null;
    }
  }

  setRootElementMountedCallback(fn: (element: Element) => void): void {
    this.rootElementMountedCallback = fn;
  }

  addKeyInterceptor(fn: (event: KeyEvent) => boolean): () => void {
    this.keyInterceptors.push(fn);
    return () => {
      const idx = this.keyInterceptors.indexOf(fn);
      if (idx !== -1) this.keyInterceptors.splice(idx, 1);
    };
  }

  // ... private methods: registerFrameCallbacks, setupEventHandlers,
  // createMediaQueryWrapper, processResizeIfPending, beginFrame, paint,
  // render, updateRootRenderObject, updateRootConstraints, waitForExit,
  // cleanup, handleGlobalKeyEvent, requestForcedPaintFrame, renderRenderObject
}
```

## 测试策略

- **widgets-binding.test.ts (~14 tests)**:
  - 注意: 大量终端操作需 mock。使用 mock TuiController 和 mock process.stdout
  - 单例: WidgetsBinding.instance 返回同一实例
  - 构造: 所有子系统实例化成功
  - 构造: 6 个 frame callbacks 注册到 FrameScheduler
  - 构造: setBuildOwner/setPipelineOwner 桥接调用
  - runApp: isRunning 防止重复调用
  - runApp: 创建 MediaQuery wrapper
  - runApp: rootElement mount 成功
  - runApp: rootElementMountedCallback 被调用 (验证参数为 Element)
  - stop: 设置 isRunning=false, resolve exitPromise
  - addKeyInterceptor: 返回 unsubscribe 函数
  - setupEventHandlers: onKey 路由 (interceptors → focusManager → global)
  - handleGlobalKeyEvent: Ctrl+Z 调用 tui.handleSuspend
  - cleanup: unmount + dispose all 子系统
  - processResizeIfPending: 更新 MediaQuery data 并重建
