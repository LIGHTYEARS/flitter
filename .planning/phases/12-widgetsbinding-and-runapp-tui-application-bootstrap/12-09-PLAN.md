---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 09
type: tdd
wave: C
depends_on: [12-08]
files_modified:
  - packages/tui/src/binding/run-app.ts
  - packages/tui/src/binding/index.ts
  - packages/tui/src/index.ts
  - packages/tui/src/binding/run-app.test.ts
autonomous: true
requirements: [TUI-RUN-APP]
user_setup: []

must_haves:
  truths:
    - "runApp(widget, options?) 为顶层异步函数"
    - "内部获取 WidgetsBinding.instance 并调用 binding.runApp(widget)"
    - "options.onRootElementMounted 可选回调，传递给 binding.setRootElementMountedCallback"
    - "runApp 是 @flitter/tui 的主要公开 API"
    - "函数签名: async function runApp(widget: Widget, options?: RunAppOptions): Promise<void>"
    - "RunAppOptions 接口: { onRootElementMounted?: (element: Element) => void }"
  artifacts:
    - "packages/tui/src/binding/run-app.ts"
    - "packages/tui/src/binding/index.ts (re-export)"
    - "packages/tui/src/index.ts (re-export)"
    - "packages/tui/src/binding/run-app.test.ts"
  key_links:
    - "逆向参考: tui-render-pipeline.js:199-203 (T1T)"
    - "依赖 12-08 WidgetsBinding"
    - "被 12-13 interactive.ts 替换 直接调用"
---

<objective>
实现 runApp() — 顶层便捷函数，@flitter/tui 的主要公开 API。

Purpose: 提供简洁的一行启动入口: `await runApp(myWidget)`
Output: `packages/tui/src/binding/` 下 1 个新文件 + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/binding/widgets-binding.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `T1T` | tui-render-pipeline.js | 199-203 | runApp 函数 |

## 参考实现

### run-app.ts

```typescript
import type { Widget, Element } from "../tree/element.js";
import { WidgetsBinding } from "./widgets-binding.js";

/**
 * runApp 选项。
 */
export interface RunAppOptions {
  onRootElementMounted?: (element: Element) => void;
}

/**
 * runApp — 启动 TUI 应用的顶层入口。
 *
 * 逆向: T1T in tui-render-pipeline.js:199-203
 *
 * @example
 * ```ts
 * import { runApp } from "@flitter/tui";
 * await runApp(new MyAppWidget());
 * ```
 */
export async function runApp(
  widget: Widget,
  options?: RunAppOptions,
): Promise<void> {
  const binding = WidgetsBinding.instance;
  if (options?.onRootElementMounted) {
    binding.setRootElementMountedCallback(options.onRootElementMounted);
  }
  await binding.runApp(widget);
}
```

## 测试策略

- **run-app.test.ts (~4 tests)**:
  - runApp 调用 WidgetsBinding.instance.runApp (mock WidgetsBinding)
  - onRootElementMounted 选项传递给 setRootElementMountedCallback
  - 无 options 时不调用 setRootElementMountedCallback
  - runApp 返回 Promise<void> (异步函数签名)
