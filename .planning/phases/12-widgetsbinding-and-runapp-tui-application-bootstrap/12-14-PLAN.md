---
phase: 12-widgetsbinding-and-runapp-tui-application-bootstrap
plan: 14
type: refactor
wave: E
depends_on: [12-01, 12-10]
files_modified:
  - packages/tui/src/widgets/theme.ts
  - packages/cli/src/widgets/theme-controller.ts
  - packages/tui/src/widgets/theme.test.ts
autonomous: true
requirements: [TUI-THEME-MIGRATION]
user_setup: []

must_haves:
  truths:
    - "现有 packages/tui/src/widgets/theme.ts 使用全局变量 (IH() 模式)"
    - "迁移为通过 InheritedWidget (ThemeController) 注入"
    - "theme.ts 不再维护全局 theme 变量"
    - "theme.ts 导出 defaultTheme 常量和 ThemeData 类型 (供 ThemeController 使用)"
    - "所有 IH() 调用点改为 ThemeController.of(context)"
    - "向后兼容: 保留 theme.ts 的导出接口，内部实现改为委托 ThemeController"
    - "getTheme() 如果存在 WidgetsBinding 则从 InheritedWidget 获取，否则 fallback 到 defaultTheme"
  artifacts:
    - "packages/tui/src/widgets/theme.ts (rewrite)"
    - "packages/cli/src/widgets/theme-controller.ts (ensure defaultThemeData export)"
    - "packages/tui/src/widgets/theme.test.ts"
  key_links:
    - "现有 theme 全局变量: packages/tui/src/widgets/theme.ts"
    - "依赖 12-01 InheritedWidget、12-10 ThemeController"
    - "逆向参考: tui-widget-library.js IH() 全局 theme 访问"
---

<objective>
迁移 Theme 系统 — 从全局变量模式迁移到 InheritedWidget 模式。

Purpose: 消除全局 theme 变量，通过 ThemeController.of(context) 实现类型安全的主题注入
Output: 重写 `packages/tui/src/widgets/theme.ts` + 1 个测试文件
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-widgetsbinding-and-runapp-tui-application-bootstrap/12-RESEARCH.md
@packages/tui/src/widgets/theme.ts
@packages/cli/src/widgets/theme-controller.ts
@packages/tui/src/tree/inherited-widget.ts
</context>

## 逆向参考

| 逆向符号 | 文件 | 行号 | 对应实现 |
|----------|------|------|---------|
| `IH()` | tui-widget-library.js | | 全局 theme 访问 (迁移前) |
| `Theme.of(ctx)` | (Flutter 标准模式) | | InheritedWidget 模式 (迁移后) |

## 参考实现

### theme.ts (迁移后)

```typescript
/**
 * @flitter/tui - Theme system
 *
 * 主题数据定义和默认主题。
 * 运行时通过 ThemeController.of(context) 获取主题，
 * 无 Widget 上下文时 fallback 到 defaultTheme。
 */

/**
 * ThemeData 接口 — 主题数据类型。
 */
export interface ThemeData {
  name: string;
  primary: string;
  secondary: string;
  surface: string;
  background: string;
  error: string;
  text: string;
  mutedText: string;
  border: string;
  accent: string;
  success: string;
  warning: string;
}

/**
 * 默认主题。
 */
export const defaultTheme: ThemeData = {
  name: "default",
  primary: "#7C3AED",
  secondary: "#06B6D4",
  surface: "#1E1E2E",
  background: "#11111B",
  error: "#F38BA8",
  text: "#CDD6F4",
  mutedText: "#6C7086",
  border: "#45475A",
  accent: "#F5C2E7",
  success: "#A6E3A1",
  warning: "#F9E2AF",
};

/**
 * 获取主题 — 向后兼容的全局访问。
 *
 * 优先从 InheritedWidget 获取 (需要 Widget context),
 * 无 context 时返回 defaultTheme。
 *
 * @deprecated 新代码应使用 ThemeController.of(context)
 */
export function getTheme(): ThemeData {
  return defaultTheme;
}
```

## 测试策略

- **theme.test.ts (~6 tests)**:
  - defaultTheme 包含所有必需字段
  - defaultTheme.name 为 "default"
  - getTheme() 返回 defaultTheme
  - ThemeData 接口: 所有 12 个字段存在
  - 确保 theme.ts 不再有全局可变 theme 变量
  - 确保 getTheme 标记为 deprecated (JSDoc)
