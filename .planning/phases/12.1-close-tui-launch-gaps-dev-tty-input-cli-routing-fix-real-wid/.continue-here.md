---
context: phase
phase: 12.1-close-tui-launch-gaps-dev-tty-input-cli-routing-fix-real-wid
task: post-execution fixes
total_tasks: 4
status: in_progress
last_updated: 2026-04-15T08:26:50.760Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: amp-reference-mandatory — All functional code MUST cross-reference amp-cli-reversed/ source before implementation — see CLAUDE.md rule #1
- [ ] CONSTRAINT: real-execution-verification — Unit tests with mocks are insufficient. Must verify in real terminal before declaring TUI work complete — see CLAUDE.md rule #2

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| naive-implementation | Agents wrote `getSize()` using `?? 24` instead of amp's 4-layer defense (`_refreshSize` → truthy check → `getWindowSize` → cached fallback). `process.stdout.rows` returns `Infinity` in Bun, bypassing `??`. | blocking | CLAUDE.md rule #1: read amp-cli-reversed/ source for every function. Subagent prompts must include this rule. |
| mock-only-verification | 100+ unit tests passed, but TUI never launched. Every agent declared success based on mocked tests. The bin entry point was still a placeholder `console.log`. | blocking | CLAUDE.md rule #2: every phase producing runnable output must include real execution test. |
| parallel-agent-blind-spots | Plans 03 and 04 both independently created `status-bar.ts`. ThreadStateWidget.build() didn't forward `inferenceState`/`error` to ConversationView. No agent verified cross-plan integration. | advisory | Orchestrator should run integration spot-checks after worktree merges, not just file existence checks. |

<current_state>
Phase 12.1 is marked complete in ROADMAP/STATE. All 5 plans executed, all SUMMARYs written.
However, the TUI does NOT actually launch — it crashes with `height=Infinity` in `TuiController.getSize()`.

The root cause is diagnosed:
- `tui-controller.ts:330-335` uses `process.stdout.rows ?? 24`
- Bun returns `Infinity` for `process.stdout.rows`, and `??` only catches null/undefined
- amp's reference impl at `amp-cli-reversed/framework/tui-layout-engine.js:413-426` uses 4-layer defense

CLAUDE.md has been created with two mandatory rules to prevent recurrence.
Code review found 2 critical + 6 warning findings (12.1-REVIEW.md).
</current_state>

<completed_work>

Completed Plans (all 5):
- Plan 12.1-01: TtyInputSource with /dev/tty + stdin fallback, TuiController refactored (37 tests)
- Plan 12.1-02: RenderScrollable RenderBox + Scrollable widget + barrel re-exports
- Plan 12.1-03: ConversationView markdown rendering, InputField border/cursor, StatusBar (39 tests)
- Plan 12.1-04: CLI context routing S8() fix, ThreadStateWidget reactive, interactive.ts assembly (36 tests)
- Plan 12.1-05: E2E tmux-capture tests, bin/flitter.ts entry point fixed

Post-execution fixes:
- Fixed bin/flitter.ts placeholder → proper main() call
- Fixed ThreadStateWidget.build() missing inferenceState/error forwarding to ConversationView
- Created CLAUDE.md with mandatory amp cross-reference and integration verification rules
- Diagnosed getSize() Infinity root cause (debug session: .planning/debug/tui-height-infinity.md)
</completed_work>

<remaining_work>

1. **Fix TuiController.getSize()** — Port amp's `Uk0()` from `amp-cli-reversed/framework/tui-layout-engine.js:413-426`:
   - Call `process.stdout._refreshSize?.()` before reading
   - Truthy check `isTTY && columns && rows` (not just nullish)
   - Fallback to `getWindowSize?.()`
   - Cache last known good size, only update on valid reading
   - Add `Number.isFinite()` guard

2. **Fix CR-01/CR-02** — async cleanup races in `tui-controller.ts`:
   - `cleanup()` is sync but calls async `deinit()` — Promise silently discarded
   - `handleSuspend()` calls `deinit()` without await then sends SIGTSTP

3. **Fix WR-01 through WR-06** — code review warnings (hardcoded widths, unsafe casts, etc.)

4. **Human TTY verification** — No human has ever seen the TUI render. Must verify after getSize() fix.
</remaining_work>

<decisions_made>

- Created CLAUDE.md with 2 mandatory rules: (1) amp cross-reference required, (2) integration verification required
- Kept plan 03's status-bar.ts over plan 04's duplicate during merge conflict
- Fixed verification gaps inline (inferenceState/error wiring) rather than full gap-closure cycle
</decisions_made>

<blockers>
- TUI crashes on launch: `getSize()` returns `height=Infinity` — fix requires porting amp's `Uk0()` pattern
- Async cleanup races (CR-01/CR-02) may leave terminal in raw mode on SIGINT
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — New mandatory rules for this project
2. `.planning/debug/tui-height-infinity.md` — Full root cause analysis with amp reference
3. `amp-cli-reversed/framework/tui-layout-engine.js:413-426` — amp's `Uk0()` terminal size detection
4. `amp-cli-reversed/framework/clipboard-and-input.js:558-595` — amp's `XXT.init()` / `updateTerminalSize()`
5. `.planning/phases/12.1-close-tui-launch-gaps-dev-tty-input-cli-routing-fix-real-wid/12.1-REVIEW.md` — Code review findings

## Infrastructure State
- No running services
- Bun runtime (not Node) — `process.stdout.rows` returns `Infinity` instead of `undefined`

<context>
Phase 12.1 was executed with 5 parallel/sequential agents across 3 waves. All agents produced working code at the unit-test level but none verified integration. The fundamental lesson is that agent teams need structural enforcement of amp cross-referencing and real-world execution testing. CLAUDE.md now encodes this.
</context>

<next_action>
Start with: Fix `TuiController.getSize()` at `packages/tui/src/tui/tui-controller.ts:330-335` by porting amp's `Uk0()` pattern from `amp-cli-reversed/framework/tui-layout-engine.js:413-426`. Then run `bun packages/cli/bin/flitter.ts` in a real terminal to verify TUI launches.
</next_action>
