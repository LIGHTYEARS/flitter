---
phase: 12.1-close-tui-launch-gaps
plan: 01
subsystem: tui
tags: [dev-tty, terminal-input, tty-readstream, raw-mode, signal-cleanup]

# Dependency graph
requires:
  - phase: 04-tui-tree
    provides: TuiController base class with InputParser, Screen, AnsiRenderer
provides:
  - TtyInputSource interface + /dev/tty primary input (createDevTtyInput)
  - TtyOutputTarget interface + stdout/stderr/dev-tty fallback (createTtyOutput)
  - createTtyInput router with Bun version check + /dev/tty + stdin fallback
  - isBunWithTtyBug() Bun < 1.2.22 version check
  - stripAnsiEscapes() ANSI escape sequence removal
  - Refactored TuiController using TtyInputSource/TtyOutputTarget (no process.stdin)
affects: [12.1-02, 12-widgetsbinding-and-runapp, interactive-mode]

# Tech tracking
tech-stack:
  added: []
  patterns: [/dev/tty direct input for terminal apps, stdin fallback with version gating, output target cascade]

key-files:
  created:
    - packages/tui/src/tui/tty-input.ts
    - packages/tui/src/tui/tty-input.test.ts
  modified:
    - packages/tui/src/tui/tui-controller.ts
    - packages/tui/src/tui/index.ts

key-decisions:
  - "/dev/tty primary with stdin fallback matches amp's eXT/Zu0/Ju0 pattern"
  - "createTtyInput catches ENXIO and falls back to stdin for CI/sandbox environments"
  - "stdin.unref() in non-TTY fallback prevents test process hangs"

patterns-established:
  - "TtyInputSource interface: uniform /dev/tty and stdin access with early input buffering"
  - "TtyOutputTarget interface: stdout > stderr > /dev/tty write cascade"
  - "deinit() cleanup wrapped in try/catch per threat model T-12.1-03"

requirements-completed: [GAP-01]

# Metrics
duration: 14min
completed: 2026-04-15
---

# Phase 12.1 Plan 01: /dev/tty Input Strategy Summary

**/dev/tty terminal input source with stdin fallback, output target cascade, and TuiController refactor removing all process.stdin references**

## Performance

- **Duration:** 14 min
- **Started:** 2026-04-15T06:30:10Z
- **Completed:** 2026-04-15T06:44:05Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created TtyInputSource module with /dev/tty primary input (per amp's Zu0/Ju0/eXT pattern)
- Implemented output target cascade: stdout > stderr > /dev/tty write (per amp's hy0)
- Added Bun version check (isBunWithTtyBug) for Bun < 1.2.22 compatibility
- Refactored TuiController to use TtyInputSource/TtyOutputTarget, removing all direct process.stdin references
- Added early input buffer with ANSI escape stripping for pre-init keystrokes
- Signal cleanup chain properly disposes /dev/tty file descriptors

## Task Commits

Each task was committed atomically:

1. **Task 1: Create TtyInputSource module** - `1d158df` (feat)
2. **Task 2: Refactor TuiController** - `2c4e243` (feat)

## Files Created/Modified
- `packages/tui/src/tui/tty-input.ts` - TtyInputSource/TtyOutputTarget interfaces, createDevTtyInput, createStdinFallback, createTtyOutput, createTtyInput router, isBunWithTtyBug, stripAnsiEscapes
- `packages/tui/src/tui/tty-input.test.ts` - 20 tests covering Bun version check, ANSI stripping, /dev/tty input, stdin fallback, output target
- `packages/tui/src/tui/tui-controller.ts` - Refactored init/deinit/render/enterAltScreen/exitAltScreen/enableMouse/enableBracketedPaste to use TtyInputSource/TtyOutputTarget
- `packages/tui/src/tui/index.ts` - Barrel exports for TtyInputSource, TtyOutputTarget, createTtyInput, createTtyOutput, isBunWithTtyBug, stripAnsiEscapes

## Decisions Made
- /dev/tty primary with stdin fallback matches amp's eXT/Zu0/Ju0 pattern exactly
- createTtyInput catches ENXIO gracefully and falls back to stdin for CI/sandbox environments without /dev/tty
- Non-TTY stdin fallback calls stdin.unref() to prevent test process hangs (wrapped in try/catch for Bun compat)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added /dev/tty ENXIO fallback in createTtyInput()**
- **Found during:** Task 2 (TuiController refactor)
- **Issue:** Sandbox/CI environment has no /dev/tty device, causing ENXIO error on fs.openSync
- **Fix:** Wrapped createDevTtyInput() in try/catch in createTtyInput() router, falling back to createStdinFallback() on error
- **Files modified:** packages/tui/src/tui/tty-input.ts
- **Verification:** All 37 tests pass in sandbox environment
- **Committed in:** 2c4e243

**2. [Rule 3 - Blocking] Added stdin.unref() in non-TTY fallback to prevent test hangs**
- **Found during:** Task 2 (TuiController refactor)
- **Issue:** process.stdin data listener kept event loop alive, causing test process to hang indefinitely
- **Fix:** Added stdin.unref() in non-TTY path of createStdinFallback.init(), wrapped in try/catch for Bun compat
- **Files modified:** packages/tui/src/tui/tty-input.ts
- **Verification:** Test process exits cleanly, all 37 tests pass
- **Committed in:** 2c4e243

---

**Total deviations:** 2 auto-fixed (2 blocking)
**Impact on plan:** Both fixes necessary for test/CI compatibility. No scope creep -- the /dev/tty fallback matches amp's actual behavior.

## Issues Encountered
None beyond the auto-fixed deviations above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- TtyInputSource and TtyOutputTarget are ready for WidgetsBinding integration
- TuiController now correctly reads from /dev/tty, enabling interactive TUI launch
- Output target cascade ensures terminal output works even when stdout is redirected
- Plan 02 (CLI routing fix) can proceed independently

## Self-Check: PASSED

- All 4 created/modified files exist on disk
- Both task commits (1d158df, 2c4e243) found in git log
- SUMMARY.md exists at expected path

---
*Phase: 12.1-close-tui-launch-gaps, Plan: 01*
*Completed: 2026-04-15*
