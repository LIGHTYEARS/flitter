---
phase: 12.1-close-tui-launch-gaps
plan: 03
subsystem: cli
tags: [status-bar, conversation-view, input-field, markdown-rendering, visual-fidelity, tui-widgets]

requires:
  - phase: 06-tui-advanced
    provides: MarkdownParser, MarkdownRenderer, TextEditingController
  - phase: 12-widgetsbinding
    provides: StatefulWidget, StatelessWidget, FocusNode, FocusManager
  - phase: 12.1-02
    provides: Layout widget barrel re-exports from @flitter/tui

provides:
  - StatusBar StatelessWidget with model name + token count display
  - ConversationView with markdown rendering via MarkdownParser + MarkdownRenderer pipeline
  - InputField with box-drawing border, cursor inverse video, placeholder text
  - Role indicators with bold colored prefixes (You/Assistant/System)
  - Error display with retry prompt
  - Streaming indicator when inference running

affects: [12.1-04, 12.1-05, cli-interactive-mode]

tech-stack:
  added: []
  patterns: [micromark markdown pipeline for message rendering, role-specific color coding, box-drawing border with focus state, inverse video cursor]

key-files:
  created:
    - packages/cli/src/widgets/status-bar.ts
    - packages/cli/src/widgets/status-bar.test.ts
    - packages/cli/src/widgets/conversation-view.test.ts
  modified:
    - packages/cli/src/widgets/conversation-view.ts
    - packages/cli/src/widgets/input-field.ts
    - packages/cli/src/widgets/input-field.test.ts
    - packages/cli/src/widgets/index.ts

key-decisions:
  - "StatusBar uses Row with Expanded spacer for left/right alignment"
  - "ConversationView reuses MarkdownParser + MarkdownRenderer instances across builds"
  - "Role indicator rendered as TextSpan child within same RichText as content for layout efficiency"
  - "InputField border width defaults to 78 (80-col terminal minus 2 border chars)"
  - "Cursor rendered as inverse video via TextStyle background swap"
  - "Tests rewritten from bun:test to node:test for sandbox compatibility"

patterns-established:
  - "Color constants defined as module-level Color.rgb() for Tokyo Night palette"
  - "TDD with node:test + node:assert/strict for CLI widget testing"
  - "Widget tree introspection via recursive extractAllText/collectRichTexts helpers"

requirements-completed: [GAP-03]

duration: 11min
completed: 2026-04-15
---

# Phase 12.1 Plan 03: Widget Visual Fidelity (StatusBar + ConversationView + InputField) Summary

**Full visual fidelity for StatusBar (model/tokens), ConversationView (markdown rendering, role indicators, error display), and InputField (box-drawing border, cursor, placeholder) with 39 tests passing**

## Performance

- **Duration:** 11 min
- **Started:** 2026-04-15T06:58:37Z
- **Completed:** 2026-04-15T07:09:27Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Created StatusBar StatelessWidget: model name left-aligned, token count right-aligned, mutedText #565f89 color, Row + Expanded spacer layout
- Rewrote ConversationView with full markdown pipeline: MarkdownParser.parse() + MarkdownRenderer.render() for message content
- Role indicators: "You: " primary #7aa2f7 bold, "Assistant: " accent #bb9af7 bold, "System: " secondary #9ece6a bold
- Empty state: "No messages yet. Type below to begin." in mutedText color
- Error display: "Error:" prefix in error #f7768e bold + error message + "Press Enter to retry or Ctrl+C to exit." retry prompt
- Streaming indicator: "..." appended when inferenceState is "running"
- Rewrote InputField with box-drawing border (focused #7aa2f7, unfocused #3b4261), placeholder "Type a message..." in #565f89, cursor as inverse video
- Added Shift+Enter multi-line support, dynamic height clamped 1-5 rows
- Barrel export StatusBar + StatusBarConfig from widgets/index.ts
- Rewrote all test files from bun:test to node:test for sandbox compatibility

## Task Commits

Each task was committed atomically (TDD: RED then GREEN):

1. **Task 1: StatusBar + ConversationView (TDD)**
   - `6d8a8d1` (test) -- RED: failing tests for StatusBar and ConversationView
   - `1f7421a` (feat) -- GREEN: implement StatusBar and rewrite ConversationView with markdown rendering
2. **Task 2: InputField visual fidelity (TDD)**
   - `07d5751` (test) -- RED: failing tests for InputField visual fidelity
   - `417c1de` (feat) -- GREEN: rewrite InputField with border, cursor, placeholder

## Files Created/Modified

- `packages/cli/src/widgets/status-bar.ts` -- StatusBar StatelessWidget: Row layout with model name + Expanded spacer + token count, mutedText color
- `packages/cli/src/widgets/status-bar.test.ts` -- 7 tests: StatelessWidget inheritance, config, model name/token count rendering, mutedText color, Row structure
- `packages/cli/src/widgets/conversation-view.ts` -- Full rewrite: MarkdownParser + MarkdownRenderer pipeline, role indicators, empty state, error display, streaming indicator
- `packages/cli/src/widgets/conversation-view.test.ts` -- 13 tests: role prefixes with colors, markdown rendering, empty state, error display, streaming indicator
- `packages/cli/src/widgets/input-field.ts` -- Full rewrite: box-drawing border, placeholder, cursor inverse video, Shift+Enter, 1-5 row height
- `packages/cli/src/widgets/input-field.test.ts` -- 19 tests: border chars, placeholder, mutedText color, cursor, focus colors, Shift+Enter, key handling
- `packages/cli/src/widgets/index.ts` -- Added StatusBar + StatusBarConfig barrel export

## Decisions Made

- StatusBar uses Row with Expanded spacer for left/right alignment (consistent with UI-SPEC layout contract)
- ConversationView reuses MarkdownParser and MarkdownRenderer instances across builds for efficiency
- Role indicator rendered as TextSpan child within the same RichText as content (single RenderParagraph per message)
- InputField border width defaults to 78 columns (80-col terminal minus 2 border characters); MediaQuery wiring deferred to integration plan
- Cursor rendered as inverse video via TextStyle with background=TEXT_COLOR and foreground=BG_COLOR
- All tests rewritten from bun:test to node:test + node:assert/strict for sandbox compatibility (sandbox has Node.js, not Bun)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Rewrote tests from bun:test to node:test**
- **Found during:** Task 1 (RED phase)
- **Issue:** Sandbox environment uses Node.js v24, not Bun. Tests importing from `bun:test` fail with ERR_UNSUPPORTED_ESM_URL_SCHEME
- **Fix:** Rewrote all test files to use `node:test` (describe/it/afterEach) and `node:assert/strict` instead of bun:test expectations
- **Files modified:** status-bar.test.ts, conversation-view.test.ts, input-field.test.ts
- **Committed in:** 6d8a8d1, 07d5751

---

**Total deviations:** 1 auto-fixed (blocking)
**Impact on plan:** Test framework change only. No functional scope change.

## Issues Encountered

None beyond the test framework deviation above.

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- StatusBar, ConversationView, InputField all have full visual fidelity
- ConversationView integrates with MarkdownParser + MarkdownRenderer from Phase 6
- InputField supports multi-line editing via Shift+Enter
- All three widgets ready for composition in ThreadStateWidget.build() (Plan 04)
- Border width hardcoded to 78; MediaQuery integration needed in Plan 04/05

## Self-Check: PASSED

- All 7 created/modified files exist on disk
- All 4 task commits (6d8a8d1, 1f7421a, 07d5751, 417c1de) found in git log
- SUMMARY.md exists at expected path

---
*Phase: 12.1-close-tui-launch-gaps, Plan: 03*
*Completed: 2026-04-15*
