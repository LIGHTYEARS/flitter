# Phase 12.1: Close TUI Launch Gaps - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-15
**Phase:** 12.1-close-tui-launch-gaps
**Areas discussed:** Terminal input strategy, Widget tree composition, Reactive data flow, Placeholder scope

---

## Terminal Input Strategy

### TuiController I/O

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's /dev/tty strategy exactly | Open /dev/tty for input with tty.ReadStream + setRawMode, fallback to process.stdin for Bun < 1.2.22. Output uses process.stdout, fallback to /dev/tty write. | ✓ |
| /dev/tty input only, stdout as-is | Use /dev/tty for input only, keep process.stdout for output. Simpler, covers 99% of cases. | |
| Fix CLI routing only | Keep process.stdin but fix resolveCliContext. Simplest change but diverges from amp. | |

**User's choice:** Port amp's /dev/tty strategy exactly
**Notes:** None

### CLI Mode Routing

| Option | Description | Selected |
|--------|-------------|----------|
| Match amp's S8() logic exactly | executeMode = !!opts.execute \|\| (!process.stdout.isTTY && !opts.streamJson). isTTY only checks stdout. | ✓ |
| Amp logic + --interactive override | Same as amp but add --interactive flag for non-TTY environments. | |

**User's choice:** Match amp's S8() logic exactly
**Notes:** None

### Bun Compatibility

| Option | Description | Selected |
|--------|-------------|----------|
| Port Bun version check from amp | Amp checks Bun.version < 1.2.22. Port exact version check with fallback. | ✓ |
| Try/catch with auto-fallback | Try /dev/tty first, catch errors, fall back. No hardcoded version. | |
| Assume /dev/tty, fail if unavailable | Just assume /dev/tty works. Fail hard otherwise. | |

**User's choice:** Port Bun version check from amp
**Notes:** None

### Signal Handling

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's signal handler chain | Graceful SIGINT/SIGTERM with alt screen exit, raw mode restore, ReadStream close. | ✓ |
| Verify existing handlers work | Current handlers exist — just verify with /dev/tty. | |

**User's choice:** Port amp's signal handler chain
**Notes:** None

---

## Widget Tree Composition

### Tree Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's _70() tree exactly | AppWidget → ThemeController → ConfigProvider → ConversationLayout (Column with ConversationView + InputField + status bar) | ✓ |
| Minimal Column layout | AppWidget → Column(ConversationView, InputField). No wrappers. | |
| Extend current tree | Current tree + add ConversationView above InputField. | |

**User's choice:** Port amp's _70() tree exactly
**Notes:** None

### Layout

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's full layout | Includes separator lines, status bar, proper vertical distribution. | ✓ |
| Flex column: expanded + fixed | Column(Expanded(ConversationView), InputField). Standard approach. | |

**User's choice:** Port amp's full layout
**Notes:** None

### Status Bar

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's status bar | Model name, token count, thread info. | ✓ |
| Skip status bar | Defer to later phase. | |
| Minimal model indicator | Model name only. | |

**User's choice:** Port amp's status bar
**Notes:** None

### Scrolling

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's scroll | Full fidelity from tui-widget-framework.js. | ✓ |
| Adapt Phase 6 Scrollable | Non-standard performLayout needs adapter work. | |
| Minimal scroll clip | Simple ClipRenderObject with offset. No virtualization. | |

**User's choice:** Port amp's scroll
**Notes:** None

---

## Reactive Data Flow

### State Propagation

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's reactive pattern | From html-sanitizer-repl.js k40() — ThreadWorker events to widget state. | ✓ |
| setState on store/worker events | ThreadStateWidget subscribes to events$, calls setState. | |
| InheritedWidget propagation | Propagate ThreadSnapshot via InheritedWidget. | |

**User's choice:** Port amp's reactive pattern
**Notes:** None

### Streaming Display

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's streaming approach | How SSE deltas propagate through widget tree. | ✓ |
| Per-delta setState, frame batching | Standard Flutter: setState per delta, FrameScheduler batches at 16ms. | |
| Buffered flush | Accumulate deltas, flush every N ms. | |

**User's choice:** Port amp's streaming approach
**Notes:** None

### Error Display

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's error display | Inline error in conversation with retry, red-highlighted. | ✓ |
| Minimal error display | stderr fallback + simple message. | |
| Defer error handling | Errors crash TUI. Fix later. | |

**User's choice:** Port amp's error display
**Notes:** None

### Thread Initialization

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's thread init flow | --continue flag, thread picker, welcome message. | ✓ |
| Always new thread | Simple, no resume support. | |
| New + resume via flag | New by default, --continue <id> to resume. No picker. | |

**User's choice:** Port amp's thread init flow
**Notes:** None

---

## Placeholder Scope

### Visual Fidelity

| Option | Description | Selected |
|--------|-------------|----------|
| Full visual fidelity | Markdown rendering, cursor/border/placeholder, status bar, proper spacing. | ✓ |
| Functional plain text | Role prefix + plain text. No markdown, borders, or colors. | |
| Port amp's exact visuals | Full port of amp's visual appearance. | |

**User's choice:** Full visual fidelity
**Notes:** User selected preview showing markdown rendering, code blocks, inline formatting, role indicators, cursor, border, status bar.

### Verification

| Option | Description | Selected |
|--------|-------------|----------|
| Both automated + human | tmux-capture visual regression + human UAT checklist. | ✓ |
| tmux-capture only | Automated visual regression. | |
| Human UAT only | Manual terminal verification. | |

**User's choice:** Both automated + human
**Notes:** tmux-capture tooling already exists in project.

### Bun Test Fixes

| Option | Description | Selected |
|--------|-------------|----------|
| Fix Bun test failures | Port amp's Bun compatibility handling for process.stdin.unref. | ✓ |
| Defer Bun fixes | Document as known issues. | |

**User's choice:** Fix Bun test failures
**Notes:** None

### Scroll RenderBox

| Option | Description | Selected |
|--------|-------------|----------|
| Port amp's scroll RenderObject | Rewrite matching amp's RenderObject integration. | ✓ |
| Rewrite as RenderBox subclass | Standard performLayout(BoxConstraints) signature. | |

**User's choice:** Port amp's scroll RenderObject
**Notes:** None

---

## Claude's Discretion

- Exact ANSI color codes for error highlighting
- Welcome message text content
- Thread picker UI layout details

## Deferred Ideas

None — all gaps addressed in this phase per user mandate.
