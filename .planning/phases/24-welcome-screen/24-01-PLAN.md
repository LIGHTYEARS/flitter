---
plan: 24-01
phase: 24
wave: 1
title: Build WelcomeScreen widget
depends_on:
  - 23
files_modified:
  - packages/flitter-cli/src/widgets/welcome-screen.ts
  - packages/flitter-cli/src/widgets/chat-view.ts
  - packages/flitter-cli/src/__tests__/welcome-screen.test.ts
autonomous: true
requirements:
  - WELC-01
  - WELC-02
---

## 目标

将 `chat-view.ts` 中的 `buildWelcomeScreen()` 内联函数替换为独立的 `WelcomeScreen` StatefulWidget。该 Widget 复用现有的 `DensityOrbWidget({ variant: 'welcome' })` 作为 ASCII Art Logo（满足 WELC-01 中的 Perlin 渐变动画），并在 Logo 右侧渲染三行静态提示文字（满足 WELC-02 的 Tab/Shift+Tab 导航提示）。布局、颜色、字符串均严格对齐 `tmux-capture/screens/amp/welcome/` golden 文件。

---

## 任务

<task id="24-01-T1">
<title>创建 welcome-screen.ts — WelcomeScreen StatefulWidget</title>

<read_first>
- packages/flitter-cli/src/widgets/density-orb-widget.ts — 确认 DensityOrbWidget 接口：只接受 `variant?: 'default' | 'welcome'`，无 agentMode prop；内部自管理 setInterval，外部无需计时器
- packages/flitter-cli/src/widgets/chat-view.ts L82-L133 — ChatViewState 中 AppState listener 注册/注销模式（initState/dispose）
- packages/flitter-cli/src/widgets/border-shimmer.ts L108-L147 — setInterval 动画模式参考
- packages/flitter-cli/src/state/app-state.ts — 确认 AppState.addListener / removeListener 签名；agentMode 字段名为 `currentMode: string | null`
- tmux-capture/screens/amp/welcome/plain-63x244.golden — 布局验证：Logo 左、提示右、三行提示确切字符串
- tmux-capture/screens/amp/welcome/ansi-63x244.golden — ANSI 颜色验证：各提示行颜色码
- packages/flitter-cli/src/themes/index.ts — CliThemeProvider 接口（build 中用于获取主题色，但 WelcomeScreen.build 使用固定 Color.* 常量即可）
</read_first>

<action>
在 `packages/flitter-cli/src/widgets/welcome-screen.ts` 新建文件，实现：

1. **WelcomeScreen** (StatefulWidget)
   - 接受 `opts: { appState: AppState }` 构造参数，存储为 `readonly appState: AppState`
   - `createState()` 返回 `new WelcomeScreenState()`

2. **WelcomeScreenState** (State<WelcomeScreen>)
   - 使用 `export class WelcomeScreenState extends State<WelcomeScreen>`（必须带 export，因为 T3 测试文件需直接导入）
   - `private _onChange: (() => void) | null = null`
   - `initState()`: 调用 `super.initState()`，注册 `this._onChange = () => { this.setState(); }` 到 `this.widget.appState.addListener(this._onChange)`
   - `dispose()`: 调用 `this.widget.appState.removeListener(this._onChange)`，设 `this._onChange = null`，再调 `super.dispose()`
   - `build(context)`: 返回以下精确 widget 树：

```
Center(
  child: Row(
    mainAxisSize: 'min',
    crossAxisAlignment: 'center',
    children: [
      DensityOrbWidget({ variant: 'welcome' }),
      SizedBox({ width: 4 }),
      Column(
        mainAxisSize: 'min',
        crossAxisAlignment: 'start',
        children: [
          /* 行1: "Welcome to Amp" — dim:true + Color.green
             NOTE: CONTEXT D-03 says 'dim foreground', RESEARCH §5 confirms the actual golden is green-ish. Use dim:true + Color.green. */
          Text({ text: TextSpan({
            text: 'Welcome to Amp',
            style: TextStyle({ foreground: Color.green, dim: true })
          })}),
          SizedBox({ height: 2 }),
          /* 行2: "Ctrl+O for help" — 三段 TextSpan */
          Text({ text: TextSpan({ children: [
            TextSpan({ text: 'Ctrl+O', style: TextStyle({ foreground: Color.blue }) }),
            TextSpan({ text: ' for ', style: TextStyle({ dim: true }) }),
            /* NOTE: CONTEXT D-03 says 'dim for help' but RESEARCH §5 ANSI analysis shows help is Color.yellow (ANSI 38;5;3), not dim. Use Color.yellow. */
            TextSpan({ text: 'help', style: TextStyle({ foreground: Color.yellow }) }),
          ]})}),
          SizedBox({ height: 2 }),
          /* 行3: Tab 提示 第一行 — Color.cyan */
          Text({ text: TextSpan({
            text: 'Use Tab/Shift+Tab to navigate to previous',
            style: TextStyle({ foreground: Color.cyan })
          })}),
          /* 行4: Tab 提示 第二行 — Color.cyan */
          Text({ text: TextSpan({
            text: 'messages to edit or restore to a previous state',
            style: TextStyle({ foreground: Color.cyan })
          })}),
        ],
      ),
    ],
  ),
)
```

3. **Import 列表**（仅使用已存在的路径，参照 density-orb-widget.ts 的 import 模式）：
   - `StatefulWidget, State, Widget, type BuildContext` from `'../../../flitter-core/src/framework/widget'`
   - `Column, Row` from `'../../../flitter-core/src/widgets/flex'`
   - `Center` from `'../../../flitter-core/src/widgets/center'`
   - `SizedBox` from `'../../../flitter-core/src/widgets/sized-box'`
   - `Text` from `'../../../flitter-core/src/widgets/text'`
   - `TextSpan` from `'../../../flitter-core/src/core/text-span'`
   - `TextStyle` from `'../../../flitter-core/src/core/text-style'`
   - `Color` from `'../../../flitter-core/src/core/color'`
   - `DensityOrbWidget` from `'./density-orb-widget'`
   - `type AppState` from `'../state/app-state'`

**关键约束：**
- DensityOrbWidget 不传 agentMode（该 prop 不存在）
- 不添加任何自管理的 setInterval（DensityOrbWidget 已内置动画）
- 提示字符串必须与 golden file 一字不差：
  - `'Welcome to Amp'`
  - `'Ctrl+O'`, `' for '`, `'help'`
  - `'Use Tab/Shift+Tab to navigate to previous'`
  - `'messages to edit or restore to a previous state'`
</action>

<acceptance_criteria>
1. 文件 `packages/flitter-cli/src/widgets/welcome-screen.ts` 存在
2. `rg -n "class WelcomeScreen extends StatefulWidget" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行
3. `rg -n "export.*WelcomeScreenState" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行
4. `rg -n "appState.addListener" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回至少 1 行（initState 中注册监听器）
5. `rg -n "appState.removeListener" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回至少 1 行（dispose 中注销监听器）
6. `rg -n "DensityOrbWidget" packages/flitter-cli/src/widgets/welcome-screen.ts | wc -l` 返回 >= 2（import 和实例化）
7. `rg -n "variant: 'welcome'" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行
8. `rg -n "agentMode" packages/flitter-cli/src/widgets/welcome-screen.ts | wc -l` 返回 0（不得有 agentMode）
9. `rg -n "setInterval" packages/flitter-cli/src/widgets/welcome-screen.ts | wc -l` 返回 0（不得自管理计时器）
10. `rg -n "Welcome to Amp" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行
11. `rg -n "dim: true" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 >= 1 行（"Welcome to Amp" dim 样式）
12. `rg -n "Use Tab/Shift\\+Tab to navigate to previous" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行
13. `rg -n "messages to edit or restore to a previous state" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行
14. `rg -n "Color.blue" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行（Ctrl+O 颜色）
15. `rg -n "Color.yellow" packages/flitter-cli/src/widgets/welcome-screen.ts` 返回 1 行（help 颜色）
16. `rg -n "Color.cyan" packages/flitter-cli/src/widgets/welcome-screen.ts | wc -l` 返回 2（Tab 提示两行均为 cyan）
17. `rg -n "mainAxisSize: 'min'" packages/flitter-cli/src/widgets/welcome-screen.ts | wc -l` 返回 2（Row 和 Column 各一个）
</acceptance_criteria>
</task>

<task id="24-01-T2">
<title>修改 chat-view.ts — 替换 buildWelcomeScreen() 为 WelcomeScreen widget</title>

<read_first>
- packages/flitter-cli/src/widgets/chat-view.ts L1-L45 — 现有 import 列表（确认哪些已存在，避免重复导入）
- packages/flitter-cli/src/widgets/chat-view.ts L115-L133 — screenState switch 分支，重点看 `case 'welcome':` 行（L119-L120）
- packages/flitter-cli/src/widgets/chat-view.ts L395-L446 — `buildWelcomeScreen()` 函数全文（需要整体删除）
- packages/flitter-cli/src/widgets/welcome-screen.ts — T1 刚创建的文件（确认导出名）
</read_first>

<action>
对 `packages/flitter-cli/src/widgets/chat-view.ts` 进行两处修改：

**修改 1：** 在文件顶部 import 区添加 WelcomeScreen 导入。
在现有的本地 widget import 行（如 `import { ToolCallWidget }` 附近）之后添加：
```typescript
import { WelcomeScreen } from './welcome-screen';
```

**修改 2：** 将 `case 'welcome':` 分支从调用 `buildWelcomeScreen()` 改为实例化 `WelcomeScreen`。
将：
```typescript
case 'welcome':
  return buildWelcomeScreen(this.widget.appState);
```
替换为：
```typescript
case 'welcome':
  return new WelcomeScreen({ appState: this.widget.appState });
```

**修改 3：** 删除整个 `buildWelcomeScreen()` 函数（L403-L446，共 44 行）。
删除从函数注释 `/**\n * Welcome screen — first launch, no history.` 到函数结束 `}` 的全部内容（含 JSDoc 注释）。

**注意：** 删除操作不可影响紧接其后的 `buildEmptyScreen()` 函数。
</action>

<acceptance_criteria>
1. `rg -n "import.*WelcomeScreen" packages/flitter-cli/src/widgets/chat-view.ts` 返回 1 行，路径为 `'./welcome-screen'`
2. `rg -n "new WelcomeScreen" packages/flitter-cli/src/widgets/chat-view.ts` 返回 1 行
3. `rg -n "buildWelcomeScreen" packages/flitter-cli/src/widgets/chat-view.ts | wc -l` 返回 0（函数调用和函数声明均已删除）
4. `rg -n "text: 'flitter-cli'" packages/flitter-cli/src/widgets/chat-view.ts | wc -l` 返回 0（单引号字符串字面量 "flitter-cli" 已删除）
5. `rg -n "buildEmptyScreen" packages/flitter-cli/src/widgets/chat-view.ts | wc -l` 返回 >= 2（函数声明和调用均完整保留）
6. `cd packages/flitter-cli && bun run typecheck 2>&1 | rg -c "error"` 返回 0（无 TypeScript 类型错误）
</acceptance_criteria>
</task>

<task id="24-01-T3">
<title>编写 welcome-screen.test.ts — 四组测试验证渲染、提示文字、生命周期、集成</title>

<read_first>
- packages/flitter-cli/src/__tests__/chat-view.test.ts L1-L151 — 测试工具函数全部定义：MockProvider、createTestAppState()、buildChatView()、findWidget()、findAllWidgets()；理解 stubContext 模式和 `(state as any)._widget = widget` 的 widget 绑定方式
- packages/flitter-cli/src/__tests__/chat-view.test.ts L157-L171 — Group 1 中 test 1.1 是需要被 Phase 24 集成测试替换/修改的测试（当前断言 `Center` 和 `'flitter-cli'`，Phase 24 完成后这些断言会失效）
- packages/flitter-cli/src/widgets/welcome-screen.ts — T1 创建的文件（WelcomeScreen、WelcomeScreenState export）
- packages/flitter-cli/src/widgets/density-orb-widget.ts — DensityOrbWidget class（用于 instanceof 检查）
- packages/flitter-cli/src/state/app-state.ts — AppState._listeners 访问方式（通过 `(appState as any)._listeners` 读取 Set.size）
</read_first>

<action>
新建文件 `packages/flitter-cli/src/__tests__/welcome-screen.test.ts`，包含以下四组测试：

**Group 1: WelcomeScreen 渲染路径**
- test 1.1 `builds without throwing`：创建 WelcomeScreen，调用 createState() → initState() → build(stubContext)，断言不抛出
- test 1.2 `contains DensityOrbWidget with variant welcome`：findAllWidgets(tree, DensityOrbWidget)，断言数组长度 >= 1 且 `[0].variant === 'welcome'`
- test 1.3 `layout wraps in Center`：断言 tree 根节点为 Center 实例

**Group 2: 提示文字内容验证**
- test 2.1 `contains Welcome to Amp text`：findAllWidgets(tree, Text)，检查某个 Text 的 TextSpan.text === `'Welcome to Amp'`
- test 2.2 `"Ctrl+O", " for ", "help" 三段 TextSpan 共享同一 RichText/Text 父节点（CONTEXT D-10）`：在 widget 树中找到一个 Text/RichText 节点，其 textSpan.children（或 children 数组）同时包含 `'Ctrl+O'`、`' for '`、`'help'` 三段内容：
  ```typescript
  const richText = findWidget(tree, (w) => w instanceof Text && (w as any).text?.children?.some((s: any) => s.text === 'Ctrl+O'));
  expect(richText).toBeTruthy();
  expect((richText as any).text.children.map((s: any) => s.text)).toContain(' for ');
  expect((richText as any).text.children.map((s: any) => s.text)).toContain('help');
  ```
- test 2.3 `contains for hint with dim style`：找到含 `' for '` 的 TextSpan，断言 `style.dim === true`
- test 2.4 `contains help with yellow foreground`：找到含 `'help'` 的 TextSpan，断言 foreground 等于 `Color.yellow`
- test 2.5 `contains Use Tab/Shift+Tab first line`：断言 tree 中存在文字 `'Use Tab/Shift+Tab to navigate to previous'`
- test 2.6 `contains second hint line`：断言 tree 中存在文字 `'messages to edit or restore to a previous state'`

**Group 3: 生命周期 / 清理路径**
- test 3.1 `initState registers AppState listener`：记录 `(appState as any)._listeners.size` before，执行 initState()，断言 size 增加了 1
- test 3.2 `dispose removes AppState listener (no leak)`：执行 initState() 后记录 size，再执行 dispose()，断言 size 恢复到 initState() 之前的值（严格相等）

**Group 4: 集成测试（chat-view 调用路径）**
- test 4.1 `welcome screenState in ChatView produces WelcomeScreen (not bare Center with flitter-cli)`：调用 buildChatView(appState) （需从 chat-view 重新导入 buildChatView 辅助，或在本文件内复制），断言 findAllWidgets(tree, DensityOrbWidget).length >= 1（从而证明 WelcomeScreen 已接管，而不是旧的 'flitter-cli' 文字 Center）
- test 4.2 `welcome tree no longer contains flitter-cli text`：findAllWidgets(tree, Text)，断言没有任何 Text 包含 'flitter-cli' 字符串

测试文件顶部需要的导入：
```typescript
import { describe, test, expect } from 'bun:test';
import type { BuildContext, Widget } from '../../../flitter-core/src/framework/widget';
import { Text } from '../../../flitter-core/src/widgets/text';
import { Center } from '../../../flitter-core/src/widgets/center';
import { Color } from '../../../flitter-core/src/core/color';
import { SessionState } from '../state/session';
import { PromptHistory } from '../state/history';
import { SessionStore } from '../state/session-store';
import { AppState } from '../state/app-state';
import { PromptController } from '../state/prompt-controller';
import { WelcomeScreen, WelcomeScreenState } from '../widgets/welcome-screen';
import { DensityOrbWidget } from '../widgets/density-orb-widget';
import { ChatView, ChatViewState } from '../widgets/chat-view';
```

findAllWidgets 函数直接在测试文件中复制（与 chat-view.test.ts 相同实现，用于 BFS 遍历 widget 树）。
</action>

<acceptance_criteria>
1. 文件 `packages/flitter-cli/src/__tests__/welcome-screen.test.ts` 存在
2. `rg -c "test\\(" packages/flitter-cli/src/__tests__/welcome-screen.test.ts` 返回 >= 10（至少 10 个 test 块）
3. `rg -n "DensityOrbWidget" packages/flitter-cli/src/__tests__/welcome-screen.test.ts | wc -l` 返回 >= 3（import、Group 1 断言、Group 4 断言）
4. `rg -n "Welcome to Amp" packages/flitter-cli/src/__tests__/welcome-screen.test.ts` 返回 1 行（Group 2 测试）
5. `rg -n "Use Tab/Shift\\+Tab" packages/flitter-cli/src/__tests__/welcome-screen.test.ts` 返回 1 行（Group 2 测试）
6. `rg -n "_listeners" packages/flitter-cli/src/__tests__/welcome-screen.test.ts | wc -l` 返回 2（Group 3 两个 listener 测试）
7. `rg -n "flitter-cli" packages/flitter-cli/src/__tests__/welcome-screen.test.ts | wc -l` 返回 >= 1（Group 4 test 4.2 断言不含该字符串）
</acceptance_criteria>
</task>

<task id="24-01-T4">
<title>更新 chat-view.test.ts — 修复 test 1.1 使其与新 WelcomeScreen 对齐</title>

<read_first>
- packages/flitter-cli/src/__tests__/chat-view.test.ts L157-L171 — Group 1 test 1.1 的当前断言内容（检查 `Center` 实例和 `'flitter-cli'` 文字）
- packages/flitter-cli/src/widgets/welcome-screen.ts — T1 创建的文件（WelcomeScreen 实例取代了 Center）
- packages/flitter-cli/src/widgets/density-orb-widget.ts — DensityOrbWidget（用于新断言）
</read_first>

<action>
修改 `packages/flitter-cli/src/__tests__/chat-view.test.ts` 中的 test 1.1：

1. 在文件顶部 import 区追加：
   ```typescript
   import { WelcomeScreen } from '../widgets/welcome-screen';
   ```

2. 将 test 1.1 的断言替换为：
   ```typescript
   expect(tree).toBeInstanceOf(WelcomeScreen);
   ```
   `ChatViewState.build()` 中 `case 'welcome': return new WelcomeScreen({ appState })` 返回 `WelcomeScreen` 实例，因此直接断言 `tree` 为 `WelcomeScreen` 实例即可。
</action>

<acceptance_criteria>
1. `rg -n "WelcomeScreen" packages/flitter-cli/src/__tests__/chat-view.test.ts | wc -l` 返回 >= 3（import + 断言）
2. `rg -n "DensityOrbWidget" packages/flitter-cli/src/__tests__/chat-view.test.ts | wc -l` 返回至少 2 行（import + 使用）
3. `rg -n "hasFlitterCli.*toBe\\(true\\)" packages/flitter-cli/src/__tests__/chat-view.test.ts | wc -l` 返回 0（不得再有关于 'flitter-cli' 文字存在性的正向断言）
4. `cd packages/flitter-cli && bun test src/__tests__/chat-view.test.ts 2>&1 | rg -i "pass|fail" | tail -5` 输出包含 `pass` 且不包含 `fail`（全部已有测试通过）
</acceptance_criteria>
</task>

<task id="24-01-T5">
<title>全量测试套件验证</title>

<read_first>
- packages/flitter-cli/package.json — 确认测试命令为 `bun test`
- packages/flitter-cli/src/__tests__/ — 全部测试文件列表（确认无新的测试因 chat-view 改动而失效）
</read_first>

<action>
在 `packages/flitter-cli` 目录下执行：
```bash
cd packages/flitter-cli && bun test 2>&1
```

观察输出：
- 新文件 `welcome-screen.test.ts` 中全部测试通过
- `chat-view.test.ts` 全部测试通过（包括修改后的 test 1.1）
- 其余所有测试文件无失败

如有测试失败，逐一修复后再重新运行，直至全套绿灯。
</action>

<acceptance_criteria>
1. `cd packages/flitter-cli && bun test 2>&1 | rg -E "^(✓|pass|fail)" | rg fail` 返回空（无失败测试）
2. `cd packages/flitter-cli && bun test 2>&1 | rg "welcome-screen"` 至少出现 1 行（welcome-screen.test.ts 被执行）
3. `cd packages/flitter-cli && bun test 2>&1 | rg -E "tests? passed"` 至少出现 1 行，且通过总数 >= 315（基线 305 个测试，Phase 24 新增后应 >= 315）
</acceptance_criteria>
</task>

---

## 验证

### must_haves（目标必须全部为 TRUE）

1. **WELC-01 满足**：`DensityOrbWidget({ variant: 'welcome' })` 出现在 `welcome-screen.ts` 的 build 方法中，Perlin 渐变动画由 DensityOrbWidget 内部自驱动，无需外部定时器
2. **WELC-02 满足**：`'Use Tab/Shift+Tab to navigate to previous'` 和 `'messages to edit or restore to a previous state'` 两行字符串以 `Color.cyan` 颜色出现在 `welcome-screen.ts` 的提示列中
3. **buildWelcomeScreen() 已删除**：`rg -n "buildWelcomeScreen" packages/flitter-cli/src/widgets/chat-view.ts | wc -l` 返回 0
4. **WelcomeScreen 已接管**：`rg -n "new WelcomeScreen" packages/flitter-cli/src/widgets/chat-view.ts` 返回 1 行，在 `case 'welcome':` 分支
5. **无监听器泄漏**：`WelcomeScreenState.dispose()` 中调用 `appState.removeListener()`，welcome-screen.test.ts Group 3 test 3.2 绿色通过
6. **无 agentMode prop**：`rg -n "agentMode" packages/flitter-cli/src/widgets/welcome-screen.ts | wc -l` 返回 0（DensityOrbWidget 无此 prop，D-06 偏差已处理）
7. **全部测试通过**：`cd packages/flitter-cli && bun test` 无 fail，pass 数 >= 315
8. **颜色精确对齐 ANSI golden file**：
   - `'Ctrl+O'` → `Color.blue`（对应 ANSI `[38;5;4m`）
   - `' for '` → `dim: true`（对应 ANSI `[2m`）
   - `'help'` → `Color.yellow`（对应 ANSI `[38;5;3m`）
   - Tab 提示两行 → `Color.cyan`（对应 ANSI `[38;5;6m`）

### nice_to_haves（非阻塞性，尽量满足）

1. `'Welcome to Amp'` 使用 `Color.green`（对应 golden file 绿色渐变帧的近似色），而非 dim 灰色
2. `SizedBox({ width: 4 })` 作为 Logo 与提示列的间距（golden file 显示约 4-8 列可接受）
3. `crossAxisAlignment: 'center'` 在外层 Row 使提示列垂直居中于 Logo
4. TypeScript 严格模式下无 `any` 警告（welcome-screen.ts 不使用 `as any` 类型断言）
5. 测试文件包含行内注释说明各 Group 测试意图

---

*Plan: 24-01 | Phase: 24 | Wave: 1 | Created: 2026-04-07*
