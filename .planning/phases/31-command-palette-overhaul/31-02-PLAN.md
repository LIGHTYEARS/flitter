# Plan 31-02: Register All Missing Commands

**Wave**: 2 (depends on 31-01)
**Requirements**: CPAL-04

## Goal

Register all 15+ commands matching AMP's observed command palette, covering amp/mode/thread/prompt/context/news categories.

## AMP Command List (from golden screenshot)

| # | Category | Label | Shortcut | Exists? |
|---|----------|-------|----------|---------|
| 1 | amp | help | — | No |
| 2 | mode | use rush | — | No |
| 3 | mode | use large | — | No |
| 4 | mode | use deep | — | No |
| 5 | mode | set | — | No |
| 6 | mode | toggle | Ctrl s | Yes (shortcut: cycle-mode) |
| 7 | thread | switch | — | Yes |
| 8 | thread | new | — | Yes |
| 9 | prompt | open in editor | Ctrl g | Yes (shortcut: open-editor) |
| 10 | thread | map | — | Yes |
| 11 | thread | switch to cluster | — | No |
| 12 | context | analyze | — | No |
| 13 | news | open in browser | — | No |
| 14 | thread | set visibility | — | Yes |
| 15 | prompt | paste image from clipboard | Ctrl v | No (shortcut exists but not palette cmd) |

## Tasks

### Task 1: Add missing commands to buildCommandList

**File**: `packages/flitter-cli/src/commands/command-registry.ts`

New commands to register:
1. `help` (category: amp) — show shortcut help overlay
2. `use-rush` (category: mode) — set agent mode to rush
3. `use-large` (category: mode) — set agent mode to large
4. `use-deep` (category: mode) — set agent mode to deep
5. `mode-set` (category: mode) — open mode selector
6. `switch-to-cluster` (category: thread) — show thread cluster picker
7. `context-analyze` (category: context) — show context analysis modal
8. `news-open` (category: news) — open news in browser
9. `paste-image` (category: prompt) — paste image from clipboard

### Task 2: Reorder commands to match AMP order

**File**: `packages/flitter-cli/src/commands/command-registry.ts`

- Ensure the command list order matches AMP exactly
- Shortcut-derived commands that now appear in palette with category should NOT duplicate (e.g., mode toggle = Ctrl+S)
- Remove shortcut-derived commands from the registry loop that have explicit palette entries with categories

### Task 3: Remove commands not in AMP palette

Remove from palette display:
- `thread-navigate-back` (not in AMP palette)
- `thread-navigate-forward` (not in AMP palette)
- `toggle-tool-calls` (not in AMP palette — exists as Alt+T shortcut only)
- `insert-file-mention` (not in AMP palette)

Also exclude shortcut-derived commands that are NOT in AMP palette:
- `clear-conversation` (Ctrl+L)
- `copy-last-response` (Ctrl+Shift+C)
- `toggle-tool-calls` (Alt+T)
- `toggle-deep-reasoning` (Alt+D)
- `history-search` (Ctrl+R)
- `toggle-shortcut-help` (?)

## Verification

- `npx tsc --noEmit` passes
- 15+ commands registered
- All 6 categories present: amp, mode, thread, prompt, context, news
- Command order matches AMP golden screenshot
