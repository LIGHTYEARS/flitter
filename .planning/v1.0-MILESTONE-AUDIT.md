---
milestone: v1.0
audit_date: "2026-04-15"
audited_by: "4 parallel research agents"
result: gaps_found
gaps:
  requirements:
    - id: LLM-01
      description: "Anthropic Provider missing retry logic, thinking budget dynamics, cache_control, prompt injection defense"
      reason: "Implementation does not match amp's OwT class. No exponential backoff, no EfR/dfR sanitization"
      severity: critical
      phase: 7b
      fix_type: deep_port
    - id: LLM-06
      description: "SSE retry/reconnect strategy not implemented at provider level"
      reason: "Phase 7b removed stream/ infrastructure but did not port retry to SDK layer"
      severity: critical
      phase: 7b
      fix_type: deep_port
    - id: AGNT-01
      description: "ThreadWorker state machine is 347 lines vs amp's 1200 lines — missing retry, snapshots, blocked-on-user, title gen"
      reason: "Shallow port of amp's ov class. Only happy-path states implemented"
      severity: critical
      phase: 10
      fix_type: deep_port
    - id: AGNT-07
      description: "Permission matcher inverts enable/disable precedence vs amp. Uses string includes() not glob"
      reason: "checkToolEnabled logic is backwards relative to amp's yy function"
      severity: high
      phase: 10
      fix_type: fix
    - id: AGNT-08
      description: "Permission DSL uses string matching not glob patterns"
      reason: "tools.disable/enable lists need picomatch-style matching per amp"
      severity: high
      phase: 10
      fix_type: fix
    - id: AGNT-06
      description: "System prompt is generic, not derived from amp. Context blocks missing mode awareness and 32KB budget"
      reason: "BASE_ROLE_PROMPT bears no resemblance to amp's actual prompt"
      severity: high
      phase: 10
      fix_type: rewrite
    - id: DATA-02
      description: "ConfigService only 2-tier (global+workspace), not 3-tier. Imperative not reactive"
      reason: "Missing managed/admin settings tier. Not using Observable pipeline like amp's LX"
      severity: high
      phase: 9
      fix_type: deep_port
    - id: DATA-04
      description: "Guidance loader uses hand-rolled YAML/glob. Missing subtree type, byte budget, safety checks"
      reason: "parseSimpleYaml breaks on complex YAML. simpleGlobMatch != picomatch"
      severity: high
      phase: 9
      fix_type: rewrite
    - id: DATA-03
      description: "SkillService only 2-tier discovery. Missing file validation and path traversal check"
      reason: "Amp has 3-tier (workspace+project+global). Missing jqR/SqR security"
      severity: high
      phase: 9
      fix_type: fix
    - id: LLM-07
      description: "MCP connection default transport factory throws. Missing ping/keepalive, sampling/elicitation"
      reason: "_createTransport() always throws instead of dynamic Stdio/SSE/HTTP creation"
      severity: high
      phase: 8
      fix_type: fix
    - id: LLM-08
      description: "MCP StreamableHTTP/SSE transports missing auth recovery (401/403) and reconnection"
      reason: "Amp handles OAuth re-auth on 401, insufficient_scope on 403, SSE reconnection"
      severity: critical
      phase: 8
      fix_type: deep_port
    - id: LLM-09
      description: "MCP server-manager exposes all tools unconditionally — no mcpPermissions check"
      reason: "Amp's ADT checks mcpPermissions. Also missing includeToolsBySkill filtering and cache"
      severity: high
      phase: 8
      fix_type: fix
    - id: CLI-01
      description: "config and threads commands are empty stubs (all TODO)"
      reason: "Handler functions void all parameters. Non-functional"
      severity: high
      phase: 11
      fix_type: implement
    - id: TUI-06
      description: "hitTest missing allowHitTestOutsideBounds — breaks overlay interactivity"
      reason: "Amp checks allowHitTestOutsideBounds and recurses into children. Flitter returns false"
      severity: critical
      phase: 4
      fix_type: fix
    - id: TUI-06
      description: "TuiController.getSize() returns Infinity in Bun (known blocker)"
      reason: "Uses ?? instead of amp's 4-layer defense. Bun returns Infinity not undefined"
      severity: critical
      phase: "12.1"
      fix_type: fix
  integration:
    - from_phase: 7b
      to_phase: 10
      connection: "Provider retry → ThreadWorker retry loop"
      reason: "ThreadWorker has no retry/backoff. Provider has no retry. Neither layer retries"
    - from_phase: 9
      to_phase: 10
      connection: "ConfigService 3-tier → Permission engine config"
      reason: "Permission engine reads config but config is only 2-tier, missing admin overrides"
    - from_phase: 9
      to_phase: 10
      connection: "Guidance loader → context-blocks prompt assembly"
      reason: "context-blocks has no 32KB budget enforcement because guidance-loader doesn't report sizes"
    - from_phase: 8
      to_phase: 10
      connection: "MCP server-manager → tool orchestrator"
      reason: "Server-manager exposes all tools without permission check. Orchestrator batching has conflict bug"
  flows:
    - name: "Interactive TUI conversation"
      broken_at: "TUI launch — getSize() returns Infinity"
      reason: "TuiController.getSize() bug. Also async cleanup races leave terminal in raw mode"
      severity: critical
    - name: "LLM retry on transient error"
      broken_at: "No retry at any layer"
      reason: "Neither Provider nor ThreadWorker implements retry/backoff. User sees immediate failure"
      severity: critical
    - name: "MCP server reconnection"
      broken_at: "Transport disconnect"
      reason: "SSE/StreamableHTTP transports do not reconnect. Connection is lost permanently"
      severity: high
    - name: "Tool permission enforcement"
      broken_at: "enable/disable precedence"
      reason: "Permission matcher inverts amp's logic. enable is treated as override instead of whitelist"
      severity: high
---

# Flitter v1.0 — Milestone Audit

**Date:** 2026-04-15
**Status:** GAPS FOUND — 15 requirement gaps, 4 integration gaps, 4 broken flows

## Required Reading

Before any gap closure work, read the comprehensive audit document:
- **`.planning/AMP-CROSS-REFERENCE-AUDIT.md`** — Full 176-file audit with specific amp references, line numbers, and divergence analysis

## Gap Summary

### Critical (P0 — blocks TUI launch and basic functionality)
1. **CR-09**: TuiController.getSize() Infinity bug (Phase 12.1)
2. **CR-10**: TuiController async cleanup races (Phase 12.1)
3. **CR-05**: hitTest missing allowHitTestOutsideBounds (Phase 4)
4. **CR-01**: Anthropic Provider no retry, no security sanitization (Phase 7b)
5. **CR-02/03**: Missing prompt injection defense in sanitize-unicode + transformer (Phase 7b)
6. **CR-04**: ThreadWorker shallow state machine — no retry/backoff (Phase 10)
7. **CR-06/07**: MCP transport no auth recovery, no reconnection (Phase 8)
8. **CR-08**: MCP connection default transport factory throws (Phase 8)

### High (P1 — behavioral divergence before v1)
9. **HI-01**: Permission matcher inverted enable/disable precedence (Phase 10)
10. **HI-02**: System prompt generic, not from amp (Phase 10)
11. **HI-03**: ConfigService 2-tier not 3-tier (Phase 9)
12. **HI-04**: Guidance loader hand-rolled YAML/glob (Phase 9)
13. **HI-05**: Context blocks no mode awareness, no 32KB budget (Phase 10)
14. **HI-06**: MCP server-manager no permissions checking (Phase 8)
15. **HI-07**: Tool orchestrator batch conflict bug (Phase 10)
16. **HI-08**: Skill parser hand-rolled YAML, missing validation (Phase 9)
17. **HI-09**: interactive.ts no dynamic theme, unsafe casts (Phase 11)
18. **HI-10**: config/threads commands empty stubs (Phase 11)

### Medium (P2 — missing references, unverified but probably functional)
- ~130 files across all packages lacking `逆向:` annotations
- See AMP-CROSS-REFERENCE-AUDIT.md for complete listing

## Audit Methodology
- 4 parallel research agents audited all ~176 source files
- Each file checked for `逆向:` comments, then spot-checked against amp source
- Key functions cross-referenced with specific amp-cli-reversed/ locations
- Severity classified as: no_reference / reference_mismatch / shallow_port
