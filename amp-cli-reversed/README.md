# amp-cli Deobfuscated Module Directory

> Auto-generated by `assemble-es6-modules.py`

## Summary

- **Total modules**: 1069
- **Total size**: 7.3 MB (source text)

### Module count by type

| Type | Count |
|------|-------|
| bun-internal | 88 |
| cjs | 658 |
| esbuild-bundle | 6 |
| esm | 278 |
| scope-hoisted | 39 |

### Module count by category

| Category | Count |
|----------|-------|
| bun-internal | 88 |
| cli | 20 |
| esbuild-bundle | 6 |
| framework | 5 |
| npm-pkg | 61 |
| rendering | 2 |
| schema | 66 |
| state | 1 |
| unknown | 128 |
| util | 690 |
| widget | 2 |

## Directory Structure

```
modules/
+-- _module-index.json     -- Complete module index (semantic name -> metadata)
+-- _preamble.js           -- Bundle runtime helpers (RT/PT wrappers, etc.)
+-- app/                   -- Application/CLI code (scope-hoisted, category=cli)
+-- framework/             -- Framework code (widget, state, rendering, framework)
+-- util/                  -- Utility modules (scope-hoisted, category=util)
+-- vendor/
|   +-- cjs/               -- CJS vendor modules (RT-wrapped, from npm packages)
|   +-- esm/               -- ESM vendor modules (PT-wrapped)
+-- bun-internal/          -- Bun runtime internal modules (node/, internal/, thirdparty/, bun/)
+-- esbuild-bundles/       -- 6 esbuild CJS bundles
```

## Navigation Guide

- **App code** (`app/`): The main CLI application logic -- LLM providers, tool execution,
  command system, conversation management, etc. These are the scope-hoisted modules
  extracted from Segment 1 with `category=cli`.

- **Framework** (`framework/`): Widget framework, layout engine, render pipeline,
  state management. Scope-hoisted modules with categories: framework, widget, state, rendering.

- **Utilities** (`util/`): Shared utility code -- HTTP SDK, JSON schema validation,
  connection transport, etc. Scope-hoisted modules with `category=util`.

- **Vendor CJS** (`vendor/cjs/`): Third-party npm packages bundled as CJS (RT wrapper).
  Includes OpenTelemetry, RxJS, debug, and many more.

- **Vendor ESM** (`vendor/esm/`): Third-party packages bundled as ESM (PT wrapper).
  Includes Zod schemas, Anthropic SDK helpers, MCP protocol schemas, etc.

- **Bun internals** (`bun-internal/`): Bun runtime internal modules -- Node.js compatibility
  layers, streams, SQL adapters, FFI bindings. Preserve original subdirectory structure.

- **Esbuild bundles** (`esbuild-bundles/`): 6 pre-bundled CJS modules produced by esbuild,
  kept as-is.

- **Preamble** (`_preamble.js`): The Segment 2 preamble containing `var` declarations and
  helper functions (`RT`, `PT`, `Ra`, `c0`, etc.) that the other modules depend on.
