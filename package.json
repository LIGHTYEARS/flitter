{
  "name": "flitter",
  "private": true,
  "scripts": {
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm -r run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@mariozechner/pi-ai": "^0.65.2"
  }
}
