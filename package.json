{
  "name": "flitter-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Flitter monorepo",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun build packages/*/src/index.ts --outdir=dist",
    "test": "bun test",
    "lint": "echo 'Lint script placeholder'",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "flitter",
    "ai",
    "agent"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "bun-types": "^1.1.0",
    "typescript": "^5.4.0"
  }
}
